# 数据库结构文档归档说明

本文件原为早期 NoSQL/样例数据快照，包含已经过时的字段、路径和示例身份信息，不再作为数据库结构、接口契约、迁移依据或 AI 实施依据。为避免后续 Agent 误读，历史快照正文已移除。

当前权威来源只有：

- `server/db/init.sql`：完整基线表结构。
- `server/db/deploy/*.sql`：按文件名顺序执行的生产迁移。
- `DatabaseSchemaNew.md`：面向阅读的当前模型索引，不能替代 SQL。
- `server/db/deploy/README.md`：迁移命名、幂等、校验和和回滚约束。

数据库结构发生变化时，必须新增带时间戳的幂等迁移并更新迁移账本；不得恢复或修改本归档文件作为结构依据。
