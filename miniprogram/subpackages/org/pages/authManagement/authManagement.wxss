.auth-page {
  min-height: 100vh;
  padding: 28rpx 24rpx 44rpx;
  box-sizing: border-box;
}

.auth-hero {
  margin-bottom: 22rpx;
  padding: 34rpx;
  border-radius: 34rpx;
  background: linear-gradient(135deg, #2563eb, #3b82f6 54%, #60a5fa);
  box-shadow: 0 22rpx 46rpx rgba(37,99,235,0.18);
}

.auth-card {
  padding: 24rpx;
}

.auth-tabs {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
  margin-bottom: 20rpx;
}

.auth-tabs.tabs-card { display: flex; flex-wrap: wrap; }
.auth-tabs .feature-tab { flex: 1 1 180rpx; }

.request-item,
.policy-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18rpx;
  padding: 20rpx 0;
  border-bottom: 1rpx solid rgba(226,232,240,0.9);
}

.request-copy {
  flex: 1;
  min-width: 0;
}

.claim-toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
  padding: 4rpx 0 14rpx;
  border-bottom: 1rpx solid rgba(226,232,240,0.9);
}

.auth-filter-row { display: flex; align-items: center; gap: 12rpx; margin: 4rpx 0 18rpx; }
.auth-filter-input { flex: 1; min-width: 0; }
.auth-filter-picker { min-width: 180rpx; padding: 14rpx 16rpx; }

.claim-toolbar-copy {
  color: #64748b;
  font-size: var(--ui-type-meta);
  line-height: 1.5;
}

.claim-toolbar-actions {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  flex-wrap: wrap;
  gap: 10rpx;
}

.claim-tool {
  min-height: 52rpx;
  padding: 0 14rpx;
  border: 1rpx solid rgba(203,213,225,0.82);
  border-radius: 12rpx;
  color: #52677f;
  font-size: var(--ui-type-caption);
  line-height: 52rpx;
  background: rgba(248,250,252,0.8);
}

.batch-issue-btn {
  min-width: 132rpx;
}

.claim-selector {
  display: flex;
  align-items: center;
  justify-content: center;
  flex: 0 0 48rpx;
  width: 48rpx;
  height: 48rpx;
  border: 1rpx solid rgba(148,163,184,0.58);
  border-radius: 12rpx;
  background: rgba(248,250,252,0.84);
}

.claim-selector-active {
  border-color: rgba(59,130,246,0.72);
  background: rgba(219,234,254,0.9);
}

.claim-selector-mark {
  color: #2563eb;
  font-size: var(--ui-type-body);
  font-weight: 900;
  line-height: 1;
}

.request-name {
  color: #0f172a;
  font-size: var(--ui-type-emphasis);
  font-weight: 800;
  line-height: 1.5;
}

.request-meta,
.loading-text,
.empty-state {
  margin-top: 5rpx;
  color: #64748b;
  font-size: var(--ui-type-meta);
  line-height: 1.65;
  overflow-wrap: anywhere;
}

.empty-state,
.loading-text {
  padding: 34rpx 14rpx;
  text-align: center;
}

.compact-btn {
  width: auto;
  min-width: 146rpx;
  min-height: 66rpx;
  margin: 0;
  padding: 12rpx 18rpx;
  border-radius: 16rpx;
  font-size: var(--ui-type-meta);
  line-height: 1.45;
}

.policy-field {
  padding: 20rpx 0;
}

.policy-input {
  margin-top: 12rpx;
}

.policy-time-grid {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(150rpx, 0.58fr) auto;
  gap: 12rpx;
  align-items: center;
  margin-top: 12rpx;
}

.policy-clear {
  min-height: 56rpx;
  padding: 0 16rpx;
  border-radius: 12rpx;
  background: rgba(226, 232, 240, 0.7);
  color: #475569;
  font-size: var(--ui-type-control);
  line-height: 56rpx;
  text-align: center;
}

@media (min-width: 520px) {
  .policy-time-grid {
    grid-template-columns: minmax(0, 1fr) minmax(120px, 0.5fr) auto;
    gap: 10px;
  }

  .policy-clear {
    min-height: 40px;
    padding: 0 14px;
    border-radius: 8px;
    line-height: 40px;
  }
}

.policy-save {
  margin-top: 18rpx;
}

.audit-item {
  padding: 18rpx 0;
  border-bottom: 1rpx solid rgba(226,232,240,0.9);
}

.auth-dialog {
  background: linear-gradient(145deg, rgba(255,255,255,0.99), rgba(247,250,255,0.97));
  border: 1rpx solid rgba(255,255,255,0.94);
  box-shadow: 0 28rpx 68rpx rgba(15,23,42,0.22);
}

.issued-code-list {
  max-height: 480rpx;
  margin: 20rpx 0;
}

.issued-code-row + .issued-code-row {
  margin-top: 12rpx;
}

.issued-person {
  margin-bottom: 8rpx;
  color: #334155;
  font-size: var(--ui-type-meta);
  font-weight: 700;
  line-height: 1.5;
  overflow-wrap: anywhere;
}

.issued-code {
  padding: 18rpx;
  border: 1rpx solid rgba(147,197,253,0.68);
  border-radius: 16rpx;
  color: #2563eb;
  font-size: var(--ui-type-section);
  font-weight: 800;
  letter-spacing: 3rpx;
  line-height: 1.5;
  text-align: center;
  background: rgba(239,246,255,0.92);
  overflow-wrap: anywhere;
}

@media (min-width: 520px) {
  .auth-page {
    max-width: 820px;
    margin: 0 auto;
    padding: 28px 24px 42px;
  }

  .auth-hero {
    padding: 28px 30px;
    border-radius: 26px;
  }

  .auth-card { padding: 24px; }
  .claim-toolbar-copy,
  .claim-tool,
  .request-meta,
  .loading-text,
  .empty-state { font-size: var(--ui-type-meta); }
  .auth-tab { font-size: var(--ui-tab-font-size); }
  .request-name { font-size: var(--ui-type-emphasis); }

  .compact-btn {
    min-height: 42px;
    padding: 10px 16px;
    border-radius: 12px;
    font-size: var(--ui-type-label);
  }

  .claim-selector {
    flex-basis: 34px;
    width: 34px;
    height: 34px;
    border-radius: 8px;
  }

  .issued-code-list {
    max-height: 360px;
  }
}
.global-scope-notice {
  display: flex;
  align-items: flex-start;
  gap: 14rpx;
  margin-bottom: 20rpx;
  padding: 19rpx 20rpx;
  border: 1rpx solid rgba(147, 197, 253, 0.66);
  border-radius: 18rpx;
  background: linear-gradient(135deg, rgba(239, 246, 255, 0.96), rgba(224, 242, 254, 0.80));
  box-shadow: 0 12rpx 26rpx rgba(37, 99, 235, 0.08), inset 0 1rpx 0 rgba(255, 255, 255, 0.84);
  backdrop-filter: blur(20rpx);
}

.global-scope-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 50rpx;
  height: 50rpx;
  flex-shrink: 0;
  border-radius: 16rpx;
  background: linear-gradient(135deg, rgba(219, 234, 254, 0.92), rgba(239, 246, 255, 0.80));
}

.global-scope-copy {
  flex: 1;
  min-width: 0;
}

.global-scope-title {
  color: #2563eb;
  font-size: var(--ui-type-label);
  font-weight: 800;
  line-height: 1.5;
}

.global-scope-detail {
  margin-top: 4rpx;
  color: #526d8d;
  font-size: var(--ui-type-caption);
  line-height: 1.62;
  overflow-wrap: anywhere;
}

@media (min-width: 520px) {
  .global-scope-notice {
    gap: 11px;
    margin-bottom: 16px;
    padding: 14px 16px;
    border-radius: 13px;
  }

  .global-scope-icon {
    width: 38px;
    height: 38px;
    border-radius: 11px;
  }
}
