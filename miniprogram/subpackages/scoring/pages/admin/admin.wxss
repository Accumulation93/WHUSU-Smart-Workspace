@import '../../../../pages/home/home.wxss';

page {
  min-height: 100%;
  background:
    radial-gradient(circle at 12% 10%, rgba(96, 165, 250, 0.16) 0%, transparent 26%),
    radial-gradient(circle at 86% 18%, rgba(191, 219, 254, 0.22) 0%, transparent 24%),
    radial-gradient(circle at 18% 88%, rgba(125, 211, 252, 0.10) 0%, transparent 22%),
    linear-gradient(135deg, #f8fbff 0%, #f1f6fc 48%, #edf3fa 100%);
  color: #1f2a37;
  font-family: "PingFang SC", "Microsoft YaHei", sans-serif;
}

button,
input,
textarea,
picker,
view,
text,
scroll-view {
  font-family: "PingFang SC", "HarmonyOS Sans SC", "Microsoft YaHei", sans-serif;
}

.page {
  position: relative;
  min-height: 100vh;
  padding: 30rpx 24rpx 42rpx;
  box-sizing: border-box;
}

.page::before {
  content: "";
  position: fixed;
  top: -120rpx;
  right: -90rpx;
  width: 320rpx;
  height: 320rpx;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(96, 165, 250, 0.18) 0%, rgba(96, 165, 250, 0) 72%);
  pointer-events: none;
}

.page::after {
  content: "";
  position: fixed;
  left: -120rpx;
  bottom: 150rpx;
  width: 280rpx;
  height: 280rpx;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(147, 197, 253, 0.16) 0%, rgba(147, 197, 253, 0) 72%);
  pointer-events: none;
}

.hero {
  position: relative;
  overflow: hidden;
  margin-bottom: 28rpx;
  padding: 42rpx 34rpx 38rpx;
  border-radius: 38rpx;
  background:
    linear-gradient(135deg, rgba(37, 99, 235, 0.98) 0%, rgba(59, 130, 246, 0.95) 42%, rgba(96, 165, 250, 0.90) 100%),
    linear-gradient(180deg, rgba(255, 255, 255, 0.10), rgba(255, 255, 255, 0));
  border: 1rpx solid rgba(255, 255, 255, 0.24);
  box-shadow:
    0 24rpx 54rpx rgba(37, 99, 235, 0.22),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.24);
  animation: glassFadeUp 0.42s ease both;
}

.hero-admin {
  padding: 40rpx 34rpx 36rpx;
  border-radius: 34rpx;
  background:
    linear-gradient(135deg, rgba(15, 23, 42, 0.96) 0%, rgba(30, 41, 59, 0.96) 42%, rgba(37, 99, 235, 0.92) 100%);
  box-shadow:
    0 26rpx 54rpx rgba(15, 23, 42, 0.22),
    0 0 28rpx rgba(37, 99, 235, 0.08),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.14);
}

.hero-admin::before {
  top: -130rpx;
  right: -56rpx;
  width: 280rpx;
  height: 280rpx;
  background: radial-gradient(circle, rgba(96, 165, 250, 0.30) 0%, rgba(96, 165, 250, 0.06) 46%, rgba(96, 165, 250, 0) 72%);
}

.hero-admin::after {
  content: "";
  position: absolute;
  left: 34rpx;
  right: 34rpx;
  bottom: 20rpx;
  height: 1rpx;
  background: linear-gradient(90deg, rgba(255, 255, 255, 0.16) 0%, rgba(191, 219, 254, 0.34) 52%, rgba(255, 255, 255, 0) 100%);
}

.hero-admin .hero-badge {
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.24) 0%, rgba(148, 163, 184, 0.14) 100%);
  border-color: rgba(191, 219, 254, 0.26);
  box-shadow: 0 10rpx 26rpx rgba(15, 23, 42, 0.12);
}

.hero-admin .hero-title {
  font-size: var(--ui-type-page);
  letter-spacing: 0.8rpx;
}

.hero-admin .hero-subtitle {
  color: rgba(241, 245, 249, 0.90);
}

.hero-admin-subtitle {
  margin-top: 16rpx;
  color: #ffffff;
  font-size: var(--ui-type-section);
  font-weight: 700;
  line-height: 1.55;
  letter-spacing: 0.4rpx;
}

.tab {
  color: #49627f;
}

.tab-active {
  color: #ffffff;
  background: linear-gradient(135deg, #2563eb 0%, #3b82f6 58%, #60a5fa 100%);
  box-shadow: 0 16rpx 30rpx rgba(37, 99, 235, 0.22);
}

/* Department tabs inside multi-picker */
.dept-tab-scroll { margin: 0 24rpx 12rpx; }
.dept-tab-scroll-view { width: 100%; white-space: nowrap; }
.dept-tab-row { display: inline-flex; gap: 8rpx; }
.dept-tab {
  display: inline-flex; align-items: center; gap: 4rpx; flex-shrink: 0;
  font-size: var(--ui-type-meta); padding: 10rpx 18rpx; border-radius: 20rpx; font-weight: 600;
  background: rgba(241, 245, 249, 0.85); color: #475569;
  border: 1rpx solid rgba(203, 213, 225, 0.55);
  transition: color 0.2s ease, background 0.2s ease, border-color 0.2s ease, box-shadow 0.24s ease, transform 0.2s ease;
}
.dept-tab-active {
  background: linear-gradient(135deg, #2563eb 0%, #3b82f6 100%);
  color: #fff; border-color: transparent;
  box-shadow: 0 6rpx 16rpx rgba(37, 99, 235, 0.20);
}
.dept-tab-count { font-size: var(--ui-type-caption); opacity: 0.85; }

.section-title,
.list-title {
  color: #10233d;
}

.field-label {
  color: #52647c;
}

.field-input,
.picker-value,
.field-textarea {
  border: 1rpx solid rgba(203, 213, 225, 0.82);
  box-shadow:
    inset 0 1rpx 0 rgba(255, 255, 255, 0.82),
    0 8rpx 18rpx rgba(15, 23, 42, 0.035);
}

.field-input:focus,
.field-textarea:focus {
  border-color: rgba(59, 130, 246, 0.96);
  box-shadow:
    0 14rpx 28rpx rgba(37, 99, 235, 0.12),
    0 0 0 4rpx rgba(191, 219, 254, 0.62),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.9);
}

.picker-value:active {
  border-color: rgba(59, 130, 246, 0.96);
  box-shadow:
    0 12rpx 24rpx rgba(37, 99, 235, 0.12),
    0 0 0 4rpx rgba(191, 219, 254, 0.52),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.9);
}

.primary-btn {
  background: linear-gradient(135deg, #2563eb 0%, #3b82f6 58%, #60a5fa 100%);
  box-shadow: 0 16rpx 32rpx rgba(37, 99, 235, 0.20);
}

.secondary-btn {
  color: #2563eb;
  border: 1rpx solid rgba(147, 197, 253, 0.88);
  box-shadow: 0 12rpx 24rpx rgba(37, 99, 235, 0.09);
}

.link,
.pill-link,
.pill-close {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 10rpx 20rpx;
  border-radius: 999rpx;
  font-size: var(--ui-type-control);
  font-weight: 600;
  line-height: 1;
  background: rgba(219, 234, 254, 0.65);
  color: #2563eb;
  border: 1rpx solid rgba(147, 197, 253, 0.45);
  transition: transform 0.12s ease, opacity 0.12s ease;
}

.link:active,
.pill-link:active,
.pill-close:active {
  transform: scale(0.94);
  opacity: 0.85;
}

.danger,
.pill-close {
  background: rgba(254, 226, 226, 0.65);
  color: #dc2626;
  border-color: rgba(252, 165, 165, 0.45);
}

.current-chip,
.select-chip {
  color: #2563eb;
  border: 1rpx solid rgba(96, 165, 250, 0.86);
}

.template-section-tip {
  color: #51657f;
  border: 1rpx solid rgba(147, 197, 253, 0.76);
}

.template-hero {
  position: relative;
  overflow: hidden;
  margin-bottom: 22rpx;
  padding: 30rpx 26rpx 28rpx;
  border-radius: 28rpx;
  background:
    linear-gradient(135deg, rgba(255, 255, 255, 0.88) 0%, rgba(238, 246, 255, 0.78) 100%);
  border: 1rpx solid rgba(219, 234, 254, 0.92);
  box-shadow:
    0 18rpx 38rpx rgba(37, 99, 235, 0.08),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.86);
  animation: glassFadeUp 0.4s ease both;
}

.template-hero::before {
  content: "";
  position: absolute;
  top: -120rpx;
  right: -80rpx;
  width: 260rpx;
  height: 260rpx;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(59, 130, 246, 0.18) 0%, rgba(59, 130, 246, 0) 68%);
  pointer-events: none;
}

.template-hero::after {
  content: "";
  position: absolute;
  left: -60rpx;
  bottom: -90rpx;
  width: 220rpx;
  height: 220rpx;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(14, 165, 233, 0.12) 0%, rgba(14, 165, 233, 0) 70%);
  pointer-events: none;
}

.template-hero-badge,
.template-hero-title,
.template-hero-desc {
  position: relative;
  z-index: 1;
}

.template-hero-badge {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 8rpx 18rpx;
  border-radius: 999rpx;
  background: linear-gradient(135deg, rgba(219, 234, 254, 0.98) 0%, rgba(191, 219, 254, 0.9) 100%);
  color: #2563eb;
  font-size: var(--ui-type-caption);
  font-weight: 700;
  box-shadow: 0 10rpx 20rpx rgba(37, 99, 235, 0.08);
}

.template-hero-title {
  margin-top: 16rpx;
  color: #0f172a;
  font-size: var(--ui-type-page);
  font-weight: 800;
  line-height: 1.36;
}

.template-hero-desc {
  margin-top: 12rpx;
  color: #5f7289;
  font-size: var(--ui-type-control);
  line-height: 1.8;
}

.template-section-head {
  margin-bottom: 14rpx;
}

.template-section-head .list-title {
  margin-bottom: 10rpx;
}

.template-section-tip {
  padding: 14rpx 18rpx;
  border-radius: 18rpx;
  background: linear-gradient(135deg, rgba(239, 246, 255, 0.92) 0%, rgba(247, 250, 255, 0.84) 100%);
  border: 1rpx solid rgba(191, 219, 254, 0.72);
  color: #5f7289;
  font-size: var(--ui-type-meta);
  line-height: 1.72;
  box-shadow: inset 0 1rpx 0 rgba(255, 255, 255, 0.82);
}

.template-save-btn {
  position: relative;
  overflow: hidden;
  box-shadow:
    0 18rpx 34rpx rgba(37, 99, 235, 0.18),
    0 0 26rpx rgba(96, 165, 250, 0.16),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.26);
}

.template-save-btn::before {
  content: "";
  position: absolute;
  inset: -24% auto auto -24%;
  width: 220rpx;
  height: 220rpx;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(255, 255, 255, 0.24) 0%, rgba(255, 255, 255, 0) 66%);
  pointer-events: none;
}

.hero::before {
  content: "";
  position: absolute;
  top: -90rpx;
  right: -35rpx;
  width: 240rpx;
  height: 240rpx;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.14);
}

.hero-badge,
.hero-title,
.hero-subtitle {
  position: relative;
  z-index: 1;
}

.hero-badge {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 18rpx;
  padding: 10rpx 24rpx;
  border-radius: 999rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.22) 0%, rgba(255, 255, 255, 0.14) 100%);
  border: 1rpx solid rgba(255, 255, 255, 0.28);
  color: #f8fbff;
  font-size: var(--ui-type-meta);
  font-weight: 600;
  letter-spacing: 1rpx;
}

.hero-title {
  color: #ffffff;
  font-size: var(--ui-type-page);
  font-weight: 700;
  line-height: 1.24;
  letter-spacing: 1rpx;
}

.hero-subtitle {
  margin-top: 12rpx;
  color: rgba(255, 255, 255, 0.92);
  font-size: var(--ui-type-emphasis);
  line-height: 1.72;
}

.section,
.empty-card {
  position: relative;
  margin-bottom: 24rpx;
  padding: 26rpx 24rpx;
  border-radius: 30rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.82) 0%, rgba(248, 251, 255, 0.74) 100%);
  border: 1rpx solid rgba(255, 255, 255, 0.62);
  box-shadow:
    0 18rpx 36rpx rgba(15, 23, 42, 0.06),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.78);
  backdrop-filter: blur(24rpx);
  -webkit-backdrop-filter: blur(24rpx);
  box-sizing: border-box;
  transition: transform 0.22s ease, box-shadow 0.28s ease, border-color 0.24s ease;
  animation: glassFadeUp 0.46s ease both;
}

.empty-card {
  text-align: center;
  padding-top: 64rpx;
  padding-bottom: 64rpx;
}

.tabs {
  display: flex;
  flex-wrap: wrap;
  gap: var(--ui-tab-gap, 8rpx);
  margin-bottom: 22rpx;
}

.tab {
  min-height: var(--ui-tab-min-height, 66rpx);
  padding: var(--ui-tab-padding-y, 14rpx) var(--ui-tab-padding-x, 18rpx);
  border-radius: var(--ui-tab-radius, 20rpx);
  background: rgba(255, 255, 255, 0.74);
  border: 1rpx solid rgba(255, 255, 255, 0.54);
  color: #53687f;
  font-size: var(--ui-tab-font-size, 24rpx);
  line-height: var(--ui-leading-control, 1.35);
  font-weight: 600;
  box-shadow: 0 10rpx 22rpx rgba(15, 23, 42, 0.04);
  transition: transform 0.18s ease, box-shadow 0.22s ease, background 0.22s ease, color 0.22s ease;
}

.tab-active {
  color: #ffffff;
  background: linear-gradient(135deg, #2563eb 0%, #60a5fa 100%);
  box-shadow: 0 16rpx 30rpx rgba(37, 99, 235, 0.18);
}

.tab:active {
  transform: translateY(1rpx) scale(0.985);
}

.section-title,
.list-title {
  position: relative;
  padding-left: 18rpx;
  margin-bottom: 18rpx;
  color: #0f172a;
  font-size: var(--ui-type-section);
  font-weight: 700;
}

.section-title::before,
.list-title::before {
  content: "";
  position: absolute;
  left: 0;
  top: 50%;
  width: 8rpx;
  height: 28rpx;
  border-radius: 999rpx;
  transform: translateY(-50%);
  background: linear-gradient(180deg, #2563eb 0%, #60a5fa 100%);
  box-shadow: 0 0 14rpx rgba(59, 130, 246, 0.24);
}

.field {
  margin-bottom: 16rpx;
}

.field-row {
  display: flex;
  gap: 16rpx;
}

.field-half {
  flex: 1;
  min-width: 0;
}

.field-label {
  margin-bottom: 10rpx;
  color: #64748b;
  font-size: var(--ui-type-meta);
  line-height: 1.5;
  padding-left: 24rpx;
}

.field-value {
  color: #1e293b;
  font-size: var(--ui-type-section);
  font-weight: 700;
  line-height: 1.5;
  padding-left: 24rpx;
}

.field-input,
.picker-value,
.field-textarea {
  width: 100%;
  box-sizing: border-box;
  border-radius: 22rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.92) 0%, rgba(247, 250, 255, 0.82) 100%);
  border: 1rpx solid rgba(226, 232, 240, 0.68);
  color: #0f172a;
  box-shadow:
    inset 0 1rpx 0 rgba(255, 255, 255, 0.78),
    0 8rpx 18rpx rgba(15, 23, 42, 0.03);
  transition: box-shadow 0.22s ease, border-color 0.22s ease, background 0.22s ease;
}

.field-input,
.picker-value {
  min-height: 88rpx;
  font-size: var(--ui-type-emphasis);
}

.field-input {
  display: block;
  padding: 18rpx 24rpx;
  line-height: 52rpx;
}

.picker-value {
  display: flex;
  align-items: center;
  padding: 0 24rpx;
}

.field-textarea {
  min-height: 160rpx;
  padding: 22rpx 24rpx;
  font-size: var(--ui-type-emphasis);
}

.field-input:focus,
.field-textarea:focus {
  border-color: rgba(96, 165, 250, 0.92);
  box-shadow:
    0 14rpx 28rpx rgba(37, 99, 235, 0.10),
    0 0 0 4rpx rgba(191, 219, 254, 0.55),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.9);
}

.picker-value:active {
  transform: translateY(1rpx) scale(0.992);
  border-color: rgba(96, 165, 250, 0.92);
  box-shadow:
    0 12rpx 24rpx rgba(37, 99, 235, 0.10),
    0 0 0 4rpx rgba(191, 219, 254, 0.48),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.9);
}

.question-textarea {
  min-height: 120rpx;
  white-space: pre-wrap;
}

.button-row {
  display: flex;
  flex-wrap: wrap;
  gap: 14rpx;
  margin: 18rpx 0;
}

.compact-row {
  margin-top: 10rpx;
}

.primary-btn,
.secondary-btn {
  flex: 1 1 calc(50% - 7rpx);
  min-width: calc(50% - 7rpx);
  max-width: 100%;
  min-height: 76rpx;
  height: auto;
  padding: 14rpx 20rpx;
  border-radius: 20rpx;
  line-height: 1.35;
  font-size: var(--ui-type-control);
  font-weight: 700;
  white-space: normal;
  word-break: normal;
  overflow-wrap: anywhere;
  transition: box-shadow 0.24s ease, opacity 0.18s ease, filter 0.24s ease;
}

.primary-btn {
  color: #ffffff;
  background: linear-gradient(135deg, #2563eb 0%, #60a5fa 100%);
  box-shadow: 0 16rpx 32rpx rgba(37, 99, 235, 0.18);
}

.secondary-btn {
  color: #2563eb;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.92) 0%, rgba(235, 243, 255, 0.86) 100%);
  border: 1rpx solid rgba(191, 219, 254, 0.8);
  box-shadow: 0 12rpx 24rpx rgba(37, 99, 235, 0.08);
}

.primary-btn:active,
.secondary-btn:active {
  transform: translateY(2rpx) scale(0.986);
  filter: saturate(1.03);
}

.edit-box,
.list-card {
  margin-top: 18rpx;
  padding: 20rpx;
  border-radius: 26rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.72) 0%, rgba(247, 250, 255, 0.62) 100%);
  border: 1rpx solid rgba(255, 255, 255, 0.62);
  box-shadow:
    inset 0 1rpx 0 rgba(255, 255, 255, 0.78),
    0 12rpx 26rpx rgba(15, 23, 42, 0.04);
  transition: transform 0.22s ease, box-shadow 0.28s ease, border-color 0.24s ease, background 0.24s ease;
  animation: glassFadeUp 0.48s ease both;
}

.nested-card {
  margin-top: 10rpx;
}

/* The condition editor is a centered sub-panel inside the step dialog.
   The page-level field utility adds left padding for full-width forms; that
   padding must not be accumulated again inside this nested panel. */
.audit-step-condition-editor {
  width: calc(100% - 32rpx);
  margin-left: auto !important;
  margin-right: auto !important;
  box-sizing: border-box;
  text-align: left;
}

.audit-designation-options {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 12rpx;
  width: 100%;
}

.audit-designation-option {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 68rpx;
  padding: 8rpx 18rpx;
  box-sizing: border-box;
  border: 1rpx solid rgba(94, 139, 205, 0.22);
  border-radius: 14rpx;
  color: #52647d;
  background: rgba(247, 250, 255, 0.82);
  font-size: var(--ui-type-control);
  line-height: 1.35;
}

.audit-designation-option-active {
  color: #1769d2;
  border-color: rgba(57, 129, 232, 0.46);
  background: rgba(230, 241, 255, 0.92);
  box-shadow: inset 0 0 0 1rpx rgba(255, 255, 255, 0.72);
}
.audit-step-condition-editor .field,
.audit-step-condition-editor .button-row,
.audit-step-condition-editor .list-title {
  width: 100%;
  margin-left: auto;
  margin-right: auto;
  box-sizing: border-box;
}
.audit-step-condition-editor .field-label,
.audit-step-condition-editor .field-value {
  padding-left: 0;
}
.audit-step-condition-editor .field-input,
.audit-step-condition-editor .picker-value,
.audit-step-condition-editor .field-textarea {
  margin-left: auto;
  margin-right: auto;
}

.rule-panel {
  margin-top: 18rpx;
  padding: 24rpx;
  border-radius: 24rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.86) 0%, rgba(248, 251, 255, 0.74) 100%);
  border: 1rpx solid rgba(219, 234, 254, 0.72);
  box-shadow:
    inset 0 1rpx 0 rgba(255, 255, 255, 0.82),
    0 14rpx 30rpx rgba(15, 23, 42, 0.05);
  box-sizing: border-box;
}

.rule-panel:first-child {
  margin-top: 0;
}

.rule-panel-head {
  margin-bottom: 18rpx;
}

.rule-panel-head-row {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 18rpx;
}

.rule-panel-title {
  position: relative;
  padding-left: 18rpx;
  color: #0f172a;
  font-size: var(--ui-type-section);
  font-weight: 800;
  line-height: 1.35;
}

.rule-panel-title::before {
  content: "";
  position: absolute;
  left: 0;
  top: 50%;
  width: 8rpx;
  height: 28rpx;
  border-radius: 999rpx;
  transform: translateY(-50%);
  background: linear-gradient(180deg, #2563eb 0%, #60a5fa 100%);
  box-shadow: 0 0 14rpx rgba(59, 130, 246, 0.24);
}

.rule-panel-subtitle {
  margin-top: 6rpx;
  color: #64748b;
  font-size: var(--ui-type-meta);
  line-height: 1.6;
}

.mini-action-btn {
  flex-shrink: 0;
  min-width: 190rpx;
  padding-left: 18rpx;
  padding-right: 18rpx;
}

.rule-switch-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18rpx;
  margin-top: 14rpx;
  padding: 18rpx;
  border-radius: 18rpx;
  background: rgba(248, 250, 252, 0.82);
  border: 1rpx solid rgba(226, 232, 240, 0.86);
}

.rule-empty {
  padding: 24rpx 20rpx;
  border-radius: 20rpx;
  color: #64748b;
  font-size: var(--ui-type-control);
  text-align: center;
  background: rgba(248, 250, 252, 0.74);
  border: 1rpx dashed rgba(148, 163, 184, 0.56);
}

.small-empty {
  margin-top: 14rpx;
  padding: 16rpx;
  font-size: var(--ui-type-meta);
}

.rule-clause-card,
.template-config-card {
  margin-bottom: 16rpx;
  padding: 20rpx;
  border-radius: 22rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.96) 0%, rgba(249, 251, 255, 0.88) 100%);
  border: 1rpx solid rgba(226, 237, 247, 0.96);
  box-shadow:
    0 12rpx 26rpx rgba(15, 23, 42, 0.045),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.84);
  box-sizing: border-box;
}

.rule-clause-card:last-child,
.template-config-card:last-child {
  margin-bottom: 0;
}

.rule-clause-title {
  color: #0f172a;
  font-size: var(--ui-type-value);
  font-weight: 800;
  line-height: 1.45;
}

.rule-tag-row,
.rule-template-tags,
.template-config-meta,
.rule-list-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
}

.rule-tag-row {
  margin-top: 12rpx;
}

.rule-tag {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 38rpx;
  padding: 6rpx 14rpx;
  border-radius: 999rpx;
  font-size: var(--ui-type-caption);
  font-weight: 700;
  line-height: 1.35;
  border: 1rpx solid transparent;
}

.tag-scope {
  color: #2563eb;
  background: rgba(219, 234, 254, 0.76);
  border-color: rgba(147, 197, 253, 0.64);
}

.tag-target {
  color: #0369a1;
  background: rgba(224, 242, 254, 0.78);
  border-color: rgba(56, 189, 248, 0.56);
}

.tag-required {
  color: #c2410c;
  background: rgba(255, 237, 213, 0.82);
  border-color: rgba(253, 186, 116, 0.58);
}

.tag-free {
  color: #15803d;
  background: rgba(209, 250, 229, 0.78);
  border-color: rgba(110, 231, 183, 0.58);
}

.tag-weight {
  color: #15803d;
  background: rgba(220, 252, 231, 0.78);
  border-color: rgba(134, 239, 172, 0.56);
}

.tag-order {
  color: #15803d;
  background: rgba(209, 250, 229, 0.78);
  border-color: rgba(110, 231, 183, 0.58);
}

.tag-method {
  color: #15803d;
  background: rgba(220, 252, 231, 0.78);
  border-color: rgba(134, 239, 172, 0.56);
}

.rule-template-list {
  display: grid;
  gap: 12rpx;
  margin-top: 16rpx;
}

.rule-template-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
  width: 100%;
  padding: 16rpx 18rpx;
  border-radius: 18rpx;
  background: rgba(248, 250, 252, 0.86);
  border: 1rpx solid rgba(226, 232, 240, 0.84);
  box-sizing: border-box;
}

.rule-template-name,
.template-config-name {
  flex: 1;
  min-width: 0;
  color: #0f172a;
  font-size: var(--ui-type-body);
  font-weight: 700;
  line-height: 1.45;
  word-break: break-word;
}

.rule-card-actions,
.template-config-actions {
  width: 100%;
  justify-content: flex-start;
  margin-top: 16rpx;
  gap: 20rpx;
}

.rule-config-list {
  margin-top: 18rpx;
}

.rule-config-list .list-title {
  padding-left: 0;
  margin-bottom: 14rpx;
  color: #475569;
  font-size: var(--ui-type-body);
  font-weight: 800;
}

.rule-config-list .list-title::before {
  display: none;
}

.template-config-card {
  display: flex;
  flex-direction: column;
  gap: 14rpx;
}

.template-config-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10rpx;
}

.template-config-card > .template-config-name {
  flex: none;
  width: 100%;
  min-width: auto;
  color: #0f172a;
  font-size: var(--ui-type-emphasis);
  font-weight: 700;
  line-height: 1.5;
  word-break: break-word;
}

.template-config-sort-btns {
  display: flex;
  gap: 6rpx;
  flex-shrink: 0;
}

.sort-arrow {
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 12rpx;
  background: rgba(241, 245, 249, 0.7);
  color: #64748b;
  font-size: var(--ui-type-value);
  font-weight: 600;
}

.sort-arrow-disabled {
  opacity: 0.3;
  pointer-events: none;
}

.template-config-inline-edit {
  margin-bottom: 16rpx;
  padding: 20rpx;
  border-radius: 16rpx;
  background: rgba(248, 250, 252, 0.9);
  border: 1rpx solid rgba(226, 232, 240, 0.7);
  display: flex;
  flex-direction: column;
  gap: 14rpx;
}

.template-config-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
}

.template-config-meta {
  flex-shrink: 0;
  justify-content: flex-end;
}

.template-config-actions {
  width: auto;
  flex-shrink: 0;
  margin-top: 0;
  justify-content: flex-start;
  gap: 14rpx;
}

.rule-list-card {
  margin-top: 18rpx;
}

.rule-list-item {
  align-items: stretch;
  padding: 20rpx;
  border-color: rgba(191, 219, 254, 0.42);
  background:
    linear-gradient(135deg, rgba(255, 255, 255, 0.94) 0%, rgba(248, 251, 255, 0.88) 100%);
}

.rule-list-toolbar {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 16rpx;
  margin-bottom: 16rpx;
}

.rule-list-count {
  flex-shrink: 0;
  min-height: 42rpx;
  padding: 8rpx 16rpx;
  border-radius: 999rpx;
  color: #2563eb;
  font-size: var(--ui-type-caption);
  font-weight: 800;
  background: rgba(219, 234, 254, 0.68);
  border: 1rpx solid rgba(147, 197, 253, 0.58);
}

.rule-filter-row {
  display: flex;
  gap: 16rpx;
  margin-bottom: 6rpx;
}

.rule-filter-field {
  flex: 1;
  min-width: 0;
}

.compact-picker-value {
  min-height: 70rpx;
  padding: 0 18rpx;
  font-size: var(--ui-type-label);
  border-radius: 18rpx;
}

.rule-list-action-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
  margin-bottom: 16rpx;
}

.rule-list-inline-actions {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 22rpx;
  min-width: 150rpx;
}

.rule-list-info {
  display: grid;
  gap: 10rpx;
}

.rule-list-main-line {
  display: flex;
  align-items: center;
  min-width: 0;
}

.rule-info-text {
  min-width: 0;
  color: #0f172a;
  font-size: var(--ui-type-value);
  font-weight: 800;
  line-height: 1.45;
  word-break: break-word;
}

.inner-scroll {
  padding-right: 8rpx;
}

.medium-scroll {
  max-height: 620rpx;
}

.large-scroll {
  max-height: 820rpx;
}

.small-scroll {
  max-height: 700rpx;
}

.small-scroll.template-config-scroll-expanded {
  max-height: none;
}

.scroll-hint {
  display: flex;
  justify-content: center;
  padding: 6rpx 0 8rpx;
  opacity: 0.3;
  pointer-events: none;
}

.scroll-hint-dot {
  width: 56rpx;
  height: 4rpx;
  border-radius: 4rpx;
  background: #64748b;
}

.list-item,
.question-card,
.pill {
  margin-bottom: 14rpx;
  padding: 20rpx 18rpx;
  border-radius: 22rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.92) 0%, rgba(249, 251, 255, 0.82) 100%);
  border: 1rpx solid rgba(226, 237, 247, 0.96);
  box-shadow:
    0 12rpx 24rpx rgba(15, 23, 42, 0.04),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.78);
  box-sizing: border-box;
  transition: transform 0.18s ease, box-shadow 0.24s ease, border-color 0.24s ease;
}

.list-item:last-child,
.question-card:last-child,
.pill:last-child {
  margin-bottom: 0;
}

.dragging-ghost {
  opacity: 0.4;
  transform: scale(0.96);
  box-shadow: none;
}

.drag-insert-target {
  box-shadow: inset 0 4rpx 0 0 #3b82f6, 0 8rpx 20rpx rgba(59, 130, 246, 0.12);
}

.drag-ghost-card {
  position: fixed;
  z-index: 1000;
  padding: 20rpx 18rpx;
  border-radius: 22rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.72) 0%, rgba(249, 251, 255, 0.58) 100%);
  border: 1rpx solid rgba(226, 237, 247, 0.50);
  box-shadow: 0 16rpx 32rpx rgba(15, 23, 42, 0.10);
  box-sizing: border-box;
  pointer-events: none;
  display: flex;
  flex-direction: column;
  gap: 14rpx;
}

.ghost-preview-text {
  font-size: var(--ui-type-emphasis);
  color: #334155;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  flex: 1;
  min-width: 0;
}

.drag-handle {
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 12rpx;
  color: #94a3b8;
  font-size: var(--ui-type-section);
  font-weight: 700;
  flex-shrink: 0;
}

.drag-insert-line-end {
  height: 4rpx;
  background: #3b82f6;
  border-radius: 2rpx;
  margin: 8rpx 0;
}

.question-header {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.question-title {
  display: flex;
  align-items: center;
  gap: 6rpx;
  flex: 1;
  min-width: 0;
}

.expand-indicator {
  font-size: var(--ui-type-caption);
  color: #94a3b8;
  flex-shrink: 0;
}

.question-preview {
  margin-top: 14rpx;
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}

.preview-text {
  font-size: var(--ui-type-emphasis);
  color: #0f172a;
  font-weight: 700;
  line-height: 1.5;
  word-break: break-word;
}

.preview-chips {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
}

.preview-chip {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 38rpx;
  padding: 6rpx 14rpx;
  border-radius: 999rpx;
  font-size: var(--ui-type-caption);
  font-weight: 700;
  line-height: 1.35;
  border: 1rpx solid transparent;
}

.chip-min { background: rgba(219, 234, 254, 0.76); color: #2563eb; border-color: rgba(147, 197, 253, 0.64); }
.chip-start { background: rgba(209, 250, 229, 0.78); color: #15803d; border-color: rgba(110, 231, 183, 0.58); }
.chip-max { background: rgba(255, 237, 213, 0.78); color: #c2410c; border-color: rgba(253, 186, 116, 0.58); }
.chip-step { background: rgba(224,242,254,0.76); color: #0369a1; border-color: rgba(56,189,248,0.56); }

.question-fields {
  margin-top: 8rpx;
  animation: fadeInDown 0.18s ease;
}

@keyframes fadeInDown {
  from { opacity: 0; transform: translateY(-6rpx); }
  to { opacity: 1; transform: translateY(0); }
}

.question-order-btns {
  display: flex;
  gap: 8rpx;
}

.order-btn {
  width: 52rpx;
  height: 52rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 14rpx;
  background: rgba(239, 246, 255, 0.92);
  color: #3b82f6;
  font-size: var(--ui-type-emphasis);
  font-weight: 700;
  border: 1rpx solid rgba(191, 219, 254, 0.72);
}

.order-btn:active {
  background: rgba(59, 130, 246, 0.18);
}

.order-btn-disabled {
  opacity: 0.3;
  pointer-events: none;
}

.order-btn-delete {
  background: #ef4444;
  border-color: #ef4444;
  position: relative;
}

/* trash can lid */
.order-btn-delete::before {
  content: '';
  position: absolute;
  top: 13rpx;
  left: 50%;
  transform: translateX(-50%);
  width: 30rpx;
  height: 5rpx;
  background: white;
  border-radius: 2rpx;
}

/* trash can body */
.order-btn-delete::after {
  content: '';
  position: absolute;
  top: 19rpx;
  left: 50%;
  transform: translateX(-50%);
  width: 22rpx;
  height: 21rpx;
  background: white;
  border-radius: 2rpx 2rpx 4rpx 4rpx;
}

.order-btn-delete:active {
  background: #dc2626;
  border-color: #dc2626;
}

.field-error {
  border-color: #ef4444 !important;
  box-shadow: 0 0 0 2rpx rgba(239, 68, 68, 0.12) !important;
}

.list-item:active,
.question-card:active,
.pill:active {
  transform: translateY(1rpx) scale(0.992);
  box-shadow:
    0 16rpx 28rpx rgba(37, 99, 235, 0.08),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.82);
}

.list-item {
  display: flex;
  justify-content: space-between;
  gap: 16rpx;
}

.list-item-stacked {
  display: block;
}

.list-actions-below {
  margin-top: 16rpx;
  justify-content: flex-start;
}

.list-main {
  flex: 1;
  min-width: 0;
}

.list-name,
.question-title {
  color: #0f172a;
  font-size: var(--ui-type-value);
  font-weight: 700;
  line-height: 1.45;
  margin-bottom: 10rpx;
}

.list-desc {
  color: #64748b;
  font-size: var(--ui-type-meta);
  line-height: 1.72;
  white-space: pre-wrap;
  word-break: break-word;
}

.chip-row,
.pill-list,
.pill-actions,
.inline-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
}

.pill-list {
  width: 100%;
}

.pill,
.inline-actions {
  align-items: center;
  justify-content: space-between;
}

.clause-pill {
  width: 100%;
  align-items: flex-start;
  flex-direction: column;
  gap: 12rpx;
  padding: 18rpx 18rpx 16rpx;
}

.clause-pill-main {
  flex: 1;
  min-width: 0;
}

.clause-pill-title {
  color: #0f172a;
  font-size: var(--ui-type-control);
  font-weight: 700;
  line-height: 1.5;
  word-break: break-word;
}

.clause-pill-meta {
  display: inline-flex;
  align-items: center;
  margin-top: 6rpx;
  padding: 6rpx 14rpx;
  border-radius: 999rpx;
  background: linear-gradient(135deg, rgba(239, 246, 255, 0.98) 0%, rgba(219, 234, 254, 0.90) 100%);
  border: 1rpx solid rgba(147, 197, 253, 0.72);
  color: #2563eb;
  font-size: var(--ui-type-caption);
  font-weight: 600;
  line-height: 1.4;
}

.clause-pill .pill-actions {
  width: 100%;
  margin-top: 2rpx;
  justify-content: flex-start;
  gap: 18rpx;
}

.inline-actions {
  align-content: flex-start;
  justify-content: flex-end;
  min-width: 140rpx;
}

.status-chip,
.invite-chip,
.select-chip {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 8rpx 16rpx;
  border-radius: 999rpx;
  font-size: var(--ui-type-caption);
  font-weight: 700;
}

.current-chip,
.select-chip {
  background: linear-gradient(135deg, rgba(239, 246, 255, 0.98) 0%, rgba(219, 234, 254, 0.90) 100%);
  color: #2563eb;
  border: 1rpx solid rgba(147, 197, 253, 0.8);
}

.select-chip-active {
  background: linear-gradient(135deg, rgba(220, 252, 231, 0.98) 0%, rgba(187, 247, 208, 0.9) 100%);
  color: #15803d;
  border-color: rgba(134, 239, 172, 0.8);
}

.invite-chip {
  margin-top: 10rpx;
  background: linear-gradient(135deg, rgba(255, 247, 237, 0.96) 0%, rgba(254, 215, 170, 0.82) 100%);
  color: #c2410c;
}

.csv-tip,
.invite-banner,
.empty-desc {
  color: #64748b;
  font-size: var(--ui-type-control);
  line-height: 1.7;
}

.invite-banner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12rpx;
  flex-wrap: wrap;
  margin-top: 8rpx;
  padding: 16rpx 18rpx;
  border-radius: 20rpx;
  background: linear-gradient(135deg, rgba(239, 246, 255, 0.96) 0%, rgba(219, 234, 254, 0.9) 100%);
}

.empty-title {
  color: #0f172a;
  font-size: var(--ui-type-dialog);
  font-weight: 700;
  margin-bottom: 12rpx;
}

.stats-grid {
  display: grid;
  gap: 18rpx;
  margin-top: 14rpx;
  margin-bottom: 22rpx;
}

.stats-grid-summary {
  grid-template-columns: repeat(2, minmax(0, 1fr));
  margin-top: 0;
  margin-bottom: 0;
}

.stats-grid-completion {
  grid-template-columns: minmax(0, 1fr);
}

.stats-panel {
  margin-top: 12rpx;
  margin-bottom: 22rpx;
  padding: 16rpx;
  border-radius: 28rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.72) 0%, rgba(239, 246, 255, 0.66) 100%);
  border: 1rpx solid rgba(219, 234, 254, 0.8);
  box-shadow: inset 0 1rpx 0 rgba(255, 255, 255, 0.72);
  transition: transform 0.22s ease, box-shadow 0.28s ease, border-color 0.24s ease, background 0.24s ease;
}

.stats-panel:active {
  transform: translateY(1rpx) scale(0.996);
  box-shadow:
    inset 0 1rpx 0 rgba(255, 255, 255, 0.78),
    0 14rpx 30rpx rgba(37, 99, 235, 0.06);
}

.stat-card {
  padding: 18rpx 18rpx;
  border-radius: 24rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.88) 0%, rgba(240, 247, 255, 0.76) 100%);
  border: 1rpx solid rgba(219, 234, 254, 0.9);
  box-shadow: 0 12rpx 28rpx rgba(15, 23, 42, 0.04);
  transition: transform 0.2s ease, box-shadow 0.26s ease, border-color 0.24s ease, background 0.24s ease;
}

.stat-card-clickable {
  transition: transform 0.18s ease, box-shadow 0.24s ease, border-color 0.24s ease;
}

.stat-card-clickable:active {
  transform: translateY(1rpx) scale(0.992);
  box-shadow:
    0 14rpx 30rpx rgba(37, 99, 235, 0.12),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.88);
  border-color: rgba(96, 165, 250, 0.65);
}

.stat-card-selected {
  border-color: rgba(37, 99, 235, 0.55);
  background: linear-gradient(135deg, rgba(219, 234, 254, 0.65) 0%, rgba(239, 246, 255, 0.55) 100%);
  box-shadow:
    0 14rpx 30rpx rgba(37, 99, 235, 0.12),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.88);
}

.stat-card-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12rpx;
}

.stat-group-name {
  flex: 1;
  min-width: 0;
  color: #0f172a;
  font-size: var(--ui-type-emphasis);
  font-weight: 800;
  line-height: 1.4;
  letter-spacing: 0.2rpx;
}

.stat-label {
  color: #64748b;
  font-size: var(--ui-type-meta);
  line-height: 1.5;
}

.stat-value {
  margin-top: 8rpx;
  color: #0f172a;
  font-size: var(--ui-type-dialog);
  font-weight: 700;
}

.stat-value-large {
  margin-top: 0;
  font-size: var(--ui-type-dialog);
  font-weight: 800;
  color: #0f172a;
}

.result-current {
  color: #0f172a;
  font-size: var(--ui-type-value);
  font-weight: 700;
  line-height: 1.6;
}

.empty-inline {
  padding: 24rpx 8rpx;
  color: #64748b;
  font-size: var(--ui-type-control);
  line-height: 1.7;
}

.completion-intro {
  margin-top: 8rpx;
}

.result-table {
  border-radius: 24rpx;
  overflow: hidden;
  background: rgba(255, 255, 255, 0.78);
  border: 1rpx solid rgba(219, 234, 254, 0.95);
}

.result-table-head,
.result-table-row {
  display: flex;
  align-items: stretch;
}

.result-table-head {
  background: linear-gradient(135deg, rgba(239, 246, 255, 0.96) 0%, rgba(219, 234, 254, 0.88) 100%);
}

.result-table-row {
  border-top: 1rpx solid rgba(226, 232, 240, 0.75);
  transition: transform 0.18s ease, box-shadow 0.24s ease, background 0.24s ease;
}

.result-table-row:active {
  transform: translateY(1rpx) scale(0.992);
  background: linear-gradient(135deg, rgba(239, 246, 255, 0.78) 0%, rgba(248, 251, 255, 0.9) 100%);
  box-shadow: inset 0 1rpx 0 rgba(255, 255, 255, 0.82);
}

.result-col {
  padding: 18rpx 14rpx;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
  color: #0f172a;
  font-size: var(--ui-type-control);
  line-height: 1.55;
}

.result-col-name {
  flex: 1.45;
  min-width: 0;
}

.result-col-score {
  width: 150rpx;
}

.result-col-count {
  width: 100rpx;
}

.result-name {
  max-width: 100%;
  color: #0f172a;
  font-size: var(--ui-type-emphasis);
  font-weight: 700;
  line-height: 1.45;
  word-break: break-word;
  text-align: center;
}

.result-meta {
  margin-top: 4rpx;
  color: #64748b;
  font-size: var(--ui-type-caption);
  line-height: 1.45;
  text-align: center;
  word-break: break-word;
}
.result-col-progress {
  flex: 1.15;
  min-width: 180rpx;
}

.progress-cell {
  width: 100%;
}

.progress-top {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12rpx;
  margin-bottom: 10rpx;
}

.progress-count {
  color: #0f172a;
  font-size: var(--ui-type-caption);
  font-weight: 700;
}

.progress-percent {
  color: #64748b;
  font-size: var(--ui-type-micro);
  font-weight: 700;
}

.progress-track {
  width: 100%;
  height: 14rpx;
  overflow: hidden;
  border-radius: 999rpx;
  background: rgba(226, 232, 240, 0.9);
  box-shadow: inset 0 1rpx 3rpx rgba(15, 23, 42, 0.12);
}

.progress-fill {
  height: 100%;
  border-radius: 999rpx;
  box-shadow: 0 0 18rpx rgba(96, 165, 250, 0.16);
  transition: width 0.25s ease, box-shadow 0.24s ease, opacity 0.24s ease;
}

.completion-progress {
  margin-top: 10rpx;
}

.stat-card-tip {
  color: #2563eb;
  font-size: var(--ui-type-micro);
  font-weight: 600;
  line-height: 1;
  padding: 6rpx 12rpx;
  border-radius: 999rpx;
  background: rgba(37, 99, 235, 0.06);
}

.stat-card-tip-wrap {
  margin-top: 12rpx;
  display: flex;
  justify-content: center;
}

.popup-mask {
  position: fixed;
  inset: 0;
  z-index: 99;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 40rpx 28rpx;
  background: rgba(15, 23, 42, 0.30);
  box-sizing: border-box;
}

.popup-card {
  width: 100%;
  max-height: 74vh;
  padding: 24rpx;
  border-radius: 30rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.94) 0%, rgba(248, 251, 255, 0.92) 100%);
  border: 1rpx solid rgba(255, 255, 255, 0.72);
  box-shadow: 0 24rpx 56rpx rgba(15, 23, 42, 0.14);
  backdrop-filter: blur(24rpx);
  -webkit-backdrop-filter: blur(24rpx);
  box-sizing: border-box;
  transition: transform 0.22s ease, box-shadow 0.26s ease, border-color 0.24s ease;
}

.completion-popup-card {
  max-width: 960rpx;
}

.result-table-row-clickable {
  border-color: rgba(96, 165, 250, 0.36);
  transition: background 0.15s ease, border-color 0.15s ease;
}

.result-table-row-clickable:active {
  background: rgba(59, 130, 246, 0.06);
  border-color: rgba(59, 130, 246, 0.52);
}

.popup-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
  margin-bottom: 18rpx;
}

.popup-title {
  color: #0f172a;
  font-size: var(--ui-type-section);
  font-weight: 700;
}

.popup-close {
  color: #2563eb;
  font-size: var(--ui-type-control);
  font-weight: 700;
  transition: transform 0.18s ease, opacity 0.18s ease, text-shadow 0.22s ease;
}

.popup-close:active {
  transform: translateY(1rpx) scale(0.986);
  opacity: 0.9;
  text-shadow: 0 0 12rpx rgba(37, 99, 235, 0.16);
}

.popup-scroll {
  max-height: 56vh;
}

.result-drill-header {
  margin-bottom: 12rpx;
}

.result-drill-header .list-title {
  margin-bottom: 0;
  flex: 1;
  min-width: 0;
  font-size: var(--ui-type-value);
  line-height: 1.45;
}

.result-drill-header .popup-close {
  flex-shrink: 0;
}

.scorer-result-card {
  position: relative;
  padding-top: 24rpx;
  padding-right: 22rpx;
}

.scorer-result-card-clickable {
  border-color: rgba(96, 165, 250, 0.52);
}

.scorer-card-title {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-bottom: 10rpx;
}

.scorer-name {
  margin-bottom: 0;
}

.scorer-inline-status {
  min-width: auto;
  min-height: 42rpx;
  padding: 7rpx 16rpx;
  box-shadow: 0 6rpx 14rpx rgba(15, 23, 42, 0.06);
}

.scorer-meta-lines {
  margin-top: 10rpx;
}

.submitted-text {
  margin-top: 8rpx;
}

.score-tag {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 42rpx;
  padding: 7rpx 16rpx;
  border-radius: 999rpx;
  font-size: var(--ui-type-caption);
  font-weight: 800;
  line-height: 1.2;
  border: 1rpx solid transparent;
  box-sizing: border-box;
}

.score-tag-completed,
.score-tag-total {
  color: #075985;
  background: linear-gradient(135deg, rgba(224, 242, 254, 0.98) 0%, rgba(186, 230, 253, 0.90) 100%);
  border-color: rgba(56, 189, 248, 0.42);
}

.score-tag-pending {
  color: #c2410c;
  background: linear-gradient(135deg, rgba(255, 247, 237, 0.98) 0%, rgba(254, 215, 170, 0.92) 100%);
  border-color: rgba(253, 186, 116, 0.55);
}

.score-tag-inactive {
  color: #0369a1;
  background: linear-gradient(135deg, rgba(240, 249, 255, 0.98) 0%, rgba(224, 242, 254, 0.90) 100%);
  border-color: rgba(56, 189, 248, 0.45);
}

.score-tag-muted {
  color: #64748b;
  background: linear-gradient(135deg, rgba(241, 245, 249, 0.98) 0%, rgba(226, 232, 240, 0.90) 100%);
  border-color: rgba(148, 163, 184, 0.42);
}

.score-tag-template {
  color: #15803d;
  background: linear-gradient(135deg, rgba(240, 253, 244, 0.98) 0%, rgba(187, 247, 208, 0.82) 100%);
  border-color: rgba(74, 222, 128, 0.42);
}

.score-tag-weight {
  color: #2563eb;
  background: linear-gradient(135deg, rgba(239, 246, 255, 0.98) 0%, rgba(219, 234, 254, 0.86) 100%);
  border-color: rgba(147, 197, 253, 0.48);
}

.score-tag-weighted,
.score-tag-question {
  color: #9a3412;
  background: linear-gradient(135deg, rgba(255, 247, 237, 0.98) 0%, rgba(254, 215, 170, 0.82) 100%);
  border-color: rgba(251, 146, 60, 0.44);
}

.score-tag-template,
.score-tag-weight,
.score-tag-weighted {
  min-height: 52rpx;
  padding: 9rpx 18rpx;
  font-size: var(--ui-type-body);
  line-height: 1.25;
  box-shadow: 0 8rpx 18rpx rgba(15, 23, 42, 0.055);
}

.detail-summary-row,
.detail-tag-row,
.detail-question-score {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
  margin-top: 10rpx;
}

.detail-summary-card {
  position: relative;
  min-height: 104rpx;
}

.detail-action-row {
  margin-top: 14rpx;
  display: flex;
  justify-content: flex-end;
}

.compact-action-btn {
  min-width: 168rpx;
  padding: 0 22rpx;
  font-size: var(--ui-type-label);
  line-height: 56rpx;
}

.secondary-btn.revoke-score-btn {
  min-width: 188rpx;
  min-height: 64rpx;
  height: auto;
  line-height: 1.2;
  padding: 12rpx 26rpx;
  border-radius: 20rpx;
  color: #b42318;
  font-size: var(--ui-type-meta);
  font-weight: 750;
  background:
    radial-gradient(circle at 22% 0%, rgba(255,255,255,0.90), transparent 42%),
    linear-gradient(135deg, rgba(255,255,255,0.98), rgba(254,242,242,0.88));
  border: 1rpx solid rgba(248,113,113,0.46);
  box-shadow:
    0 10rpx 22rpx rgba(239,68,68,0.08),
    inset 0 1rpx 0 rgba(255,255,255,0.90);
}

.detail-people-row {
  display: flex;
  align-items: stretch;
  gap: 16rpx;
  margin-bottom: 10rpx;
}

.detail-person-block {
  flex: 1;
  min-width: 0;
}

.detail-person-label {
  color: #64748b;
  font-size: var(--ui-type-caption);
  font-weight: 700;
  line-height: 1.35;
}

.detail-person-name {
  margin-top: 6rpx;
  color: #0f172a;
  font-size: var(--ui-type-value);
  font-weight: 800;
  line-height: 1.35;
  word-break: break-word;
}

.detail-person-divider {
  width: 1rpx;
  min-height: 58rpx;
  background: linear-gradient(180deg, rgba(191, 219, 254, 0) 0%, rgba(147, 197, 253, 0.72) 50%, rgba(191, 219, 254, 0) 100%);
}

.detail-question {
  margin-top: 14rpx;
  padding-top: 14rpx;
  border-top: 1rpx solid rgba(226, 232, 240, 0.78);
}

.compact-question {
  position: relative;
  padding: 16rpx 0 2rpx;
}

.compact-question-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 18rpx;
}

.detail-question-title {
  flex: 1;
  min-width: 0;
  font-size: var(--ui-type-control);
  margin-bottom: 6rpx;
  padding-right: 8rpx;
}

.question-score-corner {
  flex: 0 0 auto;
  min-width: 104rpx;
  min-height: 46rpx;
  padding: 7rpx 14rpx;
  border-radius: 16rpx;
  color: #2563eb;
  font-size: var(--ui-type-control);
  font-weight: 900;
  line-height: 1.2;
  text-align: center;
  background: linear-gradient(135deg, rgba(239, 246, 255, 0.96) 0%, rgba(219, 234, 254, 0.86) 100%);
  border: 1rpx solid rgba(147, 197, 253, 0.62);
  box-shadow:
    0 8rpx 18rpx rgba(37, 99, 235, 0.08),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.86);
  box-sizing: border-box;
}

.score-label-toggle {
  display: inline-flex;
  align-items: center;
  margin-top: 6rpx;
  padding: 5rpx 12rpx;
  border-radius: 999rpx;
  color: #2563eb;
  background: rgba(219, 234, 254, 0.58);
  font-size: var(--ui-type-caption);
  font-weight: 700;
}

.score-label-box {
  margin-top: 10rpx;
  padding: 14rpx 16rpx;
  border-radius: 16rpx;
  color: #475569;
  background: rgba(248, 250, 252, 0.86);
  border: 1rpx solid rgba(226, 232, 240, 0.88);
  font-size: var(--ui-type-meta);
  line-height: 1.7;
  white-space: pre-wrap;
}

.status-muted {
  color: #64748b;
  background: rgba(148, 163, 184, 0.16);
}

.status-inactive {
  color: #0369a1;
  background: rgba(224, 242, 254, 0.55);
}

.result-col-popup-name {
  flex: 1.35;
  min-width: 0;
}

.result-col-popup-detail {
  flex: 1.35;
  min-width: 0;
}

.result-col-target-name {
  flex: 0.9;
  min-width: 0;
}

.result-col-target-detail {
  flex: 1.8;
  min-width: 0;
}

.result-col-popup-progress {
  flex: 0.85;
  min-width: 160rpx;
}

.result-col-popup-status {
  width: 100rpx;
}

.result-meta-strong {
  color: #334155;
  font-weight: 600;
}

.status-chip {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: 48rpx;
  padding: 4rpx 8rpx;
  border-radius: 999rpx;
  font-size: var(--ui-type-micro);
  font-weight: 700;
  line-height: 1.1;
}

.status-pending {
  color: #c2410c;
  background: rgba(253, 186, 116, 0.22);
}

.status-completed {
  color: #15803d;
  background: rgba(110, 231, 183, 0.20);
}

/* ── Shared utility classes ── */
.danger-link { color: #dc2626; }
.list-actions { display: flex; flex-shrink: 0; align-items: center; gap: 8rpx; }
.popup-body { padding: 0 4rpx; }

@media (max-width: 360px) {
  .hero-admin {
    padding: 34rpx 28rpx 30rpx;
  }

}

.filter-loading-bar {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-top: 16rpx;
  padding: 14rpx 18rpx;
  border-radius: 18rpx;
  background: rgba(239, 246, 255, 0.82);
  border: 1rpx solid rgba(147, 197, 253, 0.42);
}

.filter-loading-spinner {
  width: 28rpx;
  height: 28rpx;
  border: 4rpx solid rgba(37, 99, 235, 0.18);
  border-top-color: #2563eb;
  border-radius: 50%;
  animation: adminSpin 0.7s linear infinite;
}

.filter-loading-text {
  color: #2563eb;
  font-size: var(--ui-type-control);
  font-weight: 500;
}

.card-loading-overlay {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 32rpx 0;
}

.popup-loading-overlay {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 48rpx 0;
  gap: 18rpx;
}

.card-loading-spinner {
  width: 40rpx;
  height: 40rpx;
  border: 5rpx solid rgba(37, 99, 235, 0.16);
  border-top-color: #2563eb;
  border-radius: 50%;
  animation: adminSpin 0.7s linear infinite;
}

@keyframes adminSpin {
  to { transform: rotate(360deg); }
}

/* 人事信息统一页签 */
.toolbar-row {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  align-items: center;
  margin-bottom: 24rpx;
}

.edit-box-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16rpx;
}

.filter-row {
  display: flex;
  gap: 16rpx;
}

.search-input {
  border: 1rpx solid rgba(203, 213, 225, 0.82);
  border-radius: 22rpx;
  padding: 14rpx 22rpx;
  background: linear-gradient(135deg, rgba(255,255,255,0.88), rgba(248,251,255,0.78));
  font-size: var(--ui-type-emphasis);
  box-shadow: inset 0 1rpx 0 rgba(255,255,255,0.82), 0 8rpx 18rpx rgba(15,23,42,0.035);
}

.list-card-header {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  margin-bottom: 16rpx;
}

.hint-text {
  font-size: var(--ui-type-meta);
  color: #64748b;
}

/* 成员卡片 — 统一毛玻璃设计 */
.hr-member-card {
  display: flex;
  flex-direction: column;
  align-items: stretch;
  padding: 24rpx;
  margin-bottom: 14rpx;
  border-radius: 22rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.92) 0%, rgba(249, 251, 255, 0.82) 100%);
  border: 1rpx solid rgba(226, 237, 247, 0.96);
  box-shadow:
    0 12rpx 24rpx rgba(15, 23, 42, 0.04),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.78);
  transition: transform 0.18s ease, box-shadow 0.24s ease, border-color 0.24s ease;
}

.hr-member-card:active {
  transform: translateY(1rpx) scale(0.992);
  border-color: rgba(96, 165, 250, 0.65);
  box-shadow:
    0 16rpx 28rpx rgba(37, 99, 235, 0.08),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.82);
}

.hr-member-main {
  flex: 1;
  min-width: 0;
}

.hr-member-top {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-bottom: 10rpx;
}

.hr-member-person {
  flex: 1 1 220rpx;
  min-width: 0;
}

.hr-member-name {
  font-size: var(--ui-type-section);
  font-weight: 700;
  color: #0f172a;
}

.hr-member-id {
  margin-top: 4rpx;
  color: #64748b;
  font-size: var(--ui-type-meta);
  font-weight: 500;
  line-height: 1.5;
  overflow-wrap: anywhere;
}

.hr-member-brief {
  color: #64748b;
  font-size: var(--ui-type-caption);
  font-weight: 600;
  line-height: 1.5;
}

.hr-member-summary {
  font-size: var(--ui-type-label);
  color: #475569;
  line-height: 1.5;
}

.hr-member-pending {
  font-size: var(--ui-type-label);
  color: #c2410c;
  margin-top: 6rpx;
  font-weight: 500;
}

.hr-member-reject {
  font-size: var(--ui-type-label);
  color: #dc2626;
  margin-top: 6rpx;
  font-weight: 500;
}

.hr-member-actions {
  display: flex;
  justify-content: flex-end;
  flex-wrap: wrap;
  gap: 12rpx;
  width: 100%;
  margin-top: 18rpx;
  padding-top: 16rpx;
  border-top: 1rpx solid rgba(226, 237, 247, 0.92);
}

.hr-member-actions .link {
  min-width: 92rpx;
  box-sizing: border-box;
}

.hr-member-arrow {
  font-size: var(--ui-type-page);
  color: #94a3b8;
  padding-left: 12rpx;
  font-weight: 600;
}

.status-chip {
  font-size: var(--ui-type-caption);
  padding: 6rpx 14rpx;
  border-radius: 999rpx;
  font-weight: 600;
  white-space: nowrap;
  border: 1rpx solid transparent;
  min-height: 40rpx;
  display: inline-flex;
  align-items: center;
}

.current-chip { background: linear-gradient(135deg, rgba(220, 252, 231, 0.96) 0%, rgba(167, 243, 208, 0.88) 100%); color: #15803d; border-color: rgba(134, 239, 172, 0.55); }
.paused-chip { background: linear-gradient(135deg, rgba(254, 226, 226, 0.96) 0%, rgba(254, 202, 202, 0.88) 100%); color: #dc2626; border-color: rgba(252, 165, 165, 0.55); }
.pending-chip { background: linear-gradient(135deg, rgba(255, 247, 237, 0.96) 0%, rgba(254, 215, 170, 0.88) 100%); color: #c2410c; border-color: rgba(253, 186, 116, 0.55); }
.rejected-chip { background: linear-gradient(135deg, rgba(254, 242, 242, 0.96) 0%, rgba(254, 202, 202, 0.88) 100%); color: #dc2626; border-color: rgba(252, 165, 165, 0.45); }
.none-chip { background: linear-gradient(135deg, rgba(241, 245, 249, 0.92) 0%, rgba(226, 232, 240, 0.84) 100%); color: #64748b; border-color: rgba(148, 163, 184, 0.3); }
.unbound-chip { background: linear-gradient(135deg, rgba(254, 242, 242, 0.92) 0%, rgba(254, 226, 226, 0.84) 100%); color: #dc2626; border-color: rgba(252, 165, 165, 0.45); }
.activation-chip { background: linear-gradient(135deg, rgba(240, 249, 255, 0.96) 0%, rgba(224, 242, 254, 0.88) 100%); color: #0369a1; border-color: rgba(56, 189, 248, 0.56); }

/* 成员卡片 chip 容器 */
.hr-member-chips {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  flex-wrap: wrap;
  gap: 10rpx;
  max-width: 100%;
  flex-shrink: 0;
}

/* 详情弹窗 */
.hr-person-editor-shell {
  width: 90vw;
  max-width: 700rpx;
  height: auto;
  max-height: calc(100vh - 48rpx - env(safe-area-inset-bottom));
  display: flex;
  flex-direction: column;
  border-radius: 30rpx;
  overflow: hidden;
}

.detail-body {
  width: 100%;
  height: 60vh;
  max-height: calc(100vh - 250rpx - env(safe-area-inset-bottom));
  padding: 0 8rpx 20rpx;
  box-sizing: border-box;
}

.detail-loading {
  padding: 18rpx 20rpx 28rpx;
}

.detail-loading-line {
  height: 64rpx;
  margin-top: 16rpx;
  border: 1rpx solid rgba(219, 229, 241, 0.78);
  border-radius: 16rpx;
  background: linear-gradient(135deg, rgba(239, 246, 255, 0.88), rgba(255, 255, 255, 0.96));
  box-shadow: inset 0 1rpx 0 rgba(255, 255, 255, 0.84);
}

.detail-loading-line-title {
  width: 42%;
  height: 32rpx;
  margin-top: 0;
}

.detail-section {
  margin-bottom: 24rpx;
  padding: 20rpx;
  border-radius: 20rpx;
  background: linear-gradient(135deg, rgba(248, 250, 252, 0.74) 0%, rgba(241, 245, 249, 0.62) 100%);
  border: 1rpx solid rgba(226, 232, 240, 0.72);
  box-shadow: inset 0 1rpx 0 rgba(255, 255, 255, 0.78);
}

.detail-basic-section {
  border-top: 0;
}

.detail-section-title {
  font-size: var(--ui-type-section);
  font-weight: 700;
  color: #0f172a;
  margin-bottom: 18rpx;
  padding-left: 16rpx;
  border-left: 6rpx solid #2563eb;
  line-height: 1.35;
}

.required-mark {
  color: #ef4444;
  font-weight: 600;
}

.field-hint {
  font-size: var(--ui-type-meta);
  color: #94a3b8;
  margin-top: 6rpx;
  line-height: 1.4;
}

.pending-section {
  background: linear-gradient(135deg, rgba(255, 251, 235, 0.96) 0%, rgba(254, 243, 199, 0.88) 100%);
  border-radius: 20rpx;
  padding: 24rpx;
  border: 1rpx solid rgba(251, 191, 36, 0.35);
  box-shadow: 0 10rpx 22rpx rgba(217, 119, 6, 0.06);
}

.pending-list {
  margin-bottom: 18rpx;
}

.pending-item {
  padding: 10rpx 0;
  font-size: var(--ui-type-value);
  display: flex;
  gap: 8rpx;
}

.pending-label {
  color: #64748b;
  flex-shrink: 0;
}

.pending-value {
  color: #c2410c;
  font-weight: 600;
}

.detail-buttons {
  margin-top: 0;
  padding-top: 18rpx;
  border-top: 1rpx solid rgba(226, 232, 240, 0.8);
}

.danger-btn {
  background: linear-gradient(135deg, #ef4444 0%, #f97316 100%);
  color: #fff;
  border-radius: 20rpx;
  padding: 14rpx 28rpx;
  font-size: var(--ui-type-control);
  font-weight: 700;
  border: none;
  box-shadow: 0 12rpx 24rpx rgba(239, 68, 68, 0.18);
}

.loading-inline {
  text-align: center;
  padding: 48rpx;
  color: #64748b;
  font-size: var(--ui-type-emphasis);
}

/* 通用模态框 */
.modal-mask {
  position: fixed;
  inset: 0;
  z-index: 100;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 40rpx 28rpx calc(40rpx + env(safe-area-inset-bottom));
  background: rgba(15, 23, 42, 0.35);
  box-sizing: border-box;
}

.modal-card {
  width: 100%;
  max-height: 80vh;
  padding: 28rpx;
  border-radius: 30rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.96) 0%, rgba(248, 251, 255, 0.94) 100%);
  border: 1rpx solid rgba(255, 255, 255, 0.72);
  box-shadow: 0 28rpx 60rpx rgba(15, 23, 42, 0.16);
  backdrop-filter: blur(24rpx);
  -webkit-backdrop-filter: blur(24rpx);
  box-sizing: border-box;
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
  margin-bottom: 20rpx;
  padding-bottom: 16rpx;
  border-bottom: 1rpx solid #f1f5f9;
}

.modal-title {
  color: #0f172a;
  font-size: var(--ui-type-dialog);
  font-weight: 700;
}

.modal-close {
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: rgba(241, 245, 249, 0.8);
  color: #64748b;
  font-size: var(--ui-type-value);
  font-weight: 600;
  transition: background 0.15s;
}

.modal-close:active {
  background: rgba(226, 232, 240, 0.9);
}

.modal-body {
  max-height: 56vh;
}

.hr-profile-export-modal {
  display: flex;
  flex-direction: column;
  width: 680rpx;
  max-height: 78vh;
  overflow: hidden;
}

.hr-profile-export-note {
  margin-top: 6rpx;
  color: #64748b;
  font-size: var(--ui-type-label);
  line-height: 1.5;
}

.hr-profile-export-toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18rpx;
  margin-bottom: 14rpx;
  padding: 14rpx 16rpx;
  border-radius: 20rpx;
  background: linear-gradient(135deg, rgba(239, 246, 255, 0.88), rgba(248, 251, 255, 0.76));
  border: 1rpx solid rgba(191, 219, 254, 0.7);
}

.hr-profile-export-count {
  color: #2563eb;
  font-size: var(--ui-type-label);
  font-weight: 700;
}

.hr-profile-export-quick-actions {
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.hr-profile-export-body {
  flex: 1;
  min-height: 0;
  max-height: none;
}

.hr-profile-export-column {
  display: flex;
  align-items: center;
  gap: 16rpx;
  min-height: 76rpx;
  margin-bottom: 10rpx;
  padding: 12rpx 16rpx;
  border-radius: 20rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.94), rgba(246, 249, 255, 0.84));
  border: 1rpx solid rgba(226, 237, 247, 0.96);
  box-sizing: border-box;
}

.hr-profile-export-column checkbox {
  flex-shrink: 0;
  transform: scale(0.88);
}

.hr-profile-export-column-text {
  display: flex;
  flex: 1;
  align-items: center;
  justify-content: space-between;
  gap: 14rpx;
  min-width: 0;
}

.hr-profile-export-column-label {
  overflow: hidden;
  color: #1e293b;
  font-size: var(--ui-type-body);
  font-weight: 700;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.hr-profile-export-column-group {
  flex-shrink: 0;
  padding: 5rpx 12rpx;
  border-radius: 999rpx;
  color: #2563eb;
  font-size: var(--ui-type-micro);
  font-weight: 700;
  background: rgba(219, 234, 254, 0.76);
  border: 1rpx solid rgba(147, 197, 253, 0.56);
}

.hr-profile-export-format {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 12rpx;
  margin-top: 16rpx;
}

.hr-profile-export-format-option {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  min-height: 66rpx;
  border-radius: 18rpx;
  color: #49627f;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.96), rgba(246, 249, 255, 0.88));
  border: 1rpx solid rgba(191, 219, 254, 0.74);
  box-sizing: border-box;
}

.hr-profile-export-format-option.is-active {
  color: #ffffff;
  background: linear-gradient(135deg, #2563eb 0%, #3b82f6 58%, #60a5fa 100%);
  border-color: transparent;
  box-shadow: 0 12rpx 24rpx rgba(29, 78, 216, 0.18);
}

.hr-profile-export-format-title {
  font-size: var(--ui-type-control);
  font-weight: 800;
}

.hr-profile-export-format-ext {
  font-size: var(--ui-type-caption);
  font-weight: 600;
  opacity: 0.78;
}

.hr-profile-export-footer {
  margin-top: 0;
}

/* CSV mapping dialog */
.csv-mapping-card {
  display: flex;
  flex-direction: column;
  width: 680rpx;
  max-height: 80vh;
  overflow: hidden;
}

.csv-mapping-card .modal-header {
  flex: 0 0 auto;
}

.csv-mapping-body {
  flex: 1 1 auto;
  min-height: 0;
  max-height: none;
  padding: 0 4rpx;
  box-sizing: border-box;
}
.csv-mapping-tip {
  font-size: var(--ui-type-control);
  color: #64748b;
  margin-bottom: 20rpx;
  line-height: 1.5;
}
.csv-mapping-row {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 18rpx 16rpx;
  align-items: start;
  margin-bottom: 16rpx;
  padding: 20rpx;
  border-radius: 22rpx;
  background: linear-gradient(135deg, rgba(255,255,255,0.96), rgba(239,246,255,0.82));
  border: 1rpx solid rgba(191,219,254,0.62);
  box-shadow: 0 10rpx 22rpx rgba(15,23,42,0.04), inset 0 1rpx 0 rgba(255,255,255,0.86);
  box-sizing: border-box;
}
.csv-mapping-row-header {
  display: none;
  background: linear-gradient(135deg, rgba(248,250,252,0.94), rgba(239,246,255,0.82));
  font-weight: 600;
  font-size: var(--ui-type-control);
  color: #64748b;
  border-radius: 8rpx;
  padding: 12rpx 16rpx;
  margin-bottom: 4rpx;
}
.csv-mapping-col {
  min-width: 0;
  font-size: var(--ui-type-emphasis);
  color: #1e293b;
  box-sizing: border-box;
}
.csv-mapping-col-csv { grid-column: 1; }
.csv-mapping-col-type { grid-column: 2; text-align: right; }
.csv-mapping-col-sample { grid-column: 1 / -1; color: #64748b; font-size: var(--ui-type-control); }
.csv-mapping-col-target { grid-column: 1 / -1; min-width: 0; }
.csv-mapping-col-target picker { display: block; width: 100%; }
.csv-mapping-mobile-label {
  display: block;
  margin-bottom: 8rpx;
  color: #64748b;
  font-size: var(--ui-type-caption);
  font-weight: 700;
  line-height: 1.35;
}
.csv-mapping-primary-text {
  display: block;
  color: #0f172a;
  font-size: var(--ui-type-value);
  font-weight: 750;
  line-height: 1.5;
  overflow-wrap: anywhere;
}
.csv-mapping-sample-text {
  display: -webkit-box;
  overflow: hidden;
  color: #64748b;
  line-height: 1.55;
  text-overflow: ellipsis;
  overflow-wrap: anywhere;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}
.csv-mapping-sample-hint {
  display: block;
  margin-top: 6rpx;
  color: #2563eb;
  font-size: var(--ui-type-caption);
  font-weight: 700;
}
.csv-mapping-type-tag {
  display: inline-block;
  font-size: var(--ui-type-meta);
  padding: 2rpx 12rpx;
  border-radius: 6rpx;
  background: #eff6ff;
  color: #2563eb;
}
.csv-mapping-picker-value {
  display: flex;
  align-items: center;
  width: 100%;
  min-height: 72rpx;
  padding: 14rpx 18rpx;
  border: 1rpx solid rgba(147,197,253,0.78);
  border-radius: 16rpx;
  font-size: var(--ui-type-body);
  line-height: 1.45;
  font-weight: 700;
  color: #1e293b;
  background: linear-gradient(135deg, rgba(255,255,255,0.98), rgba(239,246,255,0.90));
  white-space: normal;
  overflow: visible;
  text-overflow: clip;
  overflow-wrap: anywhere;
  box-sizing: border-box;
}

.csv-skip-invalid-row {
  flex: 0 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20rpx;
  padding: 20rpx 28rpx;
  border-top: 1rpx solid #f1f5f9;
  border-bottom: 1rpx solid #f1f5f9;
  margin: 0 0 8rpx 0;
}
.csv-skip-invalid-label {
  flex: 1 1 auto;
  min-width: 0;
}
.csv-skip-invalid-row switch {
  flex: 0 0 auto;
}
.csv-skip-invalid-label text {
  display: block;
}
.csv-skip-invalid-label text:first-child {
  font-size: var(--ui-type-emphasis);
  font-weight: 500;
  color: #1e293b;
}
.csv-skip-invalid-desc {
  font-size: var(--ui-type-meta);
  color: #94a3b8;
  margin-top: 4rpx;
}

.csv-mapping-scroll-hint {
  flex: 0 0 auto;
}

.csv-mapping-footer {
  flex: 0 0 auto;
  display: flex;
  align-items: center;
  gap: 14rpx;
  padding-top: 14rpx;
  padding-bottom: 0;
  box-sizing: border-box;
}

.csv-mapping-footer button {
  flex: 1 1 0;
  min-width: 0;
  min-height: 76rpx;
  height: 76rpx;
  margin: 0;
  padding: 0 18rpx;
  line-height: 76rpx;
  font-size: var(--ui-type-control);
  box-sizing: border-box;
}

.hr-import-preview-card {
  display: flex;
  flex-direction: column;
  width: 680rpx;
  max-height: 82vh;
  overflow: hidden;
}

.hr-import-preview-source {
  max-width: 520rpx;
  margin-top: 6rpx;
  color: #64748b;
  font-size: var(--ui-type-caption);
  line-height: 1.45;
  overflow-wrap: anywhere;
}

.hr-import-preview-body {
  flex: 1 1 auto;
  min-height: 0;
  max-height: none;
  padding: 0 4rpx 18rpx;
  box-sizing: border-box;
}

.hr-import-preview-stats {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 14rpx;
}

.hr-import-preview-stat {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
  min-width: 0;
  padding: 18rpx;
  border: 1rpx solid rgba(191, 219, 254, 0.62);
  border-radius: 18rpx;
  background: linear-gradient(135deg, rgba(255,255,255,0.98), rgba(239,246,255,0.84));
  color: #64748b;
  font-size: var(--ui-type-meta);
  box-sizing: border-box;
}

.hr-import-preview-stat-value {
  color: #0f172a;
  font-size: var(--ui-type-dialog);
  font-weight: 750;
  line-height: 1.2;
}

.hr-import-preview-stat.is-success .hr-import-preview-stat-value { color: #15803d; }
.hr-import-preview-stat.is-warning .hr-import-preview-stat-value { color: #c2410c; }

.hr-import-preview-warning,
.hr-import-preview-note {
  margin-top: 18rpx;
  padding: 18rpx 20rpx;
  border-radius: 16rpx;
  font-size: var(--ui-type-label);
  line-height: 1.6;
  overflow-wrap: anywhere;
}

.hr-import-preview-warning {
  border: 1rpx solid rgba(253, 186, 116, 0.52);
  background: linear-gradient(135deg, rgba(255, 247, 237, 0.96), rgba(254, 215, 170, 0.72));
  color: #c2410c;
}

.hr-import-preview-note {
  border: 1rpx solid rgba(147, 197, 253, 0.55);
  background: rgba(239, 246, 255, 0.88);
  color: #2563eb;
}

.hr-import-preview-section {
  margin-top: 22rpx;
}

.hr-import-preview-heading {
  margin-bottom: 12rpx;
  color: #0f172a;
  font-size: var(--ui-type-body);
  font-weight: 750;
}

.hr-import-preview-mapping {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
  gap: 12rpx;
  align-items: center;
  padding: 14rpx 16rpx;
  border-bottom: 1rpx solid rgba(226, 232, 240, 0.9);
  color: #334155;
  font-size: var(--ui-type-label);
  line-height: 1.5;
}

.hr-import-preview-column,
.hr-import-preview-target {
  min-width: 0;
  overflow-wrap: anywhere;
}

.hr-import-preview-arrow { color: #60a5fa; }

.hr-import-preview-target {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
  color: #2563eb;
  font-weight: 700;
}

.hr-import-preview-type {
  color: #94a3b8;
  font-size: var(--ui-type-micro);
  font-weight: 500;
}

.hr-import-preview-chips {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
}

.hr-import-preview-chip {
  max-width: 100%;
  padding: 7rpx 13rpx;
  border: 1rpx solid rgba(147, 197, 253, 0.58);
  border-radius: 999rpx;
  background: rgba(239, 246, 255, 0.88);
  color: #2563eb;
  font-size: var(--ui-type-caption);
  line-height: 1.45;
  overflow-wrap: anywhere;
  box-sizing: border-box;
}

.hr-import-preview-chip.is-muted {
  border-color: rgba(203, 213, 225, 0.7);
  background: rgba(248, 250, 252, 0.9);
  color: #64748b;
}

.hr-import-preview-dict-row {
  padding: 12rpx 0;
  border-bottom: 1rpx solid rgba(226, 232, 240, 0.75);
}

.hr-import-preview-dict-label {
  display: block;
  margin-bottom: 10rpx;
  color: #334155;
  font-size: var(--ui-type-label);
  font-weight: 700;
}

.hr-import-preview-empty {
  color: #94a3b8;
  font-size: var(--ui-type-meta);
}

.hr-import-preview-card button[disabled] {
  opacity: 0.46;
}

.validation-errors-card {
  width: 700rpx;
  max-height: 85vh;
}

.validation-errors-summary {
  font-size: var(--ui-type-emphasis);
  color: #ef4444;
  padding: 0 28rpx 16rpx 28rpx;
  font-weight: 500;
}

.validation-errors-body {
  max-height: 52vh;
  padding: 0 24rpx;
}

/* Validation error cards — matching hr-member-card design */
.validation-error-card {
  padding: 24rpx;
  margin-bottom: 14rpx;
  border-radius: 22rpx;
  background: linear-gradient(135deg, rgba(255,255,255,0.92) 0%, rgba(254,250,250,0.82) 100%);
  border: 1rpx solid rgba(254,226,226,0.8);
  box-shadow: 0 12rpx 24rpx rgba(15,23,42,0.04), inset 0 1rpx 0 rgba(255,255,255,0.78);
}

.ve-card-top {
  display: flex;
  align-items: baseline;
  gap: 12rpx;
  margin-bottom: 14rpx;
  padding-bottom: 12rpx;
  border-bottom: 1rpx solid #fef2f2;
}

.ve-card-name {
  font-size: var(--ui-type-section);
  font-weight: 700;
  color: #0f172a;
}

.ve-card-sid {
  font-size: var(--ui-type-label);
  color: #64748b;
  font-weight: 500;
}

.ve-card-errors {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}

.ve-error-item {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
  padding: 10rpx 14rpx;
  background: #fffbfb;
  border-radius: 12rpx;
  border: 1rpx solid #fee2e2;
}

.ve-error-label {
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.ve-error-field {
  font-size: var(--ui-type-body);
  font-weight: 600;
  color: #1e293b;
}

.ve-error-type-tag {
  display: inline-block;
  font-size: var(--ui-type-caption);
  padding: 2rpx 10rpx;
  border-radius: 6rpx;
  background: #eff6ff;
  color: #2563eb;
}

.ve-error-detail {
  font-size: var(--ui-type-label);
  color: #64748b;
  line-height: 1.5;
}

.ve-error-reason {
  color: #dc2626;
  font-weight: 500;
}

.ve-error-value {
  display: block;
  margin-top: 2rpx;
  color: #94a3b8;
  font-size: var(--ui-type-meta);
}

.picker-placeholder {
  padding: 20rpx 24rpx;
  border-radius: 24rpx;
  background: #f8fafc;
  border: 1rpx dashed #cbd5e1;
  font-size: var(--ui-type-value);
  color: #94a3b8;
}

/* Publications tab — minimal additions using existing rules-tab patterns */
.select-chip-inline { display: inline-flex; margin-right: 12rpx; vertical-align: middle; }
.popup-footer { display: flex; align-items: center; justify-content: space-between; padding: 20rpx 28rpx; border-top: 1rpx solid rgba(226,237,247,0.92); }
.popup-footer-info { font-size: var(--ui-type-emphasis); color: #64748b; }

/* Designation popup */
.desig-popup-card {
  max-height: 88vh;
  padding: 28rpx 28rpx 24rpx;
}
.desig-popup-card .button-row { margin-top: 16rpx; }

.desig-inner-scroll { margin-bottom: 8rpx; border-radius: 20rpx; background: rgba(248,250,252,0.6); border: 1rpx solid rgba(226,232,240,0.7); padding: 8rpx; }
.desig-current-scroll { height: 280rpx; }
.desig-candidate-scroll { height: 480rpx; }
.desig-current-item { background: rgba(255,255,255,0.85); }
.popup-scroll { max-height: 58vh; }
.popup-scroll .button-row button {
  min-height: 76rpx;
  height: auto;
  line-height: 1.35;
  font-size: var(--ui-type-value);
  padding: 14rpx 24rpx;
}

/* ── Card-based overview result list (matches user-side "结果公示" style) ── */

@keyframes glassFadeUp {
  from { opacity: 0; transform: translateY(18rpx); }
  to   { opacity: 1; transform: translateY(0); }
}

.ranked-list {
  display: flex; flex-direction: column; gap: 16rpx;
  padding-bottom: 8rpx;
}

.rank-card {
  display: flex; align-items: center; gap: 14rpx;
  padding: 20rpx 20rpx 20rpx 16rpx; border-radius: 24rpx;
  background: linear-gradient(135deg, rgba(255,255,255,0.90) 0%, rgba(248,251,255,0.82) 100%);
  border: 1rpx solid rgba(226,237,247,0.94);
  box-shadow: 0 10rpx 24rpx rgba(15,23,42,0.035), inset 0 1rpx 0 rgba(255,255,255,0.78);
  backdrop-filter: blur(20rpx); -webkit-backdrop-filter: blur(20rpx);
  box-sizing: border-box;
  transition: transform 0.20s ease, box-shadow 0.24s ease;
  animation: glassFadeUp 0.36s ease both;
}
.rank-card:active {
  transform: translateY(2rpx) scale(0.992);
  box-shadow: 0 6rpx 16rpx rgba(15,23,42,0.05);
}

/* Score-tier card tints */
.rank-card-gold {
  border-color: rgba(251,191,36,0.55);
  box-shadow: 0 14rpx 28rpx rgba(245,158,11,0.10), inset 0 1rpx 0 rgba(255,255,255,0.78), 0 0 36rpx rgba(251,191,36,0.06);
  background: linear-gradient(135deg, rgba(255,252,245,0.94) 0%, rgba(254,249,235,0.86) 100%);
}
.rank-card-silver {
  border-color: rgba(203,213,225,0.60);
  box-shadow: 0 14rpx 28rpx rgba(148,163,184,0.08), inset 0 1rpx 0 rgba(255,255,255,0.78), 0 0 32rpx rgba(203,213,225,0.06);
  background: linear-gradient(135deg, rgba(252,252,253,0.94) 0%, rgba(246,248,252,0.86) 100%);
}
.rank-card-bronze {
  border-color: rgba(251,191,36,0.35);
  box-shadow: 0 14rpx 28rpx rgba(217,119,6,0.07), inset 0 1rpx 0 rgba(255,255,255,0.78), 0 0 30rpx rgba(245,158,11,0.04);
  background: linear-gradient(135deg, rgba(255,251,247,0.94) 0%, rgba(254,246,235,0.86) 100%);
}

/* Rank badge */
.rank-badge {
  flex-shrink: 0; width: 60rpx; height: 60rpx; border-radius: 18rpx;
  display: flex; align-items: center; justify-content: center;
  font-size: var(--ui-type-emphasis); font-weight: 800; line-height: 1;
  box-shadow: 0 8rpx 18rpx rgba(15,23,42,0.06);
}
.rank-badge-gold {
  background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 60%, #d97706 100%);
  font-size: var(--ui-type-dialog); color: #ffffff;
  box-shadow: 0 8rpx 20rpx rgba(245,158,11,0.28);
}
.rank-badge-silver {
  background: linear-gradient(135deg, #e2e8f0 0%, #cbd5e1 60%, #94a3b8 100%);
  font-size: var(--ui-type-dialog); color: #ffffff;
  box-shadow: 0 8rpx 20rpx rgba(148,163,184,0.22);
}
.rank-badge-bronze {
  background: linear-gradient(135deg, #fcd34d 0%, #f59e0b 60%, #b45309 100%);
  font-size: var(--ui-type-dialog); color: #ffffff;
  box-shadow: 0 8rpx 20rpx rgba(217,119,6,0.22);
}
.rank-badge-normal {
  background: linear-gradient(135deg, rgba(239,246,255,0.96) 0%, rgba(219,234,254,0.88) 100%);
  color: #2563eb; border: 1rpx solid rgba(147,197,253,0.55);
}

/* Member info */
.member-info {
  flex: 1; min-width: 0; display: flex; flex-direction: column; gap: 4rpx;
}
.member-name {
  color: #0f172a; font-size: var(--ui-type-value); font-weight: 700; line-height: 1.4;
  word-break: break-word;
}
.member-detail {
  color: #64748b; font-size: var(--ui-type-meta); line-height: 1.5; word-break: break-word;
}

/* Score display */
.score-display {
  flex-shrink: 0; display: flex; flex-direction: column; align-items: flex-end;
  min-width: 120rpx; padding-left: 8rpx;
}
.score-value {
  color: #2563eb; font-size: var(--ui-type-dialog); font-weight: 800; line-height: 1.2;
  letter-spacing: 0; white-space: nowrap;
  font-variant-numeric: tabular-nums;
}
.score-label {
  color: #94a3b8; font-size: var(--ui-type-caption); font-weight: 500; line-height: 1.4;
  margin-top: 2rpx;
}

/* Inline progress bar (inside card) */
.progress-row {
  display: flex; align-items: center; gap: 10rpx; margin-top: 6rpx;
}
.progress-row .progress-track {
  flex: 1; max-width: 200rpx; height: 8rpx; border-radius: 8rpx;
  background: rgba(226,232,240,0.75); overflow: hidden;
}
.progress-row .progress-fill {
  height: 100%; border-radius: 8rpx;
}
.progress-row .progress-nums {
  font-size: var(--ui-type-caption); color: #94a3b8; white-space: nowrap;
}

/* ─── Grade Band Bubbles (Feature 4) ─── */
.grade-band-card {
  transition: background 0.2s ease, border-color 0.2s ease, box-shadow 0.24s ease, transform 0.2s ease;
}
.grade-band-collapsed {
  display: flex; align-items: center; padding: 0 32rpx 24rpx; cursor: pointer;
}
.grade-band-bubble {
  display: inline-flex; align-items: center; justify-content: center;
  padding: 14rpx 28rpx; border-radius: 40rpx;
  box-shadow: 0 4rpx 16rpx rgba(15,23,42,0.10), inset 0 1rpx 0 rgba(255,255,255,0.22);
}
.grade-band-bubble-name {
  color: #fff; font-size: var(--ui-type-emphasis); font-weight: 600;
  white-space: nowrap;
}

/* ─── Merit Summary (Feature 5) — redesigned to match project card language ─── */
.merit-summary-panel {
  background: transparent; border: none; box-shadow: none; padding: 0;
}
.merit-summary-head-bar {
  display: flex; align-items: center; justify-content: space-between;
  margin-top: 16rpx;
}
.merit-export-btn-wrap {
  flex-shrink: 0; width: 320rpx;
}
.merit-export-btn-wrap .secondary-btn {
  width: 100%; min-width: 0; padding: 0; font-size: var(--ui-type-meta);
}
.merit-summary-total-chip {
  font-size: var(--ui-type-meta); font-weight: 600; color: #64748b;
  padding: 8rpx 18rpx; border-radius: 999rpx;
  background: rgba(255,255,255,0.64);
  border: 1rpx solid rgba(148,163,184,0.20);
}
.merit-summary-filter-bar {
  display: flex; gap: 18rpx; padding: 18rpx 24rpx; margin-bottom: 22rpx;
  border-radius: 20rpx;
  background: linear-gradient(135deg, rgba(255,255,255,0.78) 0%, rgba(248,251,255,0.72) 100%);
  border: 1rpx solid rgba(255,255,255,0.65);
  box-shadow: 0 8rpx 22rpx rgba(15,23,42,0.04), inset 0 1rpx 0 rgba(255,255,255,0.72);
}
.merit-summary-filter-field {
  display: flex; align-items: center; gap: 10rpx;
}
.merit-summary-filter-label {
  font-size: var(--ui-type-meta); font-weight: 600; color: #64748b; white-space: nowrap;
}
.merit-summary-filter-value {
  font-size: var(--ui-type-label); color: #163047; padding: 8rpx 16rpx;
  border-radius: 14rpx;
  background: rgba(255,255,255,0.82);
  border: 1rpx solid rgba(148,163,184,0.25);
  min-width: 88rpx; text-align: center;
}
.glass-empty {
  background: linear-gradient(135deg, rgba(255,255,255,0.68) 0%, rgba(248,251,255,0.62) 100%);
  border-radius: 24rpx; border: 1rpx solid rgba(255,255,255,0.55);
  display: flex; flex-direction: column; align-items: center;
  padding: 48rpx 30rpx; text-align: center;
}
.merit-summary-panel .glass-empty .empty-title,
.merit-summary-panel .glass-empty .empty-desc {
  width: 100%;
  margin-left: auto;
  margin-right: auto;
  text-align: center;
}

/* ── Group card (matches .list-item / .rule-clause-card pattern) ── */
.merit-group-card {
  margin-bottom: 20rpx; border-radius: 22rpx;
  background: linear-gradient(135deg, rgba(255,255,255,0.92) 0%, rgba(249,251,255,0.82) 100%);
  border: 1rpx solid rgba(226,237,247,0.96);
  box-shadow: 0 12rpx 24rpx rgba(15,23,42,0.04), inset 0 1rpx 0 rgba(255,255,255,0.78);
  overflow: hidden;
}

/* ── Group header ── */
.merit-group-head {
  display: flex; align-items: center; gap: 0;
  padding: 22rpx 24rpx;
  transition: background 0.18s ease;
}
.merit-group-head:active {
  background: rgba(241,245,249,0.28);
}
.merit-group-head-accent {
  width: 6rpx; height: 52rpx; border-radius: 999rpx;
  flex-shrink: 0; margin-right: 18rpx;
  opacity: 0.82;
}
.merit-group-head-body {
  flex: 1; min-width: 0;
}
.merit-group-head-title {
  line-height: 1.4;
}
.merit-title-dept {
  font-weight: 600; color: #475569; font-size: var(--ui-type-value);
}
.merit-group-head-meta {
  display: flex; align-items: center; gap: 10rpx; margin-top: 8rpx;
}

/* ── Colorful accent chips (matches .preview-chip style) ── */
.merit-chip {
  display: inline-flex; align-items: center; justify-content: center;
  min-height: 36rpx; padding: 5rpx 14rpx; border-radius: 999rpx;
  font-size: var(--ui-type-caption); font-weight: 700; line-height: 1.35;
  border: 1rpx solid transparent;
}
.merit-chip-count {
  background: rgba(219,234,254,0.76); color: #2563eb;
  border-color: rgba(147,197,253,0.64);
}
.merit-chip-quota {
  background: rgba(255,237,213,0.78); color: #c2410c;
  border-color: rgba(253,186,116,0.58);
}
.merit-chip-ident {
  background: rgba(209,250,229,0.78); color: #15803d;
  border-color: rgba(110,231,183,0.58);
}

/* ── Chevron circle ── */
.merit-group-chevron {
  width: 44rpx; height: 44rpx; border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0; margin-left: 10rpx;
  background: rgba(241,245,249,0.65);
  border: 1rpx solid rgba(148,163,184,0.18);
  transition: background 0.2s ease, border-color 0.2s ease;
}
.merit-group-chevron text {
  font-size: var(--ui-type-micro); color: #64748b;
  transition: transform 0.24s ease;
}
.merit-group-chevron-open {
  background: rgba(219,234,254,0.64);
  border-color: rgba(147,197,253,0.38);
}
.merit-group-chevron-open text {
  color: #2563eb;
}

/* ── Group body (expanded member list) ── */
.merit-group-body {
  padding: 0 16rpx 18rpx;
}

/* ── Member card ── */
.merit-member-card {
  display: flex; align-items: center; gap: 0;
  margin: 0 4rpx 8rpx; padding: 16rpx 18rpx;
  border-radius: 18rpx;
  background: linear-gradient(135deg, rgba(255,255,255,0.90) 0%, rgba(241,245,249,0.78) 100%);
  border: 1rpx solid rgba(226,232,240,0.65);
  box-shadow: 0 4rpx 12rpx rgba(15,23,42,0.03);
}
.merit-member-accent {
  width: 5rpx; height: 42rpx; border-radius: 999rpx;
  flex-shrink: 0; margin-right: 16rpx;
  opacity: 0.58;
}
.merit-member-body {
  flex: 1; min-width: 0;
}

/* ── Member top row: name (left) + student ID chip (right) ── */
.merit-member-row {
  display: flex; align-items: center; justify-content: space-between;
}
.merit-member-name {
  font-size: var(--ui-type-value); font-weight: 700; color: #1e293b; line-height: 1.35;
}
.merit-member-sid-chip {
  display: inline-flex; align-items: center; justify-content: center;
  padding: 4rpx 16rpx; border-radius: 999rpx; flex-shrink: 0; margin-left: 14rpx;
  background: rgba(241,245,249,0.72);
  border: 1rpx solid rgba(148,163,184,0.16);
}
.merit-member-sid-chip text {
  font-size: var(--ui-type-caption); font-weight: 600; color: #64748b; line-height: 1.5;
  font-family: -apple-system, "Helvetica Neue", "SF Pro Text", "Segoe UI", Roboto, sans-serif;
  letter-spacing: 0.8rpx;
}

/* ── Member tag row (colorful bubbles matching .preview-chip style) ── */
.merit-member-tags {
  display: flex; flex-wrap: wrap; gap: 8rpx; margin-top: 8rpx;
}
.merit-tag {
  display: inline-flex; align-items: center; justify-content: center;
  min-height: 34rpx; padding: 4rpx 12rpx; border-radius: 999rpx;
  font-size: var(--ui-type-micro); font-weight: 600; line-height: 1.35;
  border: 1rpx solid transparent;
}
.merit-tag-ident {
  background: rgba(219,234,254,0.76); color: #2563eb;
  border-color: rgba(147,197,253,0.64);
}
.merit-tag-dept {
  background: rgba(207,250,254,0.72); color: #0891b2;
  border-color: rgba(103,232,249,0.48);
}
.merit-tag-wg {
  background: rgba(209,250,229,0.78); color: #059669;
  border-color: rgba(110,231,183,0.58);
}

/* ── Portal back button ── */
.actions {
  margin-top: 8rpx;
  padding-bottom: 12rpx;
}
.actions button {
  position: relative;
  overflow: hidden;
  height: 94rpx;
  line-height: 94rpx;
  margin-bottom: 20rpx;
  border: none;
  border-radius: 24rpx;
  font-size: var(--ui-type-value);
  font-weight: 700;
  letter-spacing: 0.5rpx;
  box-shadow: 0 16rpx 30rpx rgba(15,23,42,0.06), inset 0 1rpx 0 rgba(255,255,255,0.24);
}
.actions button::after {
  border: none;
}

/* ═══════════════════════════════════════════════════════════
   Admin Audit — glassmorphism enhancement styles
   ═══════════════════════════════════════════════════════════ */

/* ── Audit gradient status chips (replace flat green/red/blue-chip) ── */
.audit-chip {
  display: inline-flex; align-items: center; justify-content: center;
  min-height: 44rpx; padding: 8rpx 20rpx; border-radius: 999rpx;
  font-size: var(--ui-type-meta); font-weight: 700; line-height: 1.2; letter-spacing: 0.2rpx;
  border: 1rpx solid transparent;
  box-shadow: 0 6rpx 14rpx rgba(15,23,42,0.04);
}
.audit-chip-draft    { color: #64748b; background: linear-gradient(135deg, rgba(241,245,249,0.96) 0%, rgba(226,232,240,0.90) 100%); border-color: rgba(148,163,184,0.42); }
.audit-chip-pending  { color: #c2410c; background: linear-gradient(135deg, rgba(255,247,237,0.98) 0%, rgba(254,215,170,0.90) 100%); border-color: rgba(253,186,116,0.55); }
.audit-chip-progress { color: #2563eb; background: linear-gradient(135deg, rgba(219,234,254,0.98) 0%, rgba(191,219,254,0.90) 100%); border-color: rgba(147,197,253,0.55); }
.audit-chip-approved { color: #15803d; background: linear-gradient(135deg, rgba(220,252,231,0.98) 0%, rgba(187,247,208,0.90) 100%); border-color: rgba(134,239,172,0.52); }
.audit-chip-rejected { color: #dc2626; background: linear-gradient(135deg, rgba(254,242,242,0.98) 0%, rgba(254,226,226,0.90) 100%); border-color: rgba(252,165,165,0.50); }
.audit-chip-withdrawn{ color: #9a3412; background: linear-gradient(135deg, rgba(255,247,237,0.96) 0%, rgba(254,215,170,0.84) 100%); border-color: rgba(253,186,116,0.46); }

/* Override the old flat chips globally for audit tabs */
.green-chip, .blue-chip, .red-chip {
  min-height: 44rpx !important; padding: 8rpx 20rpx !important;
  border-radius: 999rpx !important; font-size: var(--ui-type-meta) !important; font-weight: 700 !important;
  border: 1rpx solid transparent !important;
  box-shadow: 0 6rpx 14rpx rgba(15,23,42,0.04) !important;
}
.green-chip { color: #15803d !important; background: linear-gradient(135deg, rgba(220,252,231,0.98) 0%, rgba(187,247,208,0.90) 100%) !important; border-color: rgba(134,239,172,0.52) !important; }
.blue-chip  { color: #2563eb !important; background: linear-gradient(135deg, rgba(219,234,254,0.98) 0%, rgba(191,219,254,0.90) 100%) !important; border-color: rgba(147,197,253,0.55) !important; }
.red-chip   { color: #dc2626 !important; background: linear-gradient(135deg, rgba(254,242,242,0.98) 0%, rgba(254,226,226,0.90) 100%) !important; border-color: rgba(252,165,165,0.50) !important; }
.gray-chip  { color: #64748b !important; background: linear-gradient(135deg, rgba(241,245,249,0.96) 0%, rgba(226,232,240,0.90) 100%) !important; border-color: rgba(148,163,184,0.42) !important; }
.orange-chip{ color: #c2410c !important; background: linear-gradient(135deg, rgba(255,247,237,0.98) 0%, rgba(254,215,170,0.90) 100%) !important; border-color: rgba(253,186,116,0.55) !important; }

/* ── Audit condition bubble chips (scope tags in template editor) ── */
.info-chip {
  display: inline-flex !important; align-items: center; justify-content: center;
  min-height: 40rpx !important; padding: 6rpx 16rpx !important; border-radius: 999rpx !important;
  font-size: var(--ui-type-caption) !important; font-weight: 700 !important; line-height: 1.3 !important;
  border: 1rpx solid transparent !important;
  background: linear-gradient(135deg, rgba(239,246,255,0.96) 0%, rgba(219,234,254,0.88) 100%) !important;
  color: #2563eb !important; border-color: rgba(147,197,253,0.55) !important;
  box-shadow: 0 4rpx 10rpx rgba(37,99,235,0.06) !important;
}

/* ── Audit template list items (expanded/collapsible) ── */
.audit-template-list-item {
  flex-direction: column !important;
  padding: 22rpx 20rpx !important;
  border-radius: 22rpx !important;
  margin-bottom: 12rpx !important;
  background: linear-gradient(135deg, rgba(255,255,255,0.90) 0%, rgba(248,251,255,0.80) 100%) !important;
  border: 1rpx solid rgba(226,237,247,0.90) !important;
  box-shadow: 0 10rpx 24rpx rgba(15,23,42,0.035), inset 0 1rpx 0 rgba(255,255,255,0.76) !important;
  transition: transform 0.18s ease, box-shadow 0.22s ease !important;
}
.audit-template-list-item:active { transform: translateY(1rpx) scale(0.992) !important; }
.audit-template-list-item:last-child { margin-bottom: 4rpx !important; }

/* ── Audit template expanded preview ── */
.audit-template-expanded {
  width: 100%; margin-top: 14rpx; padding-top: 14rpx;
  border-top: 1rpx solid rgba(226,232,240,0.7);
}
.audit-template-step-preview {
  margin-bottom: 12rpx; padding: 16rpx 18rpx; border-radius: 16rpx;
  background: linear-gradient(135deg, rgba(248,251,255,0.88) 0%, rgba(241,245,249,0.76) 100%);
  border: 1rpx solid rgba(226,232,240,0.55);
}
.audit-template-step-preview:last-child { margin-bottom: 0; }
.audit-template-step-head {
  display: flex; align-items: center; margin-bottom: 6rpx;
}

/* ── Condition bubble chips in expanded template preview ── */
.audit-cond-chip {
  display: inline-flex; align-items: center; justify-content: center;
  min-height: 36rpx; padding: 4rpx 14rpx; border-radius: 999rpx;
  font-size: var(--ui-type-caption); font-weight: 700; line-height: 1.3;
  border: 1rpx solid transparent; margin: 2rpx;
  box-shadow: 0 3rpx 8rpx rgba(15,23,42,0.03);
}
.audit-cond-dept  { background: linear-gradient(135deg, rgba(219,234,254,0.94) 0%, rgba(191,219,254,0.84) 100%); color: #2563eb; border-color: rgba(147,197,253,0.48); }
.audit-cond-wg    { background: linear-gradient(135deg, rgba(220,252,231,0.94) 0%, rgba(187,247,208,0.84) 100%); color: #15803d; border-color: rgba(134,239,172,0.48); }
.audit-cond-ident { background: linear-gradient(135deg, rgba(255,247,237,0.96) 0%, rgba(254,215,170,0.86) 100%); color: #c2410c; border-color: rgba(253,186,116,0.52); }
.audit-cond-person{ background: linear-gradient(135deg, rgba(224,242,254,0.94) 0%, rgba(186,230,253,0.84) 100%); color: #0369a1; border-color: rgba(56,189,248,0.48); }
.audit-cond-none   { background: linear-gradient(135deg, rgba(254,242,242,0.92) 0%, rgba(254,226,226,0.80) 100%); color: #dc2626; border-color: rgba(252,165,165,0.45); }

/* ── OR separator text ── */
.audit-or-sep {
  color: #ef4444; font-size: var(--ui-type-caption); font-weight: 700; margin: 2rpx 8rpx;
}

/* ── Audit progress detail popup (submission detail view) ── */
.audit-progress-header {
  padding: 20rpx 18rpx; margin-bottom: 16rpx; border-radius: 20rpx;
  background: linear-gradient(135deg, rgba(255,255,255,0.86) 0%, rgba(246,249,255,0.76) 100%);
  border: 1rpx solid rgba(226,237,247,0.84);
}
.audit-progress-step {
  padding: 20rpx 18rpx; margin-bottom: 10rpx; border-radius: 18rpx;
  background: linear-gradient(135deg, rgba(255,255,255,0.85) 0%, rgba(248,251,255,0.75) 100%);
  border: 1rpx solid rgba(226,237,247,0.84);
  box-shadow: 0 6rpx 14rpx rgba(15,23,42,0.025);
}
.audit-progress-step:last-child { margin-bottom: 0; }

/* ── Audit verification result card ── */
.audit-verify-result-card {
  margin-top: 18rpx; padding: 24rpx 22rpx; border-radius: 22rpx;
  background: linear-gradient(135deg, rgba(255,255,255,0.88) 0%, rgba(248,251,255,0.78) 100%);
  border: 1rpx solid rgba(226,237,247,0.88);
  box-shadow: 0 10rpx 24rpx rgba(15,23,42,0.04);
}
.audit-verify-result-badge {
  font-size: var(--ui-type-section); font-weight: 700; line-height: 1.4; margin-bottom: 10rpx;
}
.audit-verify-valid { color: #059669; }
.audit-verify-invalid { color: #ef4444; }

/* ── Audit stamp image preview ── */
.audit-stamp-preview {
  border-radius: 14rpx; background: #fff;
  border: 1rpx solid rgba(226,232,240,0.6);
  box-shadow: 0 4rpx 12rpx rgba(15,23,42,0.04);
  margin-top: 8rpx; margin-bottom: 12rpx;
}

/* ── Audit section sub-head ── */
.audit-section-subhead {
  margin-top: 22rpx; margin-bottom: 14rpx;
}

/* ── Template expand toggle indicator ── */
.audit-expand-toggle {
  display: flex; align-items: center; gap: 6rpx;
  padding: 6rpx 14rpx; border-radius: 999rpx;
  background: rgba(241,245,249,0.65);
  border: 1rpx solid rgba(148,163,184,0.18);
  color: #64748b; font-size: var(--ui-type-caption); font-weight: 600;
  transition: color 0.18s ease, background 0.18s ease, border-color 0.18s ease, box-shadow 0.22s ease, transform 0.18s ease;
}
.audit-expand-toggle:active {
  background: rgba(219,234,254,0.55);
  border-color: rgba(147,197,253,0.35);
  color: #2563eb;
}

/* ── File attachment item (for admin progress popup) ── */
.file-attach-item {
  display: flex; align-items: center; gap: 16rpx;
  padding: 18rpx 20rpx; margin-bottom: 8rpx; border-radius: 18rpx;
  background: linear-gradient(135deg, rgba(255,255,255,0.88) 0%, rgba(248,251,255,0.76) 100%);
  border: 1rpx solid rgba(226,237,247,0.88);
  box-shadow: 0 8rpx 18rpx rgba(15,23,42,0.03);
  box-sizing: border-box;
}
.file-attach-item:last-child { margin-bottom: 0; }
.file-attach-icon {
  flex-shrink: 0; width: 52rpx; height: 52rpx; border-radius: 14rpx;
  display: flex; align-items: center; justify-content: center;
  background: linear-gradient(135deg, rgba(219,234,254,0.94) 0%, rgba(191,219,254,0.84) 100%);
  border: 1rpx solid rgba(147,197,253,0.48);
  color: #2563eb; font-size: var(--ui-type-control); font-weight: 700;
}
.file-attach-name { flex: 1; min-width: 0; color: #0f172a; font-size: var(--ui-type-control); font-weight: 600; line-height: 1.4; }

/* ─── Admin audit submission list items (glassmorphism) ─── */
.audit-submission-admin-item {
  display: flex; align-items: center; gap: 18rpx;
  padding: 24rpx 22rpx; margin-bottom: 12rpx; border-radius: 22rpx;
  background: linear-gradient(135deg, rgba(255,255,255,0.90) 0%, rgba(248,251,255,0.80) 100%);
  border: 1rpx solid rgba(226,237,247,0.94);
  box-shadow: 0 10rpx 24rpx rgba(15,23,42,0.035), inset 0 1rpx 0 rgba(255,255,255,0.78);
  box-sizing: border-box; transition: transform 0.18s ease, box-shadow 0.22s ease;
  animation: glassFadeUp 0.36s ease both;
}
.audit-submission-admin-item:last-child { margin-bottom: 4rpx; }
.audit-submission-admin-item:active { transform: translateY(1rpx) scale(0.992); }
.audit-submission-admin-main { flex: 1; min-width: 0; display: flex; flex-direction: column; gap: 8rpx; }
.audit-submission-admin-title { color: #0f172a; font-size: var(--ui-type-value); font-weight: 700; line-height: 1.4; }
.audit-submission-admin-meta { display: flex; flex-wrap: wrap; align-items: center; gap: 10rpx; }
.audit-submission-admin-sub { font-size: var(--ui-type-caption); color: #94a3b8; }
.audit-submission-admin-arrow {
  flex-shrink: 0; width: 48rpx; height: 48rpx; border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  background: rgba(241,245,249,0.7); color: #94a3b8; font-size: var(--ui-type-dialog); font-weight: 600;
}

/* Final admin motion: prevent parent cards and popups from moving with child taps. */
.edit-box:active,
.list-card:active,
.card:active,
.section:active {
  transform: none !important;
  filter: none !important;
}

.result-table-row:active,
.task-table-row:active,
.popup-table-row:active,
.question-card:active,
.rank-card:active,
.hr-member-card:active,
.audit-submission-admin-item:active {
  transform: none !important;
}

@media (min-width: 520px) {
  .admin-workbench .section {
    min-width: 0;
  }

  .admin-workbench .edit-box,
  .admin-workbench .list-card {
    padding: 16px;
    border-radius: 15px;
  }

  .field-row,
  .admin-field-grid {
    gap: 12px;
  }

  .field-value {
    padding-left: 14px;
    font-size: var(--ui-type-emphasis);
    line-height: 1.5;
  }

  .field-label {
    padding-left: 14px;
  }

  .dialog-btn,
  .popup-scroll .button-row button {
    min-height: 44px !important;
    height: auto !important;
    padding: 10px 16px !important;
    font-size: var(--ui-type-body) !important;
    line-height: 20px !important;
  }
}

@media (min-width: 900px) {
  .admin-workbench {
    display: block;
  }

  .admin-workbench > .tabs {
    position: static;
    z-index: 24;
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    gap: var(--ui-tab-gap);
    max-height: none;
    overflow: visible;
    padding: var(--ui-tab-shell-padding);
    margin-bottom: 14px;
    border-radius: var(--ui-tab-shell-radius);
  }

  .admin-workbench > .tabs .tab {
    width: 100%;
    min-height: var(--ui-tab-min-height);
    justify-content: center;
    padding: var(--ui-tab-padding-y) var(--ui-tab-padding-x);
    text-align: center;
    white-space: nowrap;
  }

  .admin-workbench > .section {
    margin-bottom: 0;
  }

  .admin-workbench .edit-box,
  .admin-workbench .list-card {
    min-width: 0;
  }
}

.result-table-row.control-pressing,
.task-table-row.control-pressing,
.popup-table-row.control-pressing,
.question-card.control-pressing,
.rank-card.control-pressing,
.hr-member-card.control-pressing,
.audit-submission-admin-item.control-pressing {
  transform: translate3d(0, 2rpx, 0) scale(0.992) !important;
  opacity: 0.975 !important;
  filter: saturate(1.018) brightness(1.004) !important;
  transition:
    transform 0.18s cubic-bezier(0.2, 0.78, 0.22, 1),
    box-shadow 0.20s ease,
    opacity 0.18s ease,
    filter 0.20s ease !important;
}

.link:active,
.pill-link:active,
.pill-close:active,
.audit-template-list-item:active {
  transform: translate3d(0, 1rpx, 0) scale(0.984) !important;
  opacity: 0.94 !important;
  transition:
    transform 0.16s cubic-bezier(0.2, 0.78, 0.22, 1),
    opacity 0.16s ease,
    box-shadow 0.18s ease !important;
}

/* Popup close is plain text plus x, not a pill button. */
.popup-close,
.modal-close {
  display: inline-flex !important;
  align-items: center !important;
  justify-content: flex-end !important;
  gap: 8rpx !important;
  width: auto !important;
  height: auto !important;
  min-height: 0 !important;
  line-height: 1.4 !important;
  padding: 4rpx 0 4rpx 16rpx !important;
  border: 0 !important;
  border-radius: 0 !important;
  background: transparent !important;
  box-shadow: none !important;
  color: #475569 !important;
  font-size: var(--ui-type-control) !important;
  font-weight: 700 !important;
}

.popup-close::before,
.modal-close::before { display: none !important; content: none !important; }
.popup-close::after { content: "×" !important; color: #2563eb !important; font-size: var(--ui-type-value) !important; font-weight: 800 !important; line-height: 1 !important; }
.modal-close::before { content: "关闭" !important; display: inline !important; color: #475569 !important; }

@media (min-width: 520px) {
  .rule-panel-title,
  .question-title,
  .edit-box-title,
  .list-card-title {
    font-size: var(--ui-type-section) !important;
    line-height: 1.45 !important;
  }

  .stat-value,
  .hr-member-name,
  .result-name,
  .task-name {
    font-size: var(--ui-type-emphasis) !important;
    line-height: 1.5 !important;
  }

  .csv-mapping-col,
  .question-desc,
  .rule-panel-desc,
  .result-meta,
  .task-meta {
    font-size: var(--ui-type-meta) !important;
    line-height: 1.55 !important;
  }

  .popup-close,
  .modal-close {
    min-height: 36px !important;
    padding: 8px 0 8px 14px !important;
    font-size: var(--ui-type-meta) !important;
    line-height: 20px !important;
  }

  .popup-close::after {
    font-size: var(--ui-type-section) !important;
  }

  .csv-mapping-card {
    width: 100%;
    max-width: 720px;
    max-height: calc(100vh - 48px);
  }

  .csv-mapping-body {
    padding: 0 20px;
  }

  .csv-mapping-row,
  .csv-mapping-row-header {
    display: grid;
    grid-template-columns:
      minmax(0, 1.1fr)
      minmax(88px, 0.65fr)
      minmax(110px, 1fr)
      minmax(180px, 1.35fr);
    gap: 12px;
    align-items: center;
    margin-bottom: 0;
    padding: 12px 14px;
    border-radius: 0;
    border: 0;
    border-bottom: 1px solid rgba(226,237,247,0.96);
    background: transparent;
    box-shadow: none;
  }

  .csv-mapping-row-header {
    position: sticky;
    top: 0;
    z-index: 2;
    margin-bottom: 4px;
    border-radius: 10px;
    background: linear-gradient(135deg, rgba(248,250,252,0.98), rgba(239,246,255,0.96));
  }

  .csv-mapping-col-csv,
  .csv-mapping-col-type,
  .csv-mapping-col-sample,
  .csv-mapping-col-target {
    grid-column: auto;
    text-align: left;
  }

  .csv-mapping-mobile-label,
  .csv-mapping-sample-hint {
    display: none;
  }

  .csv-mapping-primary-text {
    display: block;
    white-space: normal;
    overflow: visible;
    text-overflow: clip;
    overflow-wrap: anywhere;
  }

  .csv-mapping-sample-text {
    display: block;
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
  }

  .csv-mapping-col-target,
  .csv-mapping-col-target picker,
  .csv-mapping-picker-value {
    min-width: 180px;
  }

  .csv-mapping-picker-value {
    min-height: 42px;
    padding: 9px 12px;
    border-radius: 12px;
    font-size: var(--ui-type-label);
    line-height: 20px;
    white-space: normal;
    overflow: visible;
    text-overflow: clip;
  }

  .csv-skip-invalid-row {
    gap: 16px;
    padding: 16px 20px;
    margin-bottom: 6px;
  }

  .csv-mapping-footer {
    gap: 12px;
    padding-top: 12px;
  }

  .csv-mapping-footer button {
    min-height: 46px;
    height: 46px;
    padding: 0 18px;
    line-height: 46px;
    font-size: var(--ui-type-label);
  }

  .hr-import-preview-card {
    width: 100%;
    max-width: 720px;
    max-height: calc(100vh - 48px);
  }

  .hr-import-preview-source {
    max-width: 560px;
    font-size: var(--ui-type-meta);
  }

  .hr-import-preview-body {
    padding: 0 20px 18px;
  }

  .hr-import-preview-stats {
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 12px;
  }

  .hr-import-preview-stat {
    gap: 6px;
    padding: 14px;
    border-radius: 14px;
    font-size: var(--ui-type-meta);
  }

  .hr-import-preview-stat-value { font-size: var(--ui-type-page); }
  .hr-import-preview-heading { font-size: var(--ui-type-body); }
  .hr-import-preview-mapping { padding: 10px 12px; font-size: var(--ui-type-label); }
  .hr-import-preview-type { font-size: var(--ui-type-caption); }
  .hr-import-preview-chip { padding: 5px 10px; font-size: var(--ui-type-meta); }
  .hr-import-preview-dict-label,
  .hr-import-preview-empty { font-size: var(--ui-type-meta); }
}

@media (min-width: 900px) {
  .csv-mapping-card,
  .hr-import-preview-card {
    max-width: 860px;
  }
}

/* 管理 Hero 使用同一组织控件语言，仅调整深色表面的光影层次。 */
.hero-admin .hero-org-context {
  background: linear-gradient(135deg, rgba(96, 165, 250, 0.20), rgba(255, 255, 255, 0.08));
  border-color: rgba(191, 219, 254, 0.24);
  box-shadow: 0 12rpx 28rpx rgba(2, 6, 23, 0.16), inset 0 1rpx 0 rgba(255, 255, 255, 0.16);
}

.hero-admin .hero-org-context-pressing {
  background: linear-gradient(135deg, rgba(96, 165, 250, 0.26), rgba(255, 255, 255, 0.11));
  box-shadow: 0 6rpx 16rpx rgba(2, 6, 23, 0.13), inset 0 1rpx 0 rgba(255, 255, 255, 0.18);
}

/* 人事模板：组织快照与共享模板库 */
.hr-template-workspace,
.hr-template-current-card,
.hr-template-editor {
  margin-top: 20rpx;
}

.hr-template-editor-actions {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 14rpx;
  width: 100%;
}

.hr-template-editor-btn {
  display: block;
  box-sizing: border-box;
  width: 100%;
  min-width: 0;
  min-height: 74rpx;
  height: 74rpx;
  margin: 0;
  padding: 0 12rpx;
  border-radius: 18rpx;
  font-size: var(--ui-type-meta);
  line-height: 74rpx;
  white-space: nowrap;
  overflow: hidden;
}

.hr-template-editor-save {
  grid-column: auto;
}

.hr-template-editor-btn.template-save-btn::before {
  display: none;
}

.hr-template-current-card {
  border-color: rgba(59, 130, 246, 0.22);
  background: linear-gradient(145deg, rgba(239, 246, 255, 0.90), rgba(255, 255, 255, 0.78));
}

.hr-template-empty,
.hr-switch-empty {
  padding: 28rpx;
  border: 1rpx dashed rgba(96, 165, 250, 0.42);
  border-radius: 18rpx;
  color: #475569;
  font-size: var(--ui-type-body);
  line-height: 1.7;
  background: rgba(239, 246, 255, 0.54);
}

.hr-template-source-row,
.hr-template-card-head,
.hr-switch-source-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 20rpx;
}

.hr-template-card {
  padding: 26rpx;
  margin-top: 18rpx;
  border: 1rpx solid rgba(147, 197, 253, 0.25);
  border-radius: 22rpx;
  background: linear-gradient(145deg, rgba(255, 255, 255, 0.84), rgba(239, 246, 255, 0.72));
  box-shadow: 0 14rpx 36rpx rgba(15, 23, 42, 0.07), inset 0 1rpx 0 rgba(255, 255, 255, 0.90);
}

.hr-template-card-name {
  color: #1e3a8a;
  font-size: var(--ui-type-section);
  font-weight: 700;
  line-height: 1.4;
}

.hr-template-meta,
.hr-template-description,
.hr-switch-suggestion {
  margin-top: 8rpx;
  color: #64748b;
  font-size: var(--ui-type-label);
  line-height: 1.55;
}

.hr-template-description {
  padding: 18rpx 0 4rpx;
  color: #475569;
}

.hr-template-actions {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 14rpx;
  margin-top: 20rpx;
}

.hr-template-action {
  display: block;
  box-sizing: border-box;
  width: 100%;
  max-width: 100%;
  min-width: 0;
  min-height: 74rpx;
  height: 74rpx;
  padding: 0 16rpx;
  margin: 0;
  border: 1rpx solid rgba(147, 197, 253, 0.72);
  border-radius: 18rpx;
  color: #2563eb;
  font-size: var(--ui-type-meta);
  font-weight: 700;
  line-height: 74rpx;
  white-space: nowrap;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.96), rgba(239, 246, 255, 0.90));
  box-shadow:
    0 12rpx 24rpx rgba(37, 99, 235, 0.09),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.84);
  overflow: hidden;
}

.hr-template-action::after {
  border: 0;
}

.hr-template-action-primary {
  color: #ffffff;
  border-color: rgba(59, 130, 246, 0.72);
  background: linear-gradient(135deg, #2563eb 0%, #3b82f6 58%, #60a5fa 100%);
  box-shadow:
    0 16rpx 32rpx rgba(29, 78, 216, 0.20),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.20);
}

.hr-template-action-danger {
  color: #ffffff;
  border-color: rgba(248, 113, 113, 0.72);
  background: linear-gradient(135deg, #ef4444 0%, #f87171 100%);
  box-shadow:
    0 16rpx 30rpx rgba(239, 68, 68, 0.16),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.18);
}

.hr-snapshot-fields {
  margin: 20rpx 0 24rpx;
  padding: 18rpx;
  border: 1rpx solid rgba(191, 219, 254, 0.72);
  border-radius: 18rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.82), rgba(239, 246, 255, 0.62));
  box-shadow: inset 0 1rpx 0 rgba(255, 255, 255, 0.88);
}

.hr-snapshot-fields-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
  margin-bottom: 12rpx;
}

.hr-snapshot-fields-title {
  color: #1e3a8a;
  font-size: var(--ui-type-body);
  font-weight: 700;
}

.hr-snapshot-field {
  display: flex;
  align-items: center;
  gap: 14rpx;
  padding: 16rpx 12rpx;
  border-top: 1rpx solid rgba(219, 234, 254, 0.82);
}

.hr-snapshot-field-order {
  display: flex;
  flex: 0 0 40rpx;
  align-items: center;
  justify-content: center;
  width: 40rpx;
  height: 40rpx;
  border-radius: 10rpx;
  color: #2563eb;
  font-size: var(--ui-type-caption);
  font-weight: 700;
  background: rgba(219, 234, 254, 0.76);
}

.hr-snapshot-field-main {
  flex: 1;
  min-width: 0;
}

.hr-snapshot-field-name {
  color: #1e293b;
  font-size: var(--ui-type-body);
  font-weight: 600;
  line-height: 1.4;
}

.hr-snapshot-field-rule {
  margin-top: 4rpx;
  color: #64748b;
  font-size: var(--ui-type-caption);
  line-height: 1.45;
  overflow-wrap: anywhere;
}

.hr-snapshot-field-tags {
  display: flex;
  flex: 0 0 auto;
  align-items: center;
  gap: 8rpx;
}

.hr-snapshot-type-tag,
.hr-snapshot-required-tag {
  padding: 6rpx 10rpx;
  border-radius: 10rpx;
  font-size: var(--ui-type-caption);
  font-weight: 700;
  line-height: 1.2;
}

.hr-snapshot-type-tag {
  color: #2563eb;
  border: 1rpx solid rgba(147, 197, 253, 0.64);
  background: rgba(219, 234, 254, 0.76);
}

.hr-snapshot-required-tag {
  color: #c2410c;
  border: 1rpx solid rgba(253, 186, 116, 0.58);
  background: rgba(255, 237, 213, 0.78);
}

.hr-template-add-btn {
  display: block;
  flex: 0 0 auto;
  box-sizing: border-box;
  width: auto;
  min-width: 176rpx;
  min-height: 72rpx;
  height: 72rpx;
  margin: 0;
  padding: 0 22rpx;
  border-radius: 18rpx;
  font-size: var(--ui-type-meta);
  line-height: 72rpx;
  white-space: nowrap;
}

.hr-template-library-header,
.hr-member-list-header {
  align-items: center;
  gap: 18rpx;
}

.hr-profile-export-btn {
  flex: 0 0 auto;
  width: auto;
  min-width: 220rpx;
  min-height: 72rpx;
  height: 72rpx;
  margin: 0 0 0 auto;
  padding: 0 22rpx;
  border-radius: 18rpx;
  font-size: var(--ui-type-label);
  line-height: 72rpx;
  white-space: nowrap;
  justify-self: end;
}

.hr-template-library-header .hr-template-add-btn {
  margin-left: auto;
}

.hr-template-action,
.hr-template-editor-btn,
.hr-template-add-btn,
.hr-profile-export-btn {
  text-align: center;
}

.hr-template-action::after,
.hr-template-editor-btn::after,
.hr-template-add-btn::after,
.hr-profile-export-btn::after {
  border: 0;
}

.hr-member-profile-summary {
  display: -webkit-box;
  margin-top: 8rpx;
  overflow: hidden;
  color: #475569;
  font-size: var(--ui-type-meta);
  line-height: 1.55;
  overflow-wrap: anywhere;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
}

.hr-member-profile-pending {
  color: #c2410c;
}

.hr-template-switch-modal {
  width: calc(100vw - 48rpx);
  max-width: 1080rpx;
  max-height: 1200rpx;
}

.hr-template-switch-body {
  flex: 1;
  min-height: 0;
  padding: 8rpx 0 12rpx;
}

.hr-switch-source {
  padding: 24rpx;
  margin-top: 18rpx;
  border: 1rpx solid rgba(147, 197, 253, 0.26);
  border-radius: 20rpx;
  background: rgba(248, 250, 252, 0.78);
}

.hr-switch-suggestion {
  color: #2563eb;
}

.hr-switch-delete-warning {
  padding: 14rpx 18rpx;
  margin-top: 12rpx;
  border-radius: 14rpx;
  color: #b91c1c;
  font-size: var(--ui-type-label);
  line-height: 1.5;
  background: rgba(254, 226, 226, 0.72);
}

.hr-template-switch-footer {
  flex-shrink: 0;
  padding: 18rpx 0 0;
  border-top: 1rpx solid rgba(148, 163, 184, 0.16);
}

@media (min-width: 520px) {
  .hr-template-card,
  .hr-switch-source {
    padding: 18px;
    border-radius: 16px;
  }

  .hr-template-card-name { font-size: var(--ui-type-emphasis); }
  .hr-template-meta,
  .hr-template-description,
  .hr-switch-suggestion,
  .hr-switch-delete-warning { font-size: var(--ui-type-meta); }

  .hr-template-actions { gap: 8px; }
  .hr-template-editor-actions {
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 10px;
  }

  .hr-template-editor-btn {
    min-height: 44px;
    height: 44px;
    padding: 0 14px;
    border-radius: 12px;
    font-size: var(--ui-type-label);
    line-height: 44px;
  }

  .hr-template-editor-save {
    grid-column: auto;
  }

  .hr-template-actions {
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }

  .hr-template-action {
    min-height: 44px;
    height: 44px;
    padding: 0 12px;
    border-radius: 10px;
    font-size: var(--ui-type-meta);
    line-height: 44px;
  }

  .hr-snapshot-fields {
    margin: 16px 0 18px;
    padding: 14px;
    border-radius: 14px;
  }

  .hr-snapshot-fields-title,
  .hr-snapshot-field-name { font-size: var(--ui-type-label); }
  .hr-snapshot-field-rule,
  .hr-snapshot-type-tag,
  .hr-snapshot-required-tag { font-size: var(--ui-type-caption); }

  .hr-template-add-btn,
  .hr-profile-export-btn {
    min-width: 136px;
    min-height: 44px;
    height: 44px;
    padding: 0 16px;
    border-radius: 12px;
    font-size: var(--ui-type-label);
    line-height: 44px;
  }

  .hr-member-profile-summary {
    font-size: var(--ui-type-meta);
  }

}
.field-help {
  margin-top: 10rpx;
  color: #64748b;
  font-size: var(--ui-type-meta);
  line-height: 1.55;
}

.detail-section-heading,
.assignment-card-title-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18rpx;
}

.compact-action-btn {
  width: auto;
  min-width: 132rpx;
  min-height: 64rpx;
  margin: 0;
  padding: 0 22rpx;
  border-radius: 16rpx;
  font-size: var(--ui-type-control);
  line-height: 1.3;
}

.assignment-list {
  display: flex;
  flex-direction: column;
  gap: 14rpx;
  margin-top: 18rpx;
}

.assignment-card,
.assignment-editor {
  border: 1rpx solid rgba(148, 163, 184, 0.22);
  border-radius: 18rpx;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.90), rgba(244, 248, 255, 0.78));
  box-shadow: inset 0 1rpx 0 rgba(255, 255, 255, 0.78);
}

.assignment-card {
  display: flex;
  flex-direction: column;
  align-items: stretch;
  justify-content: space-between;
  gap: 16rpx;
  padding: 20rpx;
}

.assignment-card-main {
  min-width: 0;
  flex: 1;
}

.assignment-card-title-row {
  justify-content: space-between;
  flex-wrap: wrap;
}

.assignment-kind-label {
  padding: 5rpx 10rpx;
  border-radius: 12rpx;
  background: rgba(226, 232, 240, 0.72);
  border: 1rpx solid rgba(148, 163, 184, 0.18);
  color: #475569;
  font-size: var(--ui-type-caption);
  font-weight: 600;
  line-height: 1.45;
}

.assignment-card-title {
  color: #172033;
  font-size: var(--ui-type-value);
  font-weight: 650;
  line-height: 1.45;
}

.assignment-facts {
  display: grid;
  grid-template-columns: minmax(0, 1fr);
  gap: 10rpx;
  margin-top: 14rpx;
}

.assignment-fact {
  display: grid;
  grid-template-columns: 164rpx minmax(0, 1fr);
  align-items: start;
  gap: 14rpx;
}

.assignment-fact-label {
  color: #64748b;
  font-size: var(--ui-type-caption);
  font-weight: 600;
  line-height: 1.55;
}

.assignment-fact-value {
  min-width: 0;
  color: #1e293b;
  font-size: var(--ui-type-label);
  font-weight: 650;
  line-height: 1.55;
  word-break: break-word;
  overflow-wrap: anywhere;
}

.assignment-actions {
  justify-content: flex-end;
  width: 100%;
  padding-top: 14rpx;
  border-top: 1rpx solid rgba(148, 163, 184, 0.16);
}

.assignment-editor {
  margin-top: 18rpx;
  padding: 22rpx;
}

.assignment-editor-title {
  margin-bottom: 16rpx;
  color: #172033;
  font-size: var(--ui-type-section);
  font-weight: 700;
  line-height: 1.45;
}

.assignment-editor-grid {
  display: grid;
  grid-template-columns: minmax(0, 1fr);
  gap: 0;
}

.assignment-editor-field {
  min-width: 0;
}

@media (min-width: 520px) {
  .hr-person-editor-shell {
    width: 78vw;
    max-width: 760px;
    height: auto;
    max-height: calc(100vh - 32px);
  }

  .detail-body {
    height: 520px;
    max-height: calc(100vh - 176px);
    padding: 0 14px 16px;
  }

  .assignment-editor-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
    column-gap: 16px;
  }

  .assignment-editor-field-wide {
    grid-column: 1 / -1;
  }

  .assignment-facts {
    grid-template-columns: repeat(2, minmax(0, 1fr));
    column-gap: 20px;
  }

  .assignment-fact {
    grid-template-columns: 112px minmax(0, 1fr);
  }
}

@media (min-width: 900px) {
  .detail-body {
    height: 420px;
    max-height: calc(100vh - 160px);
  }
}
.context-switch-guard-card {
  padding: 26rpx;
  text-align: center;
}

.context-switch-guard-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 68rpx;
  height: 68rpx;
  margin: 0 auto 17rpx;
  border: 1rpx solid rgba(147, 197, 253, 0.62);
  border-radius: 20rpx;
  background: linear-gradient(135deg, rgba(219, 234, 254, 0.94), rgba(239, 246, 255, 0.82));
}

.context-switch-guard-title {
  color: #0f172a;
  font-size: var(--ui-type-dialog);
  font-weight: 800;
  line-height: 1.45;
}

.context-switch-guard-desc {
  margin-top: 10rpx;
  color: #64748b;
  font-size: var(--ui-type-body);
  line-height: 1.65;
}

.context-switch-guard-actions {
  margin-top: 20rpx;
  padding: 0;
  border: 0;
  background: transparent;
}

.context-switch-guard-actions button {
  width: 100%;
}

@media (min-width: 520px) {
  .context-switch-guard-card { padding: 24px 26px; }
  .context-switch-guard-icon {
    width: 50px;
    height: 50px;
    margin-bottom: 13px;
    border-radius: 14px;
  }
  .context-switch-guard-desc { margin-top: 8px; }
  .context-switch-guard-actions { margin-top: 16px; }
}

/* 人员详情中的跨组织身份总览：信息分组清晰，操作保持紧凑。 */
.identity-overview-section {
  display: flex;
  flex-direction: column;
  gap: 18rpx;
}

.global-admin-section,
.organization-identity-card {
  padding: 18rpx;
  border: 1rpx solid rgba(148, 163, 184, 0.22);
  border-radius: 16rpx;
  background: rgba(248, 250, 252, 0.78);
}

.global-admin-section {
  border-color: rgba(96, 165, 250, 0.28);
  background: linear-gradient(135deg, rgba(239, 246, 255, 0.88), rgba(255, 255, 255, 0.82));
}

.identity-group-head,
.identity-subsection-head,
.admin-identity-card {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14rpx;
}

.identity-group-heading,
.admin-identity-main {
  min-width: 0;
  flex: 1;
}

.identity-group-title,
.identity-organization-name,
.identity-subsection-title,
.admin-identity-name {
  color: #172033;
  font-weight: 700;
  line-height: 1.45;
  overflow-wrap: anywhere;
}

.identity-group-title,
.identity-organization-name {
  font-size: var(--ui-type-emphasis);
}

.identity-subsection-title,
.admin-identity-name {
  font-size: var(--ui-type-label);
}

.identity-group-note,
.admin-identity-meta {
  margin-top: 4rpx;
  color: #64748b;
  font-size: var(--ui-type-caption);
  line-height: 1.5;
}

.identity-subsection {
  margin-top: 16rpx;
  padding-top: 16rpx;
  border-top: 1rpx solid rgba(148, 163, 184, 0.16);
}

.admin-identity-card {
  margin-top: 12rpx;
  padding: 12rpx 14rpx;
  border: 1rpx solid rgba(191, 219, 254, 0.55);
  border-radius: 12rpx;
  background: rgba(255, 255, 255, 0.74);
}

.compact-empty {
  padding: 14rpx 4rpx 2rpx;
  color: #94a3b8;
  font-size: var(--ui-type-caption);
  line-height: 1.5;
}

.identity-action-confirm-card {
  height: auto !important;
  min-height: 0 !important;
}

.identity-action-confirm-actions {
  margin-top: 18rpx;
}

@media (min-width: 520px) {
  .hr-person-editor-shell {
    width: min(760px, calc(100vw - 36px));
  }

  .detail-body {
    flex: 1;
    height: auto;
    min-height: 0;
    max-height: calc(100vh - 148px);
    padding: 0 12px 12px;
  }

  .identity-overview-section { gap: 12px; }

  .global-admin-section,
  .organization-identity-card {
    padding: 14px;
    border-radius: 12px;
  }

  .identity-subsection {
    margin-top: 12px;
    padding-top: 12px;
  }

  .admin-identity-card {
    margin-top: 8px;
    padding: 8px 10px;
    border-radius: 9px;
  }

  .compact-action-btn {
    min-width: 0 !important;
    min-height: 32px !important;
    padding: 6px 11px !important;
    border-radius: 9px !important;
    font-size: var(--ui-type-meta) !important;
    line-height: 18px !important;
  }

  .assignment-list { gap: 8px; margin-top: 10px; }
  .assignment-card { gap: 10px; padding: 12px; border-radius: 11px; }
  .assignment-facts { gap: 6px; margin-top: 8px; }
  .assignment-actions { padding-top: 8px; }
  .assignment-editor { margin-top: 12px; padding: 14px; border-radius: 12px; }

  .hr-template-card,
  .hr-switch-source {
    padding: 14px;
    border-radius: 12px;
  }

  .hr-template-actions,
  .hr-template-editor-actions { gap: 7px; }

  .hr-template-action,
  .hr-template-editor-btn,
  .hr-template-add-btn,
  .hr-profile-export-btn {
    min-height: 36px !important;
    height: 36px !important;
    padding: 0 12px !important;
    border-radius: 9px !important;
    line-height: 36px !important;
  }

  .hr-snapshot-fields { margin: 12px 0 14px; padding: 10px; border-radius: 10px; }
  .hr-snapshot-field { gap: 8px; padding: 8px 6px; }
  .hr-snapshot-field-order { flex-basis: 26px; width: 26px; height: 26px; border-radius: 7px; }
  .hr-snapshot-type-tag,
  .hr-snapshot-required-tag { padding: 3px 7px; border-radius: 7px; }

  .status-chip,
  .status-tag,
  .hr-template-status {
    min-height: 0 !important;
    padding: 3px 8px !important;
    border-radius: 7px !important;
    line-height: 16px !important;
  }
}

@media (min-width: 900px) {
  .detail-body {
    height: auto;
    max-height: calc(100vh - 136px);
  }
}
.target-scroll--limited {
  max-height: 500rpx;
}

.rule-panel--spaced {
  margin-top: var(--ui-card-gap, 28rpx);
}

.audit-condition-card {
  margin: 0 0 var(--ui-dialog-section-gap, 16rpx);
}

.chip-row--wrap {
  flex-wrap: wrap;
}

.audit-condition-token {
  margin: 2rpx;
}

.ui-dialog-inset {
  padding-left: var(--ui-dialog-padding) !important;
  padding-right: var(--ui-dialog-padding) !important;
  box-sizing: border-box;
}

.audit-section-title {
  margin-top: 20rpx;
}

.audit-condition-row {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  margin-top: 6rpx;
}

.audit-button-row--spaced {
  margin-top: var(--ui-dialog-section-gap, 16rpx);
}

.audit-template-toggle {
  display: flex;
  justify-content: space-between;
  width: 100%;
  min-width: 0;
}

.audit-template-actions {
  position: absolute;
  top: 16rpx;
  right: 80rpx;
}

.audit-step-index {
  color: #0f172a;
  font-size: var(--ui-type-body);
  font-weight: 700;
}

.audit-step-action {
  margin-left: 8rpx;
  color: #64748b;
  font-size: var(--ui-type-caption);
}

.audit-resolved-condition-row {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  margin-left: 12rpx;
}

.audit-empty-muted {
  color: #9ca3af;
  font-size: var(--ui-type-meta);
}

.audit-step-condition-editor--inset {
  margin: 0 16rpx var(--ui-dialog-section-gap, 16rpx);
}

.danger-clear-link {
  color: #ef4444;
  font-size: var(--ui-type-meta);
}

.danger-clear-link--inline {
  margin-left: 8rpx;
}

.audit-stamp-preview--large {
  width: 200rpx;
}

.audit-stamp-preview--small {
  width: 160rpx;
}

.compact-status-picker {
  min-height: var(--ui-compact-height);
  padding: 0 18rpx;
  border-radius: var(--ui-list-radius);
  font-size: var(--ui-type-meta);
}

.audit-file-section-title {
  margin-top: 8rpx;
}

.file-attach-item--spaced {
  margin-bottom: 6rpx;
}

.admin-section--spaced {
  margin-top: var(--ui-card-gap, 24rpx);
}

.verification-file-result {
  margin-top: 8rpx;
}

.page-actions {
  padding: 28rpx 8rpx 12rpx;
}

.ui-dialog-filter-row {
  padding: 0 var(--ui-dialog-padding) var(--ui-dialog-section-gap);
}

.rule-filter-field--grow {
  flex: 1 1 0;
  min-width: 0;
}

.ui-dialog-compact-actions {
  padding: 0 var(--ui-dialog-padding) 8rpx;
}

.ui-dialog-help-text {
  padding: 0 var(--ui-dialog-padding) var(--ui-dialog-section-gap);
  color: #6b7280;
}

@media (min-width: 520px) {
  .target-scroll--limited {
    max-height: 360px;
  }

  .audit-condition-token {
    margin: 2px;
  }

  .audit-section-title {
    margin-top: 14px;
  }

  .audit-condition-row {
    margin-top: 5px;
  }

  .audit-template-actions {
    top: 12px;
    right: 56px;
  }

  .audit-step-action,
  .danger-clear-link--inline {
    margin-left: 6px;
  }

  .audit-resolved-condition-row {
    margin-left: 8px;
  }

  .audit-step-condition-editor--inset {
    margin: 0 12px var(--ui-dialog-section-gap);
  }

  .audit-stamp-preview--large {
    width: 150px;
  }

  .audit-stamp-preview--small {
    width: 120px;
  }

  .compact-status-picker {
    padding: 0 12px;
  }

  .audit-file-section-title {
    margin-top: 6px;
  }

  .file-attach-item--spaced {
    margin-bottom: 5px;
  }

  .verification-file-result {
    margin-top: 6px;
  }

  .page-actions {
    padding: 18px 6px 10px;
  }

  .ui-dialog-compact-actions {
    padding-bottom: 6px;
  }
}

@media (min-width: 900px) and (orientation: landscape) {
  .target-scroll--limited {
    max-height: 300px;
  }

  .audit-condition-card {
    margin-bottom: 10px;
  }

  .audit-section-title {
    margin-top: 10px;
  }

  .audit-step-condition-editor--inset {
    margin: 0 10px 10px;
  }

  .page-actions {
    padding: 12px 6px 8px;
  }
}
