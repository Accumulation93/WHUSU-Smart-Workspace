<page-meta page-style="{{adHocStepEditorVisible || editStepEditorVisible || editPersonPickerVisible || stampPickerVisible || placementVisible || approvalVisible || sigSourcePickerVisible || signaturePadVisible || personPickerVisible ? 'overflow: hidden;' : ''}}"></page-meta>
<view class="page subapp-audit-page">
  <workspace-hero class="workspace-hero-host" app-name="{{localeCopy.copy_aa4f0ed6cd}}" page-name="{{action === 'create' ? localeCopy.copy_67587e6612 : localeCopy.copy_2d40e10806}}"></workspace-hero>

  <!-- ═══════════ Create Mode ═══════════ -->
  <view class="section-stack" wx:if="{{action === 'create'}}">
    <view class="card">
      <view class="section-title">{{localeCopy.copy_3fb5aace39}}</view>
      <view class="button-row">
        <button class="{{createMode === 'template' ? 'primary-btn' : 'secondary-btn'}} ui-motion-button" data-mode="template" bindtap="onCreateModeChange" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_15b653039c}}</button>
        <button class="{{createMode === 'ad_hoc' ? 'primary-btn' : 'secondary-btn'}} ui-motion-button" data-mode="ad_hoc" bindtap="onCreateModeChange" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_f49717b207}}</button>
      </view>
    </view>

    <!-- Template mode -->
    <view class="card" wx:if="{{createMode === 'template'}}">
      <view class="section-title">{{localeCopy.copy_091dc4f11f}}</view>
      <scroll-view show-scrollbar="{{true}}" bounces="{{true}}" class="inner-scroll small-scroll" scroll-y>
        <view class="template-select-item {{selectedTemplateId === item.id ? 'template-select-item-selected' : ''}} ui-motion-card" wx:for="{{flowTemplates}}" wx:key="id" data-id="{{item.id}}" catchtap="onTemplateSelect" hover-class="ui-press-card" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">
          <view class="radio-dot {{selectedTemplateId === item.id ? 'radio-dot-checked' : ''}}"></view>
          <view class="list-main">
            <view class="list-name">{{item.name}} <text class="list-desc">({{item.stepCount}}{{localeCopy.copy_9e545997b0}} · {{item.resubmitMode === 'fresh' ? localeCopy.copy_bd83f7065b : localeCopy.copy_2313ec3016}})</text></view>
            <view class="list-desc" wx:if="{{item.description}}">{{item.description}}</view>
          </view>
        </view>
        <view class="audit-empty-state" wx:if="{{!flowTemplates.length}}">
          <view class="audit-empty-icon"><ui-icon name="file" tone="muted" size="54" ariaLabel="{{localeCopy.copy_aee302ae49}}" /></view>
          <view class="audit-empty-text">{{localeCopy.copy_ca5c78dc29}}</view>
        </view>
      </scroll-view>
    </view>

    <!-- Ad-hoc mode -->
    <view class="card" wx:if="{{createMode === 'ad_hoc'}}">
      <view class="section-title">{{localeCopy.copy_265568fe85}} ({{adHocSteps.length}}{{localeCopy.copy_ef0bb32ca6}}</view>
      <view class="ad-hoc-step-item" wx:for="{{adHocSteps}}" wx:key="index">
        <view class="ad-hoc-step-main">
          <view class="ad-hoc-step-title">{{item.name || (localeCopy.copy_93c50c01c0 + (index + 1) + localeCopy.copy_493a127a99)}}：{{item.approverType === 'specific_person' ? (item.approverHrName || localeCopy.copy_5fff626dd6) : (item.approverIdentityName || localeCopy.copy_c76fae0e08) + '（' + (item.scopeType === 'same_department' ? localeCopy.copy_37ad645951 : (item.scopeType === 'same_work_group' ? localeCopy.copy_2c67faf1c7 : (item.scopeType === 'specific_department' ? (item.scopeDepartmentName || localeCopy.copy_b3604f443f) : (item.scopeType === 'specific_work_group' ? (item.scopeDepartmentName || '') + (item.scopeWorkGroupName ? '·' + item.scopeWorkGroupName : '') : localeCopy.copy_9b3c1f7a01)))) + '）'}} — {{item.actionType === 'pass' ? localeCopy.copy_8e2f75159e : (item.actionType === 'sign' ? localeCopy.copy_49cbf30d6b : (item.actionType === 'estamp' ? localeCopy.copy_7e6630535d : localeCopy.copy_a63d02480e))}}</view>
          <view class="ad-hoc-step-desc">{{item.approverType === 'identity' ? localeCopy.copy_fc2ce54999 : localeCopy.copy_e31ec452dd}}</view>
        </view>
        <view class="list-actions card-actions card-actions--inline"><text class="audit-link-btn audit-link-danger ui-motion-chip" data-index="{{index}}" bindtap="removeAdHocStep" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_c3e182f0c6}}</text></view>
      </view>
      <view class="audit-empty-state" wx:if="{{!adHocSteps.length}}">
        <view class="audit-empty-text">{{localeCopy.copy_84e125402d}}</view>
      </view>
      <view class="button-row compact-row"><button class="secondary-btn ui-motion-button" bindtap="openAdHocStepEditor" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_751c159dd2}}</button><button class="secondary-btn ui-motion-button" bindtap="openPersonPicker" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_0b61c482f3}}</button></view>
      <view class="field field-mt-16">
        <view class="field-label">{{localeCopy.copy_8d3aac9d85}}</view>
        <picker range="{{[localeCopy.copy_3cd3d7a8c2,localeCopy.copy_7897981e63]}}" bindchange="onResubmitModeChange">
          <view class="picker-value">{{resubmitMode === 'fresh' ? localeCopy.copy_3cd3d7a8c2 : localeCopy.copy_7897981e63}}</view>
        </picker>
      </view>
    </view>

    <!-- Template step preview (person override) -->
    <view class="card" wx:if="{{createMode === 'template' && selectedTemplateId && templatePreviewSteps.length}}">
      <view class="section-title">{{localeCopy.copy_9c898a71af}}{{templatePreviewSteps.length}}{{localeCopy.copy_16d27aa59e}}</view>
      <view class="section-desc section-desc-sm">{{localeCopy.copy_34f5bab454}}</view>
      <view class="tpl-step-preview-item" wx:for="{{templatePreviewSteps}}" wx:key="stepIndex">
        <view class="tpl-step-preview-main">
          <view class="tpl-step-preview-num">{{item.name || (localeCopy.copy_93c50c01c0 + (item.sortOrder || (index + 1)) + localeCopy.copy_493a127a99)}}</view>
          <view class="tpl-step-preview-desc">{{item.approverDesc || localeCopy.copy_ae42f47cf6}}</view>
          <view class="tpl-step-preview-action">{{item.actionLabel || localeCopy.copy_c9695bb971}}</view>
          <view class="tpl-step-preview-designation" wx:if="{{item.stepIndex === 1 && item.allowApproverDesignation}}">
            <text class="audit-link-btn ui-motion-chip" catchtap="openTemplateStepPersonPicker" data-step-index="1" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{templateStepOverrides.length && templateStepOverrides[0].personHrIds.length ? localeCopy.copy_d5fa17c99b : localeCopy.copy_2aad29e5cf}}</text>
            <view wx:if="{{templateStepOverrides.length && templateStepOverrides[0].personHrNames.length}}" class="tpl-step-preview-persons">
              <view class="tpl-step-person-tag" wx:for="{{templateStepOverrides[0].personHrNames}}" wx:for-item="pname" wx:for-index="pidx" wx:key="pname">
                <text>{{pname}}</text>
                <text class="tpl-step-person-remove ui-motion-chip" data-step-index="1" data-hr-id="{{templateStepOverrides[0].personHrIds[pidx]}}" catchtap="removeTemplateStepOverridePerson" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">×</text>
              </view>
            </view>
          </view>
        </view>
        <view class="tpl-step-preview-mode" wx:if="{{!(item.stepIndex === 1 && item.allowApproverDesignation)}}">
          <text class="tpl-step-fixed-mode">{{localeCopy.copy_46a814a2c4}}</text>
        </view>
      </view>
    </view>

    <!-- Common: Title + Files -->
    <view class="card">
      <view class="section-title">{{localeCopy.copy_66240d9d99}}</view>
      <view class="field">
        <view class="field-label">{{localeCopy.copy_30f93d24ca}}</view>
        <input class="field-input" value="{{createTitle}}" bindinput="onTitleInput" placeholder="{{localeCopy.copy_3ebd5d2cf0}}" />
      </view>
      <view class="field">
        <view class="field-label">{{localeCopy.copy_25e1199dc3}}</view>
        <textarea class="field-textarea" auto-height maxlength="-1" value="{{createDesc}}" bindinput="onDescInput" placeholder="{{localeCopy.copy_3234735724}}" ></textarea>
      </view>
      <view class="section-title field-mt-16">{{localeCopy.copy_d036aa723a}} ({{uploadedFiles.length}}{{localeCopy.copy_f34e9d1f5c}}</view>
      <view class="file-attach-item" wx:for="{{uploadedFiles}}" wx:key="fileId">
        <view class="file-attach-icon"><ui-icon name="file" tone="primary" size="24" ariaLabel="{{localeCopy.copy_d036aa723a}}" /></view>
        <view class="file-attach-name">{{item.fileName}}</view>
        <view class="list-actions card-actions card-actions--inline"><text class="audit-link-btn audit-link-danger ui-motion-chip" data-index="{{index}}" bindtap="removeUploadedFile" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_c3e182f0c6}}</text></view>
      </view>
      <view class="button-row compact-row">
        <button class="secondary-btn ui-motion-button" bindtap="chooseImage" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_53ce695b90}}</button>
        <button class="secondary-btn ui-motion-button" bindtap="chooseFile" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_98aea69c53}}</button>
      </view>
      <view class="audit-btn-bar">
        <button class="primary-btn ui-motion-button" loading="{{loading}}" bindtap="submitAudit" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_c07097f6db}}</button>
      </view>
    </view>

    <!-- ═══ Identity step editor popup ═══ -->
    <viewport-portal wx:if="{{adHocStepEditorVisible}}">
    <view class="popup-mask ui-overlay" wx:if="{{adHocStepEditorVisible}}" bindtap="closeAdHocStepEditor">
      <view class="ui-overlay-blocker" catchtouchmove="noop"></view>
      <view class="popup-card ui-dialog-shell ui-dialog-shell--complex" catchtap="noop">
        <view class="popup-header ui-dialog-header"><view class="popup-title">{{localeCopy.copy_7963fb24a5}} — {{localeCopy.copy_6cd86bd7ed}}</view><text class="popup-close ui-motion-chip" catchtap="closeAdHocStepEditor" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_09614cef6c}}</text></view>
        <scroll-view enhanced="{{true}}" show-scrollbar="{{true}}" bounces="{{true}}" nested-scroll-enabled="{{true}}" class="popup-scroll ui-dialog-body ui-dialog-content ui-dialog-scroll--fill" scroll-y>
          <view class="detail-popup-form ui-dialog-stack">
            <view class="field">
              <view class="field-label">{{localeCopy.copy_c5dd070c37}}</view>
              <input class="field-input" value="{{adHocStepForm.name}}" data-field="name" bindinput="onAdHocStepField" placeholder="{{localeCopy.copy_ac4d7a45e0}}"/>
            </view>
            <view class="field">
              <view class="field-label">{{localeCopy.copy_dc449bac28}}</view>
              <picker range="{{[localeCopy.copy_8e2f75159e,localeCopy.copy_49cbf30d6b,localeCopy.copy_7e6630535d,localeCopy.copy_a63d02480e]}}" bindchange="onAdHocActionTypeChange">
                <view class="picker-value">{{adHocStepForm.actionType === 'pass' ? localeCopy.copy_8e2f75159e : (adHocStepForm.actionType === 'sign' ? localeCopy.copy_49cbf30d6b : (adHocStepForm.actionType === 'estamp' ? localeCopy.copy_7e6630535d : localeCopy.copy_a63d02480e))}}</view>
              </picker>
            </view>
            <view class="field">
              <view class="field-label">{{localeCopy.copy_8f5c5f658c}}</view>
              <picker range="{{identityPickerScopeOptions}}" bindchange="onIdentityScopeChange">
                <view class="picker-value">{{identityPickerScopeOptions[identityPickerScopeIndex] || localeCopy.copy_76d431a4dc}}</view>
              </picker>
            </view>
            <view class="field" wx:if="{{identityPickerScopeValues[identityPickerScopeIndex] === 'specific_department' || identityPickerScopeValues[identityPickerScopeIndex] === 'specific_work_group'}}">
              <view class="field-label">{{localeCopy.copy_f1643742d1}}</view>
              <picker range="{{identityPickerDeptOptions}}" bindchange="onIdentityDeptChange">
                <view class="picker-value">{{identityPickerDeptOptions[identityPickerDeptIndex] || localeCopy.copy_eada426deb}}</view>
              </picker>
            </view>
            <view class="field" wx:if="{{identityPickerScopeValues[identityPickerScopeIndex] === 'specific_work_group'}}">
              <view class="field-label">{{localeCopy.copy_237707b205}}</view>
              <picker range="{{identityPickerWgOptions}}" bindchange="onIdentityWgChange">
                <view class="picker-value">{{identityPickerWgOptions[identityPickerWgIndex] || localeCopy.copy_ec3b03ecc7}}</view>
              </picker>
            </view>
            <view class="field">
              <view class="field-label">{{localeCopy.copy_4711f10351}}</view>
              <picker range="{{identityPickerIdentOptions}}" bindchange="onIdentityIdentChange">
                <view class="picker-value">{{identityPickerIdentOptions[identityPickerIdentIndex] || localeCopy.copy_d1856227b6}}</view>
              </picker>
            </view>
          </view>
        </scroll-view>
        <view class="audit-btn-bar ui-dialog-footer"><button class="primary-btn ui-motion-button" catchtap="confirmAdHocStep" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_a58e97a9f8}}</button></view>
      </view>
    </view>
    </viewport-portal>

  </view>

  <!-- ═══════════ View Mode ═══════════ -->
  <view class="section-stack" wx:if="{{action === 'view'}}">
    <view class="card-loading-overlay" wx:if="{{loading}}"><view class="card-loading-spinner"></view></view>

    <block wx:if="{{!loading && submission}}">
      <!-- Status -->
      <view class="card">
        <view class="info-grid">
          <view class="info-block">
            <view class="info-label">{{localeCopy.copy_8aee737e64}}</view>
            <view class="info-value">
              <view class="detail-status-chip {{submission.status === 'approved' ? 'detail-status-approved' : (submission.status === 'rejected' ? 'detail-status-rejected' : (submission.status === 'in_progress' ? 'detail-status-progress' : (submission.status === 'draft' ? 'detail-status-draft' : (submission.status === 'pending' ? 'detail-status-pending' : (submission.status === 'withdrawn' ? 'detail-status-withdrawn' : 'detail-status-draft')))))}}">
                {{submission.status === 'draft' ? localeCopy.copy_f6afc42806 : (submission.status === 'pending' ? localeCopy.copy_57b008f8c7 : (submission.status === 'in_progress' ? localeCopy.copy_0dc99cac16 : (submission.status === 'rejected' ? localeCopy.copy_5d5af942c5 : (submission.status === 'approved' ? localeCopy.copy_2220286f1c : localeCopy.copy_282e15e226))))}}
              </view>
            </view>
          </view>
          <view class="info-block">
            <view class="info-label">{{localeCopy.copy_ab4e14a283}}</view>
            <view class="info-value">{{submission.type === 'template' ? localeCopy.copy_600e8c5df8 : localeCopy.copy_0fd9f5f9e8}}</view>
          </view>
          <view class="info-block info-block-full" wx:if="{{submission.templateName}}">
            <view class="info-label">{{localeCopy.copy_e1b8f22129}}</view>
            <view class="info-value">{{submission.templateName}}</view>
          </view>
        </view>
        <!-- Status context hint — 6 states clearly explained -->
        <view class="status-hint-box">
          <view class="status-hint-icon">
            <ui-icon
              name="{{submission.status === 'approved' ? 'check' : (submission.status === 'rejected' ? 'x' : (submission.status === 'draft' ? 'edit' : 'clock'))}}"
              tone="{{submission.status === 'approved' ? 'primary' : (submission.status === 'rejected' ? 'danger' : 'muted')}}"
              size="22"
              ariaLabel="{{localeCopy.copy_9ecea0b582}}"
            />
          </view>
          <view class="status-hint-copy">
            <block wx:if="{{submission.status === 'draft'}}">{{localeCopy.copy_f90d5fcc1f}}</block>
            <block wx:elif="{{submission.status === 'pending'}}">{{localeCopy.copy_19e33b3f68}}</block>
            <block wx:elif="{{submission.status === 'in_progress'}}">{{localeCopy.copy_3714fd6353}}{{submission.currentStepIndex || 1}}{{localeCopy.copy_f600823615}}</block>
            <block wx:elif="{{submission.status === 'rejected'}}">{{localeCopy.copy_ef8b7ffa9b}}{{submission.previousRejectStepIndex || '?'}}{{localeCopy.copy_f69a5ce118}}</block>
            <block wx:elif="{{submission.status === 'approved'}}">{{localeCopy.copy_5477363bc5}}</block>
            <block wx:elif="{{submission.status === 'withdrawn'}}">{{localeCopy.copy_1860c2d121}}</block>
          </view>
        </view>
      </view>

      <!-- ═══════════ Active Approval Card (shown when current user can approve) ═══════════ -->
      <view class="card approval-card" wx:if="{{!editMode && submission.status === 'in_progress' && activeApprovalStepId && userIsApprover}}">
        <view class="section-title approval-card-title">{{localeCopy.copy_7bc6908a17}}</view>
        <view class="approval-step-info">
          {{activeApprovalStep.stepName || (localeCopy.copy_93c50c01c0 + activeApprovalStep.sortOrder + localeCopy.copy_493a127a99)}} · {{activeApprovalStep.actionLabel}}
          <text wx:if="{{activeApprovalStep.round > 1}}" class="approval-round-note">{{localeCopy.copy_bdabcbc8ff}}{{activeApprovalStep.round}}{{localeCopy.copy_d03f1546e9}}</text>
        </view>
        <view class="approval-step-desc">{{activeApprovalStep.approverDesc}}</view>

        <!-- Signature / Stamp section (hidden for pass-only steps) -->
        <view wx:if="{{activeApprovalStep.actionType !== 'pass'}}" class="approval-sig-section">
          <view class="approval-sig-header">
            {{activeApprovalStep.actionType === 'both' ? localeCopy.copy_7f3502c81e : (activeApprovalStep.actionType === 'sign' ? localeCopy.copy_0c02992813 : localeCopy.copy_7e6630535d)}}
          </view>

          <!-- File list -->
          <view wx:for="{{files}}" wx:key="id" wx:for-item="f" class="approval-file-item">
            <view class="approval-file-row">
              <view class="approval-file-name"><ui-icon name="file" tone="muted" size="20" ariaLabel="{{localeCopy.copy_d036aa723a}}" /><text>{{f.fileName}}</text></view>
              <view class="approval-file-actions">
                <text class="approval-file-btn-preview ui-motion-chip"
                      data-file-id="{{f.id}}" data-file-name="{{f.fileName}}"
                      bindtap="previewFile" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_332bc9211a}}</text>
                <text wx:if="{{activeApprovalStep.actionType === 'sign' || activeApprovalStep.actionType === 'both'}}"
                      class="approval-file-btn-sign ui-motion-chip"
                      data-file-id="{{f.id}}" data-file-name="{{f.fileName}}"
                      bindtap="addSignatureForFile" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_76dac19a0a}}</text>
                <text wx:if="{{activeApprovalStep.actionType === 'estamp' || activeApprovalStep.actionType === 'both'}}"
                      class="approval-file-btn-stamp ui-motion-chip"
                      data-file-id="{{f.id}}" data-file-name="{{f.fileName}}"
                      bindtap="addStampForFile" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_31d6da5144}}</text>
              </view>
            </view>

            <!-- Current sigs/stamps on this file -->
            <view wx:for="{{pendingSignatures}}" wx:for-item="sig" wx:key="_idx" wx:if="{{sig.fileId === f.id}}"
                  class="approval-sig-item">
              <view class="approval-sig-info">
                <image wx:if="{{sig.imageData}}" src="{{sig.imageData}}" mode="aspectFit" class="approval-sig-thumb" />
                <text class="approval-sig-name">{{sig.signatureType === 'stamp' ? (sig.stampName || localeCopy.copy_ed981b1744) : localeCopy.copy_0c02992813}}</text>
                <text class="approval-sig-pos">{{sig.posText || localeCopy.copy_ee81f5d088}}</text>
              </view>
              <view class="approval-sig-actions">
                <text class="approval-sig-action-adjust ui-motion-chip" data-sig-idx="{{index}}" bindtap="openPlacement" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_0887668fa5}}</text>
                <text class="approval-sig-action-remove ui-motion-chip" data-sig-idx="{{index}}" bindtap="removePendingSign" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_c3e182f0c6}}</text>
              </view>
            </view>
          </view>

          <!-- Warning for incomplete sign+stamp -->
          <view wx:if="{{activeApprovalStep.actionType === 'both' && approvalWarning}}"
                class="approval-warning">
            {{approvalWarning}}
          </view>
        </view>

        <!-- Approval comment -->
        <view class="approval-field">
          <view class="approval-field-label">{{localeCopy.copy_12555b2518}}</view>
          <textarea class="approval-textarea" auto-height maxlength="-1" value="{{approvalComment}}" bindinput="onApprovalCommentInput" placeholder="{{localeCopy.copy_79e3568eee}}"></textarea>
        </view>

        <!-- Rejection reason -->
        <view class="approval-field">
          <view class="approval-field-label">{{localeCopy.copy_47a12cc974}}</view>
          <textarea class="approval-textarea" auto-height maxlength="-1" value="{{rejectionReason}}" bindinput="onRejectionReasonInput" placeholder="{{localeCopy.copy_cc26adb7da}}"></textarea>
        </view>

        <!-- Designate next-step approvers (optional) -->
        <view class="approval-designate-row" wx:if="{{nextStepInfo && nextStepInfo.allowApproverDesignation}}">
          <view class="approval-designate-header">
            <view class="approval-designate-label">{{localeCopy.copy_3fc6899daa}}</view>
            <text class="audit-link-btn approval-designate-link ui-motion-chip" bindtap="openDesignateNextPersonPicker" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{designatedNextPersons.length ? localeCopy.copy_00e277a673 : localeCopy.copy_c47f370afa}}</text>
          </view>
          <view wx:if="{{!designatedNextPersons.length}}" class="approval-designate-empty">{{localeCopy.copy_0b9e83ebb1}}</view>
          <view wx:for="{{designatedNextPersons}}" wx:key="id" class="approval-designate-tag">
            {{item.name}}
            <text data-hr-id="{{item.id}}" bindtap="removeDesignatedNextPerson" class="approval-designate-remove ui-motion-chip" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">×</text>
          </view>
          <view wx:if="{{nextStepInfo}}" class="approval-next-step-info">{{localeCopy.copy_d5b151752b}}{{nextStepInfo.sortOrder}}{{localeCopy.copy_9e545997b0}} · {{nextStepInfo.approverDesc}}</view>
        </view>

        <!-- Action buttons -->
        <view class="button-row">
          <button class="primary-btn approval-btn-approve ui-motion-button" loading="{{loading}}" bindtap="confirmApprovalDirect" data-action="approve" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">✓ {{localeCopy.copy_1bb7eb3e1a}}</button>
          <button class="secondary-btn danger-btn approval-btn-reject ui-motion-button" loading="{{loading}}" bindtap="confirmApprovalDirect" data-action="reject" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">✗ {{localeCopy.copy_0c90eb0204}}</button>
        </view>
      </view>

      <!-- Flow Visualization -->
      <view class="card">
        <view class="section-title">{{localeCopy.copy_3bc010171a}}</view>

        <!-- Flow progress summary -->
        <view class="audit-flow-summary">
          <text class="flow-summary-text">{{localeCopy.copy_94988c2510}}</text>
          <view class="audit-flow-progress-bar">
            <view class="audit-flow-progress-fill" style="width:{{flowProgressPercent}}%;"></view>
          </view>
          <text class="audit-flow-progress-text">{{flowProgressText}}</text>
        </view>

        <!-- Flow timeline — lifecycle events + approval steps -->
        <view class="audit-flow" wx:if="{{flowTimeline.length}}">
          <block wx:for="{{flowTimeline}}" wx:key="_key" wx:for-item="node">

            <!-- ── Remaining steps separator ── -->
            <view class="flow-separator" wx:if="{{node.type === 'separator'}}">
              <view class="flow-separator-dash"></view>
              <text class="flow-separator-text">{{node.label}}</text>
              <view class="flow-separator-dash"></view>
            </view>

            <!-- ── Lifecycle event node (tappable to expand) ── -->
            <view class="flow-node flow-node-lifecycle {{expandedNodeKey === node._key ? 'flow-node-expanded' : ''}} ui-motion-chip" wx:if="{{node.type === 'lifecycle'}}" bindtap="toggleFlowNode" data-node-key="{{node._key}}" hover-class="ui-press-chip" hover-stay-time="160" hover-start-time="0" hover-stop-propagation="{{true}}">
              <view class="flow-left-col">
                <view class="flow-dot-lifecycle-icon"><ui-icon name="{{node.iconName || 'clock'}}" tone="{{node.event === 'reject' ? 'danger' : 'primary'}}" size="20" ariaLabel="{{node.label}}" /></view>
                <view class="flow-connector flow-connector-lifecycle"></view>
              </view>
              <view class="flow-info flow-info-lifecycle {{expandedNodeKey === node._key ? 'flow-info-expanded' : ''}}">
                <view class="flow-lifecycle-label">{{node.label}}<text class="flow-lifecycle-round" wx:if="{{node.subLabel}}"> {{node.subLabel}}</text></view>
                <view class="flow-lifecycle-time" wx:if="{{node.time}}">{{node.time}}</view>
                <!-- Expand indicator -->
                <view class="flow-expand-hint">{{expandedNodeKey === node._key ? localeCopy.copy_4edcf754ff : localeCopy.copy_baec508307}}</view>
                <!-- Expanded detail -->
                <view wx:if="{{expandedNodeKey === node._key}}" class="flow-expand-detail">
                  <view class="flow-detail-text">
                    <text class="flow-detail-label">{{localeCopy.copy_253aca0bd3}}</text>{{node.operatorName || localeCopy.copy_b121b80801}}
                  </view>
                  <view wx:if="{{node.comment}}" class="flow-detail-text flow-detail-comment-text">
                    <text class="flow-detail-label">{{node.event === 'approve' ? localeCopy.copy_4161e513f6 : (node.event === 'reject' ? localeCopy.copy_5564430f9c : localeCopy.copy_b141bec1f6)}}</text>{{node.comment}}
                  </view>
                  <view wx:if="{{node.time}}" class="flow-detail-meta">{{localeCopy.copy_ca1c2400cc}}{{node.time}}</view>
                </view>
                <!-- Collapsed: show comment inline for approve/reject -->
                <view wx:if="{{expandedNodeKey !== node._key && node.comment && (node.event === 'approve' || node.event === 'reject')}}" class="flow-collapsed-comment">{{node.event === 'approve' ? localeCopy.copy_4161e513f6 : localeCopy.copy_e1af2c13f5}}{{node.comment}}</view>
              </view>
            </view>

            <!-- ── Approval step node (tappable to expand) ── -->
            <view class="flow-node {{node.flowNodeClass}} {{expandedNodeKey === node._key ? 'flow-node-expanded' : ''}} ui-motion-chip" wx:if="{{node.type === 'step'}}" bindtap="toggleFlowNode" data-node-key="{{node._key}}" hover-class="ui-press-chip" hover-stay-time="160" hover-start-time="0" hover-stop-propagation="{{true}}">
              <view class="flow-left-col">
                <view class="flow-dot {{node.flowDotClass}}">
                  <block wx:if="{{node.flowIcon === 'check'}}">✓</block>
                  <block wx:elif="{{node.flowIcon === 'cross'}}">✗</block>
                  <block wx:else>{{node.sortOrder}}</block>
                </view>
                <view class="flow-connector"></view>
              </view>
              <view class="flow-info {{expandedNodeKey === node._key ? 'flow-info-expanded' : ''}}">
                <!-- Collapsed: key info only -->
                <view class="flow-step-label">{{node.stepName || (localeCopy.copy_93c50c01c0 + (node.sortOrder || '?') + localeCopy.copy_493a127a99)}} · {{node.actionLabel || localeCopy.copy_c9695bb971}}<text class="flow-round-badge" wx:if="{{node.round > 1}}">{{localeCopy.copy_ef8b7ffa9b}}{{node.round}}{{localeCopy.copy_29f6c07d3f}}</text></view>
                <view class="flow-step-approver">{{node.approverDesc || localeCopy.copy_ae42f47cf6}}</view>
                <view class="flow-step-status-tag {{node.flowTagClass}}">{{node.flowStatusLabel}}</view>
                <!-- Always show approver name + time if processed -->
                <view class="flow-step-meta" wx:if="{{node.flowIcon === 'check' || node.flowIcon === 'cross'}}">
                  {{node.operatorName || node.approverName || localeCopy.copy_7043c094bd}} · {{node.processedAt || ''}}
                </view>
                <!-- Expand indicator -->
                <view class="flow-expand-hint">{{expandedNodeKey === node._key ? localeCopy.copy_4edcf754ff : localeCopy.copy_baec508307}}</view>

                <!-- Expanded detail section -->
                <view wx:if="{{expandedNodeKey === node._key}}" class="flow-expand-detail">
                  <!-- Processed info -->
                  <view wx:if="{{node.flowIcon === 'check' || node.flowIcon === 'cross'}}" class="flow-detail-processed-text">
                    <text class="flow-detail-label">{{localeCopy.copy_1fac292fcb}}</text>{{node.operatorName || node.approverName || localeCopy.copy_7043c094bd}}
                  </view>
                  <view wx:if="{{node.flowIcon === 'check' || node.flowIcon === 'cross'}}" class="flow-detail-processed-meta">
                    {{localeCopy.copy_aa5c293ca5}}{{node.processedAt || ''}}
                  </view>
                  <!-- Approval comment -->
                  <view wx:if="{{node.comment && node.flowIcon === 'check'}}" class="flow-detail-comment">
                    <text class="flow-detail-label">{{localeCopy.copy_3b3b392755}}</text>{{node.comment}}
                  </view>
                  <!-- Rejection reason -->
                  <view wx:if="{{node.rejectionReason}}" class="flow-detail-reject">
                    <text class="flow-detail-label">{{localeCopy.copy_edbf7136ff}}</text>{{node.rejectionReason}}
                  </view>
                  <!-- Conditions detail (multi-condition OR logic) -->
                  <view wx:if="{{node.conditionsDisplay && node.conditionsDisplay.length}}" class="flow-detail-conditions">
                    <view class="flow-detail-conditions-label">{{localeCopy.copy_fbdf6b37eb}}</view>
                    <view wx:for="{{node.conditionsDisplay}}" wx:for-item="condLine" wx:key="*this" class="flow-detail-cond-line">{{condLine}}</view>
                  </view>
                  <!-- Round info -->
                  <view wx:if="{{node.round > 1}}" class="flow-detail-round">{{localeCopy.copy_ef8b7ffa9b}}{{node.round}}{{localeCopy.copy_eeb3a23b87}}</view>
                </view>

                <!-- Approval action buttons (only for active step + authorized user) -->
                <view class="flow-actions" wx:if="{{node.flowNodeClass === 'flow-node-active' && userIsApprover}}">
                  <text class="flow-action-btn ui-motion-chip" data-step-id="{{node.id}}" catchtap="openApprove" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">✓ {{localeCopy.copy_1bb7eb3e1a}}</text>
                  <text class="flow-action-btn flow-action-danger ui-motion-chip" data-step-id="{{node.id}}" catchtap="openReject" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">✗ {{localeCopy.copy_0c90eb0204}}</text>
                </view>
              </view>
            </view>

          </block>
        </view>
        <!-- Diagnostic: step count from server -->
        <view wx:if="{{rawStepCount === 0 && submission.status !== 'draft' && submission.status !== 'pending'}}" class="flow-diagnostic">
          <text class="flow-diagnostic-text">{{localeCopy.copy_84e125402d}}</text>
        </view>

        <!-- Empty state for draft/pending with no events -->
        <view wx:if="{{!flowTimeline.length && (submission.status === 'draft' || submission.status === 'pending')}}" class="flow-empty-draft">
          <text class="flow-empty-draft-text">{{submission.status === 'draft' ? localeCopy.copy_c363ddef77 : localeCopy.copy_14ba55edf3}}</text>
        </view>

        <!-- Empty state when no timeline -->
        <view wx:if="{{!flowTimeline.length}}" class="audit-empty-state">
          <view class="audit-empty-icon"><ui-icon name="list" tone="muted" size="54" ariaLabel="{{localeCopy.copy_b2c4876b3d}}" /></view>
          <view class="audit-empty-text">{{localeCopy.copy_86accadcca}}</view>
        </view>
      </view>

      <!-- Files -->
      <view class="card" wx:if="{{files.length}}">
        <view class="section-title">{{localeCopy.copy_d036aa723a}} ({{files.length}}{{localeCopy.copy_f34e9d1f5c}}</view>
        <view class="file-attach-item ui-motion-card" wx:for="{{files}}" wx:key="id" data-file-id="{{item.id}}" data-file-name="{{item.fileName}}" bindtap="previewFile" hover-class="ui-press-card" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">
          <view class="file-attach-icon"><ui-icon name="file" tone="primary" size="24" ariaLabel="{{localeCopy.copy_d036aa723a}}" /></view>
          <view class="file-attach-name">{{item.fileName}}</view>
        </view>
      </view>

      <!-- Signatures -->
      <view class="card" wx:if="{{signatures.length}}">
        <view class="section-title">{{localeCopy.copy_6a200ce590}} ({{signatures.length}}{{localeCopy.copy_7ccfef7ef3}}</view>
        <view class="audit-sig-card" wx:for="{{signatures}}" wx:key="id">
          <view class="list-name">{{item.signerName}} · {{item.signatureType === 'signature' ? localeCopy.copy_0c02992813 : localeCopy.copy_ed981b1744}} · {{localeCopy.copy_ef8b7ffa9b}}{{item.round}}{{localeCopy.copy_29f6c07d3f}}</view>
          <view class="list-desc"><text wx:if="{{item.signerStudentId}}">{{item.signerStudentId}} · </text>{{item.signedAt}}</view>
        </view>
      </view>

      <!-- Description display (view mode) -->
      <view class="card" wx:if="{{submission.description}}">
        <view class="section-title">{{localeCopy.copy_25e1199dc3}}</view>
        <view class="view-desc-text">{{submission.description}}</view>
      </view>

      <!-- Actions — submitter-only buttons -->
      <view class="card" wx:if="{{userIsSubmitter}}">
        <view class="section-title" wx:if="{{activeApprovalStepId}}">{{localeCopy.copy_e4a2b8faee}}</view>
        <view class="audit-btn-bar">
          <button class="secondary-btn ui-motion-button" wx:if="{{submission.status === 'draft' || submission.status === 'pending' || submission.status === 'rejected' || submission.status === 'withdrawn'}}" bindtap="enterEditMode" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_2415963f64}}</button>
          <button class="primary-btn ui-motion-button" wx:if="{{submission.status === 'pending' || submission.status === 'rejected' || submission.status === 'withdrawn'}}" bindtap="resubmit" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_0a01cb3a51}}</button>
          <button class="secondary-btn danger-btn ui-motion-button" wx:if="{{submission.status === 'in_progress' || submission.status === 'rejected'}}" bindtap="withdraw" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_60dd7897d1}}</button>
        </view>
      </view>

      <!-- ═══════════ Edit Mode ═══════════ -->
      <view wx:if="{{editMode}}">
        <view class="card">
          <view class="section-title">{{localeCopy.copy_2415963f64}}</view>
        </view>

        <view class="card">
          <view class="section-title">{{localeCopy.copy_c5ec30e07d}}</view>
          <view class="button-row">
            <button class="{{editType === 'template' ? 'primary-btn' : 'secondary-btn'}} ui-motion-button" data-type="template" bindtap="onEditTypeChange" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_15b653039c}}</button>
            <button class="{{editType === 'ad_hoc' ? 'primary-btn' : 'secondary-btn'}} ui-motion-button" data-type="ad_hoc" bindtap="onEditTypeChange" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_f49717b207}}</button>
          </view>
        </view>

        <!-- Basic info -->
        <view class="card">
          <view class="section-title">{{localeCopy.copy_66240d9d99}}</view>
          <view class="field">
            <view class="field-label">{{localeCopy.copy_30f93d24ca}}</view>
            <input class="field-input" value="{{editTitle}}" bindinput="onEditTitleInput" placeholder="{{localeCopy.copy_3ebd5d2cf0}}" />
          </view>
          <view class="field">
            <view class="field-label">{{localeCopy.copy_25e1199dc3}}</view>
            <textarea class="field-textarea" auto-height maxlength="-1" value="{{editDesc}}" bindinput="onEditDescInput" placeholder="{{localeCopy.copy_3234735724}}" ></textarea>
          </view>
        </view>

        <!-- 模板步骤：仅在模板允许时提供第一步审批人重指定。 -->
        <view class="card" wx:if="{{editType === 'template' && editTemplateId && templatePreviewSteps.length}}">
          <view class="section-title">{{localeCopy.copy_5d2dc79b89}}{{templatePreviewSteps.length}}{{localeCopy.copy_16d27aa59e}}</view>
          <view class="section-desc section-desc-sm">{{localeCopy.copy_34f5bab454}}</view>
          <view class="tpl-step-preview-item" wx:for="{{templatePreviewSteps}}" wx:key="stepIndex">
            <view class="tpl-step-preview-main">
              <view class="tpl-step-preview-num">{{item.name || (localeCopy.copy_93c50c01c0 + (item.sortOrder || (index + 1)) + localeCopy.copy_493a127a99)}}</view>
              <view class="tpl-step-preview-desc">{{item.approverDesc || localeCopy.copy_ae42f47cf6}}</view>
              <view class="tpl-step-preview-action">{{item.actionLabel || localeCopy.copy_c9695bb971}}</view>
              <view class="tpl-step-preview-designation" wx:if="{{item.stepIndex === 1 && item.allowApproverDesignation}}">
                <text class="audit-link-btn ui-motion-chip" catchtap="openTemplateStepPersonPicker" data-step-index="1" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{templateStepOverrides.length && templateStepOverrides[0].personHrIds.length ? localeCopy.copy_d5fa17c99b : localeCopy.copy_2aad29e5cf}}</text>
                <view wx:if="{{templateStepOverrides.length && templateStepOverrides[0].personHrNames.length}}" class="tpl-step-preview-persons">
                  <view class="tpl-step-person-tag" wx:for="{{templateStepOverrides[0].personHrNames}}" wx:for-item="pname" wx:for-index="pidx" wx:key="pname">
                    <text>{{pname}}</text>
                    <text class="tpl-step-person-remove ui-motion-chip" data-step-index="1" data-hr-id="{{templateStepOverrides[0].personHrIds[pidx]}}" catchtap="removeTemplateStepOverridePerson" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">×</text>
                  </view>
                </view>
              </view>
            </view>
            <view class="tpl-step-preview-mode" wx:if="{{!(item.stepIndex === 1 && item.allowApproverDesignation)}}">
              <text class="tpl-step-fixed-mode">{{localeCopy.copy_46a814a2c4}}</text>
            </view>
          </view>
        </view>

        <!-- Ad-hoc steps editor -->
        <view class="card" wx:if="{{editType === 'ad_hoc'}}">
          <view class="section-title">{{localeCopy.copy_265568fe85}} ({{editSteps.length}}{{localeCopy.copy_ef0bb32ca6}}</view>
          <view class="ad-hoc-step-item" wx:for="{{editSteps}}" wx:key="index">
            <view class="ad-hoc-step-main">
              <view class="ad-hoc-step-title">{{localeCopy.copy_ef8b7ffa9b}}{{index + 1}}{{localeCopy.copy_9e545997b0}} · {{item.actionType === 'pass' ? localeCopy.copy_8e2f75159e : (item.actionType === 'sign' ? localeCopy.copy_49cbf30d6b : (item.actionType === 'estamp' ? localeCopy.copy_7e6630535d : localeCopy.copy_a63d02480e))}}</view>
              <view class="ad-hoc-step-desc">{{item.approverDesc || localeCopy.copy_9f142da1e5}}</view>
              <!-- Show individual condition lines for OR logic -->
              <view wx:for="{{item.stepConditionsDisplay}}" wx:for-item="cond" wx:key="*this" wx:if="{{item.stepConditionsDisplay.length > 1}}" class="edit-cond-line">{{cond}}</view>
            </view>
            <view class="list-actions card-actions card-actions--inline"><text class="audit-link-btn audit-link-danger ui-motion-chip" data-index="{{index}}" bindtap="removeEditStep" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_c3e182f0c6}}</text></view>
          </view>
          <view class="audit-empty-state" wx:if="{{!editSteps.length}}">
            <view class="audit-empty-text">{{localeCopy.copy_84e125402d}}</view>
          </view>
          <view class="button-row compact-row">
            <button class="secondary-btn ui-motion-button" bindtap="openEditStepEditor" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_751c159dd2}}</button>
            <button class="secondary-btn ui-motion-button" bindtap="openEditPersonPicker" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_0b61c482f3}}</button>
          </view>
          <view class="field field-mt-16">
            <view class="field-label">{{localeCopy.copy_8d3aac9d85}}</view>
            <picker range="{{[localeCopy.copy_3cd3d7a8c2,localeCopy.copy_7897981e63]}}" bindchange="onEditResubmitModeChange">
              <view class="picker-value">{{editResubmitMode === 'fresh' ? localeCopy.copy_3cd3d7a8c2 : localeCopy.copy_7897981e63}}</view>
            </picker>
          </view>
        </view>

        <!-- Files -->
        <view class="card">
          <view class="section-title">{{localeCopy.copy_b33e8e9a52}} ({{editFiles.length}}{{localeCopy.copy_f34e9d1f5c}}</view>
          <view class="file-attach-item" wx:for="{{editFiles}}" wx:key="id">
            <view class="file-attach-icon"><ui-icon name="file" tone="primary" size="24" ariaLabel="{{localeCopy.copy_d036aa723a}}" /></view>
            <view class="file-attach-name">{{item.fileName}}</view>
            <view class="list-actions card-actions card-actions--inline"><text class="audit-link-btn audit-link-danger ui-motion-chip" data-index="{{index}}" bindtap="removeEditFile" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_c3e182f0c6}}</text></view>
          </view>
          <view class="section-title field-mt-16" wx:if="{{editNewFiles.length}}">{{localeCopy.copy_165509f937}} ({{editNewFiles.length}}{{localeCopy.copy_f34e9d1f5c}}</view>
          <view class="file-attach-item" wx:for="{{editNewFiles}}" wx:key="fileId">
            <view class="file-attach-icon"><ui-icon name="file" tone="primary" size="24" ariaLabel="{{localeCopy.copy_d036aa723a}}" /></view>
            <view class="file-attach-name">{{item.fileName}}</view>
            <view class="list-actions card-actions card-actions--inline"><text class="audit-link-btn audit-link-danger ui-motion-chip" data-index="{{index}}" bindtap="removeEditNewFile" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_c3e182f0c6}}</text></view>
          </view>
          <view class="button-row compact-row">
            <button class="secondary-btn ui-motion-button" bindtap="editChooseImage" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_53ce695b90}}</button>
            <button class="secondary-btn ui-motion-button" bindtap="editChooseFile" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_98aea69c53}}</button>
          </view>
        </view>

        <view class="card">
          <view class="audit-btn-bar">
            <button class="primary-btn ui-motion-button" loading="{{loading}}" bindtap="saveEdit" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_7214028117}}</button>
            <button class="secondary-btn ui-motion-button" bindtap="cancelEdit" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_06dbb49961}}</button>
          </view>
        </view>
      </view>

      <!-- ═══ Edit: Identity step editor popup ═══ -->
      <viewport-portal wx:if="{{editStepEditorVisible}}">
      <view class="popup-mask ui-overlay" wx:if="{{editStepEditorVisible}}" bindtap="closeEditStepEditor">
        <view class="ui-overlay-blocker" catchtouchmove="noop"></view>
        <view class="popup-card ui-dialog-shell ui-dialog-shell--complex" catchtap="noop">
          <view class="popup-header ui-dialog-header"><view class="popup-title">{{localeCopy.copy_7963fb24a5}} — {{localeCopy.copy_6cd86bd7ed}}</view><text class="popup-close ui-motion-chip" catchtap="closeEditStepEditor" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_09614cef6c}}</text></view>
          <scroll-view enhanced="{{true}}" show-scrollbar="{{true}}" bounces="{{true}}" nested-scroll-enabled="{{true}}" class="popup-scroll ui-dialog-body ui-dialog-content ui-dialog-scroll--fill" scroll-y>
            <view class="detail-popup-form ui-dialog-stack">
              <view class="field">
                <view class="field-label">{{localeCopy.copy_c5dd070c37}}</view>
                <input class="field-input" value="{{editStepForm.name}}" data-field="name" bindinput="onEditStepFieldInput" placeholder="{{localeCopy.copy_ac4d7a45e0}}"/>
              </view>
              <view class="field">
                <view class="field-label">{{localeCopy.copy_dc449bac28}}</view>
                <picker range="{{[localeCopy.copy_8e2f75159e,localeCopy.copy_49cbf30d6b,localeCopy.copy_7e6630535d,localeCopy.copy_a63d02480e]}}" bindchange="onEditActionTypeChange">
                  <view class="picker-value">{{editStepForm.actionType === 'pass' ? localeCopy.copy_8e2f75159e : (editStepForm.actionType === 'sign' ? localeCopy.copy_49cbf30d6b : (editStepForm.actionType === 'estamp' ? localeCopy.copy_7e6630535d : localeCopy.copy_a63d02480e))}}</view>
                </picker>
              </view>
              <view class="field">
                <view class="field-label">{{localeCopy.copy_8f5c5f658c}}</view>
                <picker range="{{identityPickerScopeOptions}}" bindchange="onEditIdentityScopeChange">
                  <view class="picker-value">{{identityPickerScopeOptions[editIdentityPickerScopeIndex] || localeCopy.copy_76d431a4dc}}</view>
                </picker>
              </view>
              <view class="field" wx:if="{{identityPickerScopeValues[editIdentityPickerScopeIndex] === 'specific_department' || identityPickerScopeValues[editIdentityPickerScopeIndex] === 'specific_work_group'}}">
                <view class="field-label">{{localeCopy.copy_f1643742d1}}</view>
                <picker range="{{identityPickerDeptOptions}}" bindchange="onEditIdentityDeptChange">
                  <view class="picker-value">{{identityPickerDeptOptions[editIdentityPickerDeptIndex] || localeCopy.copy_eada426deb}}</view>
                </picker>
              </view>
              <view class="field" wx:if="{{identityPickerScopeValues[editIdentityPickerScopeIndex] === 'specific_work_group'}}">
                <view class="field-label">{{localeCopy.copy_237707b205}}</view>
                <picker range="{{identityPickerWgOptions}}" bindchange="onEditIdentityWgChange">
                  <view class="picker-value">{{identityPickerWgOptions[editIdentityPickerWgIndex] || localeCopy.copy_ec3b03ecc7}}</view>
                </picker>
              </view>
              <view class="field">
                <view class="field-label">{{localeCopy.copy_4711f10351}}</view>
                <picker range="{{identityPickerIdentOptions}}" bindchange="onEditIdentityIdentChange">
                  <view class="picker-value">{{identityPickerIdentOptions[editIdentityPickerIdentIndex] || localeCopy.copy_d1856227b6}}</view>
                </picker>
              </view>
            </view>
          </scroll-view>
          <view class="audit-btn-bar ui-dialog-footer"><button class="primary-btn ui-motion-button" catchtap="confirmEditIdentityStep" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_a58e97a9f8}}</button></view>
        </view>
      </view>
      </viewport-portal>

      <!-- ═══ Edit: Person picker popup ═══ -->
      <viewport-portal wx:if="{{editPersonPickerVisible}}">
      <view class="popup-mask ui-overlay" wx:if="{{editPersonPickerVisible}}" bindtap="closeEditPersonPicker">
        <view class="ui-overlay-blocker" catchtouchmove="noop"></view>
        <view class="popup-card desig-popup-card ui-dialog-shell ui-dialog-shell--complex" catchtap="noop">
          <view class="popup-header ui-dialog-header"><view class="popup-title">{{localeCopy.copy_759682a69b}}</view><text class="popup-close ui-motion-chip" catchtap="closeEditPersonPicker" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_09614cef6c}}</text></view>
          <scroll-view enhanced="{{true}}" show-scrollbar="{{true}}" bounces="{{true}}" nested-scroll-enabled="{{true}}" scroll-y class="ui-dialog-body ui-dialog-content ui-dialog-scroll--fill ui-dialog-stack-scroll">
          <view class="list-title">{{localeCopy.copy_b45ad4069d}}{{editPersonPickerSelectedIds.length}}{{localeCopy.copy_72dd9e6203}}</view>
          <scroll-view enhanced="{{true}}" show-scrollbar="{{true}}" bounces="{{true}}" nested-scroll-enabled="{{true}}" scroll-y class="desig-inner-scroll desig-current-scroll ui-dialog-scroll--pane ui-dialog-selection-current">
            <view class="audit-person-item" wx:for="{{editPersonPickerSelectedList}}" wx:key="id">
              <view class="audit-person-avatar">{{item.name[0]}}</view>
              <view class="audit-person-info">
                <view class="audit-person-name">{{item.name}}</view>
                <view class="audit-person-detail">{{item.department}} · {{item.identity}} · {{item.studentId}}</view>
              </view>
              <view class="inline-actions list-actions-below card-actions">
                <text class="audit-link-btn audit-link-danger ui-motion-chip" data-hr-id="{{item.id}}" catchtap="onEditPersonToggle" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_c3e182f0c6}}</text>
              </view>
            </view>
            <view class="empty-inline" wx:if="{{!editPersonPickerSelectedList.length}}">{{localeCopy.copy_45ef5ac49b}}</view>
            <view class="scroll-tail-space"></view>
          </scroll-view>
          <view class="list-title field-mt-8">{{localeCopy.copy_8b1a42b718}}</view>
          <view class="rule-filter-row ui-inline-field-row">
            <view class="rule-filter-field"><view class="field-label">{{localeCopy.copy_02419589be}}</view><picker range="{{personPickerDeptOpts}}" bindchange="onEditPersonPickerDeptChange"><view class="picker-value compact-picker-value">{{editPersonPickerDept}}</view></picker></view>
            <view class="rule-filter-field"><view class="field-label">{{localeCopy.copy_c1965e0690}}</view><picker range="{{personPickerIdentOpts}}" bindchange="onEditPersonPickerIdentChange"><view class="picker-value compact-picker-value">{{editPersonPickerIdent}}</view></picker></view>
            <view class="rule-filter-field"><view class="field-label">{{localeCopy.copy_303b7a8611}}</view><picker range="{{personPickerWgOpts}}" bindchange="onEditPersonPickerWgChange"><view class="picker-value compact-picker-value">{{editPersonPickerWg}}</view></picker></view>
          </view>
          <view class="field field-mb-8"><input class="field-input" placeholder="{{localeCopy.copy_42dba54426}}" value="{{editPersonPickerKeyword}}" bindinput="onEditPersonPickerSearch"/></view>
          <scroll-view enhanced="{{true}}" show-scrollbar="{{true}}" bounces="{{true}}" nested-scroll-enabled="{{true}}" scroll-y class="desig-inner-scroll desig-candidate-scroll ui-dialog-scroll--pane ui-dialog-selection-options">
            <view wx:if="{{editPersonPickerLoading}}" class="empty-inline">{{localeCopy.copy_213b890c07}}</view>
            <view wx:elif="{{!editPersonPickerCandidates.length}}" class="empty-inline">{{localeCopy.copy_3f9ec35c15}}</view>
            <view wx:if="{{!editPersonPickerLoading}}" class="audit-person-item ui-motion-card" wx:for="{{editPersonPickerCandidates}}" wx:key="id" data-hr-id="{{item.id}}" catchtap="onEditPersonToggle" hover-class="ui-press-card" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">
              <view class="audit-person-avatar">{{item.name[0]}}</view>
              <view class="audit-person-info">
                <view class="audit-person-name"><view class="select-chip select-chip-inline {{item.isSelected ? 'select-chip-active' : ''}}">{{item.isSelected ? localeCopy.copy_78564bdb46 : localeCopy.copy_c47f370afa}}</view>{{item.name}}</view>
                <view class="audit-person-detail">{{item.studentId}} · {{item.department}} · {{item.identity}}<text wx:if="{{item.workGroup}}"> · {{item.workGroup}}</text></view>
              </view>
            </view>
            <view class="scroll-tail-space"></view>
          </scroll-view>
          <view class="field field-mt-8">
            <view class="field-label">{{localeCopy.copy_dc449bac28}}</view>
            <picker range="{{[localeCopy.copy_8e2f75159e,localeCopy.copy_49cbf30d6b,localeCopy.copy_7e6630535d,localeCopy.copy_a63d02480e]}}" bindchange="onEditPersonPickerActionTypeChange">
              <view class="picker-value">{{editPersonPickerStepActionType === 'pass' ? localeCopy.copy_8e2f75159e : (editPersonPickerStepActionType === 'sign' ? localeCopy.copy_49cbf30d6b : (editPersonPickerStepActionType === 'estamp' ? localeCopy.copy_7e6630535d : localeCopy.copy_a63d02480e))}}</view>
            </picker>
          </view>
          </scroll-view>
          <view class="button-row ui-dialog-footer">
            <button class="secondary-btn ui-motion-button" catchtap="closeEditPersonPicker" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_06dbb49961}}</button>
            <button class="primary-btn ui-motion-button" catchtap="confirmEditPersonPicker" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_9aa3c3673c}} {{editPersonPickerSelectedIds.length}} {{localeCopy.copy_2370e6144f}}</button>
          </view>
        </view>
      </view>
      </viewport-portal>
    </block>
  </view>

  <!-- ═══ Stamp Picker Popup ═══ -->
  <viewport-portal wx:if="{{stampPickerVisible}}">
  <view class="popup-mask ui-overlay" wx:if="{{stampPickerVisible}}" bindtap="closeStampPicker">
    <view class="ui-overlay-blocker" catchtouchmove="noop"></view>
    <view class="popup-card ui-dialog-shell ui-dialog-shell--complex" catchtap="noop">
      <view class="popup-header ui-dialog-header">
        <view class="popup-title">{{localeCopy.copy_dd46cd92a6}}</view>
        <text class="popup-close ui-motion-chip" catchtap="closeStampPicker" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_09614cef6c}}</text>
      </view>
      <scroll-view enhanced="{{true}}" show-scrollbar="{{true}}" bounces="{{true}}" nested-scroll-enabled="{{true}}" class="popup-scroll ui-dialog-body ui-dialog-content ui-dialog-scroll--fill" scroll-y>
        <view class="detail-popup-form ui-dialog-stack">
          <view wx:if="{{!availableStamps.length}}" class="stamp-empty">{{localeCopy.copy_da41cca7fe}}</view>
          <view wx:for="{{availableStamps}}" wx:key="id"
                class="stamp-picker-item ui-motion-card"
                data-stamp-id="{{item.id}}" data-stamp-name="{{item.name}}" data-stamp-image="{{item.imageData}}"
                catchtap="onStampSelect" hover-class="ui-press-card" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">
            <image wx:if="{{item.imageData}}" src="{{item.imageData}}" mode="aspectFit" class="stamp-picker-thumb" />
            <view wx:else class="stamp-picker-placeholder"><view class="stamp-placeholder-mark"></view></view>
            <text class="stamp-picker-name">{{item.name}}</text>
            <text class="stamp-picker-select">{{localeCopy.copy_896f105132}}</text>
          </view>
        </view>
      </scroll-view>
    </view>
  </view>
  </viewport-portal>

  <!-- ═══ Placement / File Preview Popup (positioning sig/stamp on file) ═══ -->
  <viewport-portal wx:if="{{placementVisible}}">
  <view class="popup-mask ui-overlay" wx:if="{{placementVisible}}" bindtap="closePlacement">
    <view class="ui-overlay-blocker" catchtouchmove="noop"></view>
    <view class="popup-card placement-popup ui-dialog-shell ui-dialog-shell--wide ui-data-grid--specialized" catchtap="noop">
      <view class="popup-header ui-dialog-header">
        <view class="popup-title">{{localeCopy.copy_17a7472824}} · {{placementFileName}}</view>
        <text class="popup-close ui-motion-chip" catchtap="closePlacement" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_09614cef6c}}</text>
      </view>

      <!-- Hint -->
      <view class="placement-hint">
        {{localeCopy.copy_7f23b372a9}}{{placementType === 'stamp' ? localeCopy.copy_ed981b1744 : localeCopy.copy_0c02992813}}{{localeCopy.copy_8980340210}}
        <text wx:if="{{placementCurrentPage > 0 && placementTotalPages > 1}}" class="placement-hint-page">
          · {{localeCopy.copy_95cf78856c}}{{placementCurrentPage}}{{localeCopy.copy_999a2408c6}}
        </text>
      </view>

      <!-- Page selector for PDFs -->
      <view wx:if="{{placementFileMime === 'application/pdf' && placementTotalPages > 1}}"
            class="placement-page-selector">
        <text class="placement-page-btn ui-motion-chip"
              data-dir="prev" catchtap="onPlacementPageChange"
              wx:if="{{placementCurrentPage > 1}}"
              hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">◀ {{localeCopy.copy_6befa183d0}}</text>
        <text class="placement-page-num">{{localeCopy.copy_ef8b7ffa9b}} {{placementCurrentPage}} / {{placementTotalPages}} {{localeCopy.copy_999a2408c6}}</text>
        <text class="placement-page-btn ui-motion-chip"
              data-dir="next" catchtap="onPlacementPageChange"
              wx:if="{{placementCurrentPage < placementTotalPages}}" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_0f143c0d61}} ▶</text>
      </view>

      <!-- Placement canvas — scrollable to show full file -->
      <scroll-view id="placementCanvas"
            enhanced="{{true}}"
            show-scrollbar="{{true}}"
            bounces="{{true}}"
            nested-scroll-enabled="{{true}}"
            scroll-y="{{true}}"
            class="placement-canvas ui-dialog-body ui-dialog-content ui-dialog-content--workspace ui-dialog-scroll--fill"
            bindtap="onPlacementTap">
        <!-- Loading indicator -->
        <view wx:if="{{placementLoading}}"
              class="placement-loading">
          <ui-icon name="clock" tone="muted" size="24" ariaLabel="{{localeCopy.copy_a31db08a14}}" />
          <text>{{localeCopy.copy_d012f0d85e}}</text>
        </view>
        <!-- File preview (image or rendered PDF page) -->
        <block wx:elif="{{placementFileImage}}">
          <view class="placement-img-wrap">
            <image id="placementPreviewImage" src="{{placementFileImage}}" mode="widthFix"
                   class="placement-preview-img" />
            <!-- Overlaid signatures/stamps -->
            <block wx:for="{{placementItems}}" wx:key="dispIdx" wx:for-item="item">
              <block wx:if="{{!placementFileMime || placementFileMime.indexOf('pdf') < 0 || item.page === placementCurrentPage}}">
                <image src="{{item.previewSrc}}"
                       mode="widthFix"
                       data-sig-idx="{{item.dispIdx}}"
                       catchtap="onPlacementItemTap"
                       class="placement-sign-overlay {{placementActiveIdx === item.dispIdx ? 'placement-sign-overlay-active' : ''}} ui-motion-card"
                       style="left:{{item.positionX * 100}}%;top:{{item.positionY * 100}}%;width:{{18 * (item.size || 1)}}%;transform:translate(-50%, -50%) rotate({{item.rotation || 0}}deg);"  hover-class="ui-press-card" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}"/>
              </block>
            </block>
            <!-- Crosshair showing current position -->
            <view wx:if="{{placementPreviewX >= 0}}" class="placement-crosshair placement-crosshair-v" style="left:{{placementPreviewX * 100}}%;top:{{placementPreviewY * 100}}%;"></view>
            <view wx:if="{{placementPreviewX >= 0}}" class="placement-crosshair placement-crosshair-h" style="left:{{placementPreviewX * 100}}%;top:{{placementPreviewY * 100}}%;"></view>
          </view>
        </block>
        <!-- Fallback: no preview available -->
        <view wx:else class="placement-fallback">
          <view class="placement-fallback-icon"><ui-icon name="file" tone="muted" size="48" ariaLabel="{{localeCopy.copy_9b8709737d}}" /></view>
          <view>{{placementFileName}}</view>
          <view class="placement-fallback-sub">
            {{placementFileMime === 'application/pdf' ? localeCopy.copy_5833eb9b10 + placementCurrentPage + localeCopy.copy_f67213967c : localeCopy.copy_d14d285351 + (placementType === 'stamp' ? localeCopy.copy_ed981b1744 : localeCopy.copy_0c02992813)}}
          </view>
        </view>
      </scroll-view>

      <!-- Position info -->
      <view wx:if="{{placementPreviewX >= 0 && placementPosText}}" class="placement-pos-info">
        {{localeCopy.copy_5d46d06a0e}}{{placementPosText}}
        <text wx:if="{{placementCurrentPage > 0 && placementTotalPages > 1}}"> · {{localeCopy.copy_ef8b7ffa9b}}{{placementCurrentPage}}{{localeCopy.copy_999a2408c6}}</text>
      </view>

      <view class="placement-transform-panel" wx:if="{{placementActiveIdx >= 0}}">
        <view class="placement-transform-row">
          <text class="placement-transform-label">{{localeCopy.copy_76c0925fc7}} {{placementSize}}%</text>
          <slider class="placement-slider" min="50" max="220" step="5" value="{{placementSize}}" activeColor="#2563eb" backgroundColor="#dbeafe" block-size="18" bindchanging="onPlacementSizeChange" bindchange="onPlacementSizeChange" />
        </view>
        <view class="placement-transform-row">
          <text class="placement-transform-label">{{localeCopy.copy_a19ba476ce}} {{placementRotation}}°</text>
          <slider class="placement-slider" min="-180" max="180" step="5" value="{{placementRotation}}" activeColor="#2563eb" backgroundColor="#dbeafe" block-size="18" bindchanging="onPlacementRotationChange" bindchange="onPlacementRotationChange" />
        </view>
        <view class="placement-rotate-actions">
          <button class="secondary-btn placement-mini-btn ui-motion-button" data-delta="-15" catchtap="nudgePlacementRotation" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_9ab49ffac1}}</button>
          <button class="secondary-btn placement-mini-btn ui-motion-button" catchtap="resetPlacementTransform" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_a7f220a4a7}}</button>
          <button class="secondary-btn placement-mini-btn ui-motion-button" data-delta="15" catchtap="nudgePlacementRotation" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_38ca054171}}</button>
        </view>
      </view>

      <view class="placement-actions ui-dialog-footer">
        <button class="secondary-btn placement-btn-cancel ui-motion-button" catchtap="closePlacement" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_06dbb49961}}</button>
        <button class="secondary-btn placement-btn-copy ui-motion-button" catchtap="addPlacementCopy" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_784c704c64}}</button>
        <button class="primary-btn placement-btn-save ui-motion-button" catchtap="confirmPlacement" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_8f10667855}}</button>
      </view>
    </view>
  </view>
  </viewport-portal>

  <!-- Approval Dialog -->
  <viewport-portal wx:if="{{approvalVisible}}">
  <view class="popup-mask ui-overlay" wx:if="{{approvalVisible}}" bindtap="closeApproval">
    <view class="ui-overlay-blocker" catchtouchmove="noop"></view>
    <view class="popup-card ui-dialog-shell ui-dialog-shell--complex" catchtap="noop">
      <view class="popup-header ui-dialog-header">
        <view class="popup-title">{{approvalAction === 'approve' ? localeCopy.copy_126a0e1f4c : localeCopy.copy_b4432643e3}}</view>
        <text class="popup-close ui-motion-chip" catchtap="closeApproval" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_09614cef6c}}</text>
      </view>
      <scroll-view enhanced="{{true}}" show-scrollbar="{{true}}" bounces="{{true}}" nested-scroll-enabled="{{true}}" class="popup-scroll ui-dialog-body ui-dialog-content ui-dialog-scroll--fill" scroll-y>
        <view class="detail-popup-form ui-dialog-stack">
          <!-- Signature section (only for approve) -->
          <view wx:if="{{approvalAction === 'approve'}}">
            <view class="list-title">{{localeCopy.copy_23590ac13f}}</view>
            <view class="sign-file-card" wx:for="{{pendingSignatures}}" wx:key="index">
              <view class="sign-file-main">
                <view class="sign-file-name">{{localeCopy.copy_76dac19a0a}}{{index + 1}}</view>
                <image wx:if="{{item.imageData}}" src="{{item.imageData}}" mode="widthFix" class="sign-file-preview" />
              </view>
              <view class="list-actions card-actions card-actions--inline"><text class="audit-link-btn audit-link-danger ui-motion-chip" data-index="{{index}}" catchtap="removePendingSignature" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_c3e182f0c6}}</text></view>
            </view>
            <view class="button-row compact-row">
              <button class="secondary-btn ui-motion-button" wx:for="{{files}}" wx:key="id" data-file-id="{{item.id}}" catchtap="openSignaturePad" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_e012d6739e}} {{item.fileName}}</button>
            </view>
            <view class="field approval-dialog-field-mt">
              <view class="field-label">{{localeCopy.copy_41c20cc066}}</view>
              <textarea class="field-textarea" auto-height maxlength="-1" value="{{approvalComment}}" bindinput="onApprovalCommentInput" placeholder="{{localeCopy.copy_7aeee7513d}}" />
            </view>
          </view>

          <!-- Reject reason -->
          <view wx:if="{{approvalAction === 'reject'}}">
            <view class="field">
              <view class="field-label">{{localeCopy.copy_47a12cc974}}</view>
              <textarea class="field-textarea" auto-height maxlength="-1" value="{{rejectionReason}}" bindinput="onRejectionReasonInput" placeholder="{{localeCopy.copy_a2d2583699}}" />
            </view>
          </view>

        </view>
      </scroll-view>
      <view class="audit-btn-bar ui-dialog-footer">
        <button class="primary-btn ui-motion-button" loading="{{loading}}" catchtap="confirmApproval" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{approvalAction === 'approve' ? localeCopy.copy_49ef170f0b : localeCopy.copy_cd42af3f87}}</button>
      </view>
    </view>
  </view>
  </viewport-portal>

  <!-- ═══ Signature Source Picker Popup (saved signatures + new drawing) ═══ -->
  <viewport-portal wx:if="{{sigSourcePickerVisible}}">
  <view class="popup-mask ui-overlay" wx:if="{{sigSourcePickerVisible}}" bindtap="closeSigSourcePicker">
    <view class="ui-overlay-blocker" catchtouchmove="noop"></view>
    <view class="popup-card ui-dialog-shell ui-dialog-shell--complex" catchtap="noop">
      <view class="popup-header ui-dialog-header">
        <view class="popup-title">{{localeCopy.copy_2649ec5bae}}</view>
        <text class="popup-close ui-motion-chip" catchtap="closeSigSourcePicker" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_09614cef6c}}</text>
      </view>
      <scroll-view enhanced="{{true}}" show-scrollbar="{{true}}" bounces="{{true}}" nested-scroll-enabled="{{true}}" class="popup-scroll ui-dialog-body ui-dialog-content ui-dialog-scroll--fill" scroll-y>
        <view class="detail-popup-form ui-dialog-stack">
          <view class="sig-source-file-name">{{sigSourceFileName}}</view>

          <!-- Saved Signatures -->
          <view wx:if="{{mySignatures.length}}" class="sig-source-section">
            <view class="sig-source-section-title">{{localeCopy.copy_290f4e2426}}</view>
            <view wx:for="{{mySignatures}}" wx:key="id"
                  class="sig-source-item ui-motion-card"
                  data-sig-id="{{item.id}}" data-sig-image="{{item.imageData}}"
                  catchtap="onSelectSavedSignature" hover-class="ui-press-card" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">
              <image wx:if="{{item.imageData}}" src="{{item.imageData}}" mode="aspectFit"
                     class="sig-source-thumb" />
              <view wx:else class="sig-source-thumb-placeholder"><ui-icon name="signature" tone="muted" size="38" ariaLabel="{{localeCopy.copy_76dac19a0a}}" /></view>
              <view class="sig-source-item-info">
                <view class="sig-source-item-name">{{item.name}}</view>
                <view wx:if="{{item.isDefault}}" class="sig-source-default-badge">{{localeCopy.copy_4d6d7783a5}}</view>
              </view>
              <text class="sig-source-use-btn">{{localeCopy.copy_8b55fdc995}}</text>
            </view>
          </view>

          <!-- No saved signatures -->
          <view wx:if="{{!mySignatures.length}}" class="sig-source-empty">
            {{localeCopy.copy_0f88ef1634}}
          </view>

          <!-- Divider -->
          <view class="sig-source-divider">
            <view class="sig-source-divider-line"></view>
            <text class="sig-source-divider-text">{{localeCopy.copy_d7c8fd23a8}}</text>
            <view class="sig-source-divider-line"></view>
          </view>

          <!-- Draw new signature -->
          <view class="sig-source-draw-section">
            <view class="sig-source-draw-title">{{localeCopy.copy_01c2184200}}</view>
            <!-- Save option -->
            <view class="sig-source-save-row ui-motion-card" catchtap="onSigSaveToggle" hover-class="ui-press-card" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">
              <view style="width:36rpx;height:36rpx;border-radius:6rpx;border:2rpx solid {{sigSaveNew ? '#3b82f6' : '#cbd5e1'}};background:{{sigSaveNew ? '#3b82f6' : '#fff'}};display:flex;align-items:center;justify-content:center;flex-shrink:0;">
                <text wx:if="{{sigSaveNew}}" class="sig-source-check-icon">✓</text>
              </view>
              <text class="sig-source-save-label">{{localeCopy.copy_777df8daa0}}</text>
            </view>
            <view wx:if="{{sigSaveNew}}" class="sig-source-save-input-wrap">
              <input class="sig-source-save-input"
                     placeholder="{{localeCopy.copy_1d953e38dc}}" value="{{sigSaveName}}" bindinput="onSigSaveNameInput" />
            </view>
            <button class="primary-btn sig-source-draw-btn ui-motion-button" catchtap="onOpenNewSignaturePad" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_a9df85e7e6}}</button>
          </view>
        </view>
      </scroll-view>
    </view>
  </view>
  </viewport-portal>

  <!-- Signature Pad Popup (with save-to-library controls) -->
  <!-- Canvas 自己 catch 触摸；祖先不得拦截，否则会破坏连续笔迹。 -->
  <view class="popup-mask ui-overlay ui-dialog-touch-lock signature-pad-overlay" wx:if="{{signaturePadVisible}}" bindtap="closeSignaturePad">
    <view class="ui-overlay-blocker" catchtouchmove="noop"></view>
    <view class="popup-card signature-popup-card ui-dialog-touch-lock" catchtap="noop">
      <view class="popup-header ui-dialog-header">
        <view class="popup-title">{{localeCopy.copy_a9df85e7e6}}</view>
        <text class="popup-close ui-motion-chip" catchtap="closeSignaturePad" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_09614cef6c}}</text>
      </view>
      <view class="ui-dialog-body ui-dialog-content ui-dialog-content--workspace signature-pad-dialog-body">
        <signature-pad id="signaturePad" bindconfirm="onSignatureConfirm" />
      </view>
    </view>
  </view>

    <!-- ═══ Person picker popup (multi-select) ═══ -->
  <viewport-portal wx:if="{{personPickerVisible}}">
  <view class="popup-mask ui-overlay" wx:if="{{personPickerVisible}}" bindtap="closePersonPicker">
    <view class="ui-overlay-blocker" catchtouchmove="noop"></view>
      <view class="popup-card desig-popup-card ui-dialog-shell ui-dialog-shell--complex" catchtap="noop">
        <view class="popup-header ui-dialog-header"><view class="popup-title">{{localeCopy.copy_759682a69b}}</view><text class="popup-close ui-motion-chip" catchtap="closePersonPicker" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_09614cef6c}}</text></view>
        <scroll-view enhanced="{{true}}" show-scrollbar="{{true}}" bounces="{{true}}" nested-scroll-enabled="{{true}}" scroll-y class="ui-dialog-body ui-dialog-content ui-dialog-scroll--fill ui-dialog-stack-scroll">

        <!-- Selected persons -->
        <view class="list-title">{{localeCopy.copy_b45ad4069d}}{{personPickerSelectedIds.length}}{{localeCopy.copy_72dd9e6203}}</view>
        <scroll-view enhanced="{{true}}" show-scrollbar="{{true}}" bounces="{{true}}" nested-scroll-enabled="{{true}}" scroll-y class="desig-inner-scroll desig-current-scroll ui-dialog-scroll--pane ui-dialog-selection-current">
          <view class="audit-person-item" wx:for="{{personPickerSelectedList}}" wx:key="id">
            <view class="audit-person-avatar">{{item.name[0]}}</view>
            <view class="audit-person-info">
              <view class="audit-person-name">{{item.name}}</view>
              <view class="audit-person-detail">{{item.department}} · {{item.identity}} · {{item.studentId}}</view>
            </view>
            <view class="inline-actions list-actions-below card-actions">
              <text class="audit-link-btn audit-link-danger ui-motion-chip" data-hr-id="{{item.id}}" catchtap="onPersonToggle" hover-class="ui-press-chip" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_c3e182f0c6}}</text>
            </view>
          </view>
          <view class="empty-inline" wx:if="{{!personPickerSelectedList.length}}">{{localeCopy.copy_45ef5ac49b}}</view>
          <view class="scroll-tail-space"></view>
        </scroll-view>

        <!-- Filters -->
        <view class="list-title field-mt-8">{{localeCopy.copy_8b1a42b718}}</view>
        <view class="rule-filter-row ui-inline-field-row">
          <view class="rule-filter-field">
            <view class="field-label">{{localeCopy.copy_02419589be}}</view>
            <picker range="{{personPickerDeptOpts}}" bindchange="onPersonPickerDeptChange">
              <view class="picker-value compact-picker-value">{{personPickerDept}}</view>
            </picker>
          </view>
          <view class="rule-filter-field">
            <view class="field-label">{{localeCopy.copy_c1965e0690}}</view>
            <picker range="{{personPickerIdentOpts}}" bindchange="onPersonPickerIdentChange">
              <view class="picker-value compact-picker-value">{{personPickerIdent}}</view>
            </picker>
          </view>
          <view class="rule-filter-field">
            <view class="field-label">{{localeCopy.copy_303b7a8611}}</view>
            <picker range="{{personPickerWgOpts}}" bindchange="onPersonPickerWgChange">
              <view class="picker-value compact-picker-value">{{personPickerWg}}</view>
            </picker>
          </view>
        </view>
        <view class="field field-mb-8">
          <input class="field-input" placeholder="{{localeCopy.copy_42dba54426}}" value="{{personPickerKeyword}}" bindinput="onPersonPickerSearch"/>
        </view>

        <!-- Candidate list -->
        <scroll-view enhanced="{{true}}" show-scrollbar="{{true}}" bounces="{{true}}" nested-scroll-enabled="{{true}}" scroll-y class="desig-inner-scroll desig-candidate-scroll ui-dialog-scroll--pane ui-dialog-selection-options">
          <view wx:if="{{personPickerLoading}}" class="empty-inline">{{localeCopy.copy_213b890c07}}</view>
          <view wx:elif="{{!personPickerCandidates.length}}" class="empty-inline">{{localeCopy.copy_3f9ec35c15}}</view>
          <view wx:if="{{!personPickerLoading}}" class="audit-person-item ui-motion-card" wx:for="{{personPickerCandidates}}" wx:key="id" data-hr-id="{{item.id}}" catchtap="onPersonToggle" hover-class="ui-press-card" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">
            <view class="audit-person-avatar">{{item.name[0]}}</view>
            <view class="audit-person-info">
              <view class="audit-person-name">
                <view class="select-chip select-chip-inline {{item.isSelected ? 'select-chip-active' : ''}}">{{item.isSelected ? localeCopy.copy_78564bdb46 : localeCopy.copy_c47f370afa}}</view>
                {{item.name}}
              </view>
              <view class="audit-person-detail">{{item.studentId}} · {{item.department}} · {{item.identity}}<text wx:if="{{item.workGroup}}"> · {{item.workGroup}}</text></view>
            </view>
          </view>
          <view class="scroll-tail-space"></view>
        </scroll-view>

        <!-- Action type (only for ad-hoc mode; template/designate-next modes inherit step definition) -->
        <view class="field field-mt-8" wx:if="{{templateOverrideStepIndex < 0 && personPickerMode !== 'designateNext'}}">
          <view class="field-label">{{localeCopy.copy_dc449bac28}}</view>
          <picker range="{{[localeCopy.copy_8e2f75159e,localeCopy.copy_49cbf30d6b,localeCopy.copy_7e6630535d,localeCopy.copy_a63d02480e]}}" bindchange="onPersonPickerActionTypeChange">
            <view class="picker-value">{{personPickerStepActionType === 'pass' ? localeCopy.copy_8e2f75159e : (personPickerStepActionType === 'sign' ? localeCopy.copy_49cbf30d6b : (personPickerStepActionType === 'estamp' ? localeCopy.copy_7e6630535d : localeCopy.copy_a63d02480e))}}</view>
          </picker>
        </view>
        </scroll-view>

        <view class="button-row ui-dialog-footer">
          <button class="secondary-btn ui-motion-button" catchtap="closePersonPicker" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_06dbb49961}}</button>
          <button class="primary-btn ui-motion-button" catchtap="confirmPersonPicker" hover-class="ui-press-button" hover-start-time="0" hover-stay-time="160" hover-stop-propagation="{{true}}">{{localeCopy.copy_9aa3c3673c}} {{personPickerSelectedIds.length}} {{localeCopy.copy_2370e6144f}}</button>
        </view>
      </view>
    </view>
  </viewport-portal>

  <view class="page-footer">
    <view class="footer-name">{{localeCopy.copy_aa4f0ed6cd}}</view>
    <view class="footer-org">{{localeCopy.copy_638da8ae60}}</view>
  </view>
</view>
